/**
 * Visual layout constants for the audio editor
 */

/**
 * Horizontal offset for clip content area
 * This creates space for the timeline ruler nubbin on the left
 * Clips cannot start before this offset, but track backgrounds extend to x=0
 */
export const CLIP_CONTENT_OFFSET = 12;

/* SPDX-License-Identifier: AGPL-3.0-only */

export const SOURCE_STATUS_COPY_BY_LOCALE = Object.freeze({
	en: Object.freeze({ loadFailed: '{name}: {message}' }),
	de: Object.freeze({ loadFailed: '{name}: {message}' }),
});

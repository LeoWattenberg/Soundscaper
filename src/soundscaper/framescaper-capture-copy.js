/* SPDX-License-Identifier: AGPL-3.0-only */

// The shared catalog keeps one import shape for both products. Soundscaper has
// no callable capture surface, so its product-selected renderer supplies no
// Framescaper capture strings.
export const FRAMESCAPER_CAPTURE_COPY_BY_LOCALE = Object.freeze({
	de: Object.freeze({}),
	en: Object.freeze({}),
});

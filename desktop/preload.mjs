/* Electron sandbox preload: restricted require exposes only Electron; Framescaper shares this contextBridge. */
const { contextBridge, ipcRenderer } = require('electron');
const PRELOAD_PRODUCT_ID = (globalThis.process?.argv ?? []).includes('--soundscaper-product=framescaper')
	? 'framescaper' : 'soundscaper';
const SOAK_DEBUG_ENABLED = PRELOAD_PRODUCT_ID === 'soundscaper'
	&& (globalThis.process?.argv ?? []).includes('--soundscaper-soak-debug');
/* Keys main may hold but the renderer may never see, whatever the shape. */
const PLUGIN_PATH_KEYS = new Set(['binaryPath', 'rootPath', 'path', 'absolutePath', 'filePath']);
const CHANNELS = Object.freeze({
	environment: 'soundscaper:v1:environment', soakDebugProcessMetrics: 'soundscaper:v1:soak-debug:process-metrics', chooseFiles: 'soundscaper:v1:files:choose', releaseRead: 'soundscaper:v1:files:release', chooseLinkedVideoOriginal: 'soundscaper:v1:linked-video:choose', loadLinkedVideoOriginal: 'soundscaper:v1:linked-video:load', reconcileLinkedVideoOriginals: 'soundscaper:v1:linked-video:reconcile', releaseLinkedVideoOriginal: 'soundscaper:v1:linked-video:release', chooseLinkedAudioOriginal: 'soundscaper:v1:linked-audio:choose', loadLinkedAudioOriginal: 'soundscaper:v1:linked-audio:load', reconcileLinkedOriginals: 'soundscaper:v1:linked-original:reconcile', releaseLinkedOriginal: 'soundscaper:v1:linked-original:release', chooseSaveTarget: 'soundscaper:v1:save:choose', beginWrite: 'soundscaper:v1:save:begin', writeChunk: 'soundscaper:v1:save:chunk', patchFinalPrefix: 'soundscaper:v1:save:prefix', finishWrite: 'soundscaper:v1:save:finish', abortWrite: 'soundscaper:v1:save:abort',
	helperProbeAvailability: 'soundscaper:v1:helper:probe-availability',
	helperProbeBegin: 'soundscaper:v1:helper:probe-begin',
	helperProbeAwait: 'soundscaper:v1:helper:probe-await',
	helperProbeCancel: 'soundscaper:v1:helper:probe-cancel',
	nativeAudioAvailability: 'soundscaper:v1:helper:native-audio-availability',
	nativeAudioInventory: 'soundscaper:v1:helper:native-audio-inventory',
	nativeAudioSetEnabled: 'soundscaper:v1:helper:native-audio-set-enabled',
	nativeAudioSessionOpen: 'soundscaper:v1:native-audio:session:open', nativeAudioSessionBind: 'soundscaper:v1:native-audio:session:bind', nativeAudioSessionStatus: 'soundscaper:v1:native-audio:session:status', nativeAudioSessionCalibrate: 'soundscaper:v1:native-audio:session:calibrate', nativeAudioSessionReport: 'soundscaper:v1:native-audio:session:report', nativeAudioSessionLoss: 'soundscaper:v1:native-audio:session:loss', nativeAudioSessionClose: 'soundscaper:v1:native-audio:session:close', nativeAudioRealtimePort: 'soundscaper:native-realtime-port',
	nativePluginAvailability: 'soundscaper:v1:helper:native-plugin-availability',
	nativePluginConsent: 'soundscaper:v1:helper:native-plugin-consent',
	nativePluginScan: 'soundscaper:v1:helper:native-plugin-scan',
	nativePluginInventory: 'soundscaper:v1:helper:native-plugin-inventory',
	nativePluginClearQuarantine: 'soundscaper:v1:helper:native-plugin-clear-quarantine',
	nativePluginReviewInstallation: 'soundscaper:v1:native-plugin:installation:review', nativePluginInstantiate: 'soundscaper:v1:native-plugin:instantiate', nativePluginRunOffline: 'soundscaper:v1:native-plugin:run-offline', nativePluginSetBypassed: 'soundscaper:v1:native-plugin:set-bypassed', nativePluginPersistState: 'soundscaper:v1:native-plugin:state:persist', nativePluginRestoreState: 'soundscaper:v1:native-plugin:state:restore', nativePluginOpenVendorUi: 'soundscaper:v1:native-plugin:vendor-ui:open', nativePluginCloseVendorUi: 'soundscaper:v1:native-plugin:vendor-ui:close', nativePluginCloseInstance: 'soundscaper:v1:native-plugin:instance:close', nativePluginRpcPort: 'soundscaper:native-plugin-rpc-port',
	nativeTierControls: 'soundscaper:v1:native-tier:controls',
	nativeTierApply: 'soundscaper:v1:native-tier:apply',
	framescaperNativeCapabilities: 'framescaper:v1:native-services:capabilities', framescaperNativeSnapshot: 'framescaper:v1:native-services:snapshot', framescaperNativeControl: 'framescaper:v1:native-services:queue:control',
	framescaperNativeReorder: 'framescaper:v1:native-services:queue:reorder', framescaperNativeRemove: 'framescaper:v1:native-services:queue:remove', framescaperNativeEnqueue: 'framescaper:v1:native-services:queue:enqueue',
	framescaperNativeSelectRoot: 'framescaper:v1:native-services:root:select', framescaperNativeReauthorizeQueueRoot: 'framescaper:v1:native-services:queue:reauthorize-root', framescaperNativeRevalidateRoot: 'framescaper:v1:native-services:root:revalidate', framescaperNativeRevokeRoot: 'framescaper:v1:native-services:root:revoke', framescaperNativeCreateWatch: 'framescaper:v1:native-services:watch:create', framescaperNativeSetWatchEnabled: 'framescaper:v1:native-services:watch:enabled', framescaperNativeRemoveWatch: 'framescaper:v1:native-services:watch:remove', framescaperNativeReconcileWatch: 'framescaper:v1:native-services:watch:reconcile', framescaperNativeClaimWatchImport: 'framescaper:v1:native-services:watch:claim', framescaperNativeCompleteWatchImport: 'framescaper:v1:native-services:watch:complete',
	framescaperNativeCleanupScratch: 'framescaper:v1:native-services:scratch:cleanup', framescaperNativeSettleScratch: 'framescaper:v1:native-services:scratch:settle', framescaperNativePublish: 'framescaper:v1:native-services:publication:publish', framescaperNativeCheckpoint: 'framescaper:v1:native-services:publication:checkpoint', framescaperNativeExternalDisplays: 'framescaper:v1:native-services:display:list', framescaperNativeSetExternalDisplay: 'framescaper:v1:native-services:display:set', framescaperNativeFramePort: 'framescaper:v1:native-services:display:frame-port',
	framescaperNativePreferences: 'framescaper:v1:native-services:preferences', framescaperNativeSetPreference: 'framescaper:v1:native-services:preferences:set', framescaperNativeRenderInputBegin: 'framescaper:v1:native-services:render-inputs:begin', framescaperNativeRenderInputPort: 'framescaper:v1:native-services:render-inputs:port', framescaperNativeRenderInputFinalize: 'framescaper:v1:native-services:render-inputs:finalize', framescaperNativeRenderInputAbandon: 'framescaper:v1:native-services:render-inputs:abandon', framescaperNativeRenderInputBeginLive: 'framescaper:v1:native-services:render-inputs:live:begin', framescaperNativeRenderInputWriteLive: 'framescaper:v1:native-services:render-inputs:live:write', framescaperNativeRenderInputCompleteLive: 'framescaper:v1:native-services:render-inputs:live:complete',
	framescaperNativeSelectImageSequence: 'framescaper:v1:native-services:image-sequence:select', framescaperNativeReadImageSequenceFile: 'framescaper:v1:native-services:image-sequence:read', framescaperNativeReleaseImageSequence: 'framescaper:v1:native-services:image-sequence:release', framescaperNativeImageSequenceImport: 'framescaper:v1:native-services:image-sequence:import', framescaperNativeImageSequenceImportPort: 'framescaper:v1:native-services:image-sequence:import-port', framescaperNativeImageSequenceDecode: 'framescaper:v1:native-services:image-sequence:decode', framescaperNativeClaimProxyOutput: 'framescaper:v1:native-services:proxy-output:claim', framescaperNativeReadProxyOutput: 'framescaper:v1:native-services:proxy-output:read', framescaperNativeReleaseProxyOutput: 'framescaper:v1:native-services:proxy-output:release', framescaperNativeOpenFxScan: 'framescaper:v1:native-services:openfx:scan', framescaperNativeOpenFxInventory: 'framescaper:v1:native-services:openfx:inventory', framescaperNativeOpenFxControl: 'framescaper:v1:native-services:openfx:control', framescaperNativeOpenFxFrame: 'framescaper:v1:native-services:openfx:frame-port', framescaperNativeOpenFxFrameOffer: 'framescaper:v1:native-services:openfx:frame-offer',
	framescaperProjectHandshake: 'framescaper:v1:project-library:handshake', framescaperProjectBundle: 'framescaper:v1:project-library:bundle', framescaperProjectBodyRead: 'framescaper:v1:project-library:bodies:read', framescaperProjectList: 'framescaper:v1:project-library:list',
	framescaperNativeOpenFxInteract: 'framescaper:v1:native-services:openfx:interact',
	framescaperProjectDelete: 'framescaper:v1:project-library:delete', framescaperProjectDuplicate: 'framescaper:v1:project-library:duplicate', framescaperProjectBegin: 'framescaper:v1:project-library:publication:begin',
	framescaperProjectChunk: 'framescaper:v1:project-library:publication:chunk', framescaperProjectFinish: 'framescaper:v1:project-library:publication:finish', framescaperProjectAbort: 'framescaper:v1:project-library:publication:abort',
	listAssistanceModels: 'soundscaper:v1:assistance:list', installAssistanceModel: 'soundscaper:v1:assistance:install', cancelAssistanceModelInstall: 'soundscaper:v1:assistance:install:cancel', installPreseededAssistanceModel: 'soundscaper:v1:assistance:install-preseeded', reconcileAssistanceModels: 'soundscaper:v1:assistance:reconcile',
	collectAssistanceModelGarbage: 'soundscaper:v1:assistance:garbage-collect', listAssistanceModelNotices: 'soundscaper:v1:assistance:notices', relocateAssistanceModels: 'soundscaper:v1:assistance:relocate', removeAssistanceModel: 'soundscaper:v1:assistance:remove', assistanceInstallProgress: 'soundscaper:v1:event:assistance-progress', assistanceOperationModels: 'soundscaper:v1:assistance:operation:models', assistanceOperationCreate: 'soundscaper:v1:assistance:operation:create', assistanceOperationStage: 'soundscaper:v1:assistance:operation:stage', assistanceOperationInputPort: 'soundscaper:v1:assistance:operation:input-port', assistanceOperationReserve: 'soundscaper:v1:assistance:operation:reserve', assistanceOperationRun: 'soundscaper:v1:assistance:operation:run', assistanceOperationCancel: 'soundscaper:v1:assistance:operation:cancel', assistanceOperationReadOutput: 'soundscaper:v1:assistance:operation:read-output', assistanceOperationOutputPort: 'soundscaper:v1:assistance:operation:output-port', assistanceOperationRelease: 'soundscaper:v1:assistance:operation:release', assistanceOperationProgress: 'soundscaper:v1:event:assistance-operation-progress', assistanceWorkflowCreate: 'soundscaper:v1:assistance:workflow:create', assistanceWorkflowStage: 'soundscaper:v1:assistance:workflow:stage', assistanceWorkflowInputPort: 'soundscaper:v1:assistance:workflow:input-port', assistanceWorkflowReserve: 'soundscaper:v1:assistance:workflow:reserve', assistanceWorkflowBindProducer: 'soundscaper:v1:assistance:workflow:bind-producer', assistanceWorkflowRun: 'soundscaper:v1:assistance:workflow:run', assistanceWorkflowCancel: 'soundscaper:v1:assistance:workflow:cancel', assistanceWorkflowReadOutput: 'soundscaper:v1:assistance:workflow:read-output', assistanceWorkflowOutputPort: 'soundscaper:v1:assistance:workflow:output-port', assistanceWorkflowRelease: 'soundscaper:v1:assistance:workflow:release', assistanceWorkflowProgress: 'soundscaper:v1:event:assistance-workflow-progress', assistanceSemanticSearchOpen: 'soundscaper:v1:assistance:semantic-search:open', assistanceSemanticSearchAuthorize: 'soundscaper:v1:assistance:semantic-search:authorize', assistanceSemanticSearchRevoke: 'soundscaper:v1:assistance:semantic-search:revoke', assistanceSemanticSearchQuery: 'soundscaper:v1:assistance:semantic-search:query', assistanceSemanticSearchCancelQuery: 'soundscaper:v1:assistance:semantic-search:cancel-query',
	setLocale: 'soundscaper:v1:locale:set', windowAction: 'soundscaper:v1:window:action', externalFfmpegStatus: 'soundscaper:v1:ffmpeg:status', externalFfmpegChoose: 'soundscaper:v1:ffmpeg:choose', externalFfmpegClear: 'soundscaper:v1:ffmpeg:clear', externalFfmpegRescan: 'soundscaper:v1:ffmpeg:rescan', externalFfmpegInstall: 'soundscaper:v1:ffmpeg:install', desktopAudioCodecExecute: 'soundscaper:v1:codecs:audio:execute', desktopAudioCodecCancel: 'soundscaper:v1:codecs:audio:cancel', desktopAudioCodecCapabilities: 'soundscaper:v1:codecs:audio:capabilities', desktopVideoCodecCapabilities: 'soundscaper:v1:codecs:video:capabilities', desktopVideoCodecBegin: 'soundscaper:v1:codecs:video:begin', desktopVideoCodecWrite: 'soundscaper:v1:codecs:video:write', desktopVideoCodecClose: 'soundscaper:v1:codecs:video:close', desktopVideoCodecExecute: 'soundscaper:v1:codecs:video:execute', desktopVideoCodecStat: 'soundscaper:v1:codecs:video:stat', desktopVideoCodecRead: 'soundscaper:v1:codecs:video:read', desktopVideoCodecDelete: 'soundscaper:v1:codecs:video:delete', desktopVideoCodecCancel: 'soundscaper:v1:codecs:video:cancel',
	checkForUpdates: 'soundscaper:v1:updates:check', openExternal: 'soundscaper:v1:external:open',
	editText: 'soundscaper:v1:text:edit', rendererReady: 'soundscaper:v1:renderer:ready',
	respondToClose: 'soundscaper:v1:close:respond', openProject: 'soundscaper:v1:event:project-open',
	menuCommand: 'soundscaper:v1:event:menu-command',
	closeRequested: 'soundscaper:v1:event:close-requested',
	windowStateChanged: 'soundscaper:v1:event:window-state-changed',
	deliverySelect: 'soundscaper:v1:delivery:root:select', deliveryReauthorize: 'soundscaper:v1:delivery:root:reauthorize', deliveryProjectIdentity: 'soundscaper:v1:delivery:project:identity', deliveryEnqueueBatch: 'soundscaper:v1:delivery:queue:enqueue-batch', deliveryList: 'soundscaper:v1:delivery:queue:list', deliveryEvents: 'soundscaper:v1:delivery:queue:events', deliveryPause: 'soundscaper:v1:delivery:queue:pause', deliveryResume: 'soundscaper:v1:delivery:queue:resume', deliveryReorder: 'soundscaper:v1:delivery:queue:reorder', deliveryCancel: 'soundscaper:v1:delivery:queue:cancel', deliveryRetry: 'soundscaper:v1:delivery:queue:retry', deliveryWorkerPort: 'soundscaper:v1:delivery:worker:port',
}); const MAX_CHUNK_BYTES = 4 * 1024 * 1024; const FINAL_PREFIX_BYTES = 32;
const READ_PROFILE_LINKED_AUDIO_RANGE_V1 = 'linked-audio-range-v1'; const READ_PROFILE_LINKED_VIDEO_RANGE_V1 = 'linked-video-range-v1';
const READ_PROFILE_MATERIALIZED_V1 = 'materialized-v1'; const READ_PROFILE_SCAPE_RANGE_V1 = 'scape-range-v1';
const SCAPE_PROJECT_MIME_TYPE = 'application/vnd.soundscaper.scape+zip';
const MAX_MATERIALIZED_READ_DESCRIPTOR_BYTES = 512 * 1024 ** 2; const MAX_SCAPE_RANGE_READ_DESCRIPTOR_BYTES = 65 * 1024 ** 3; const MAX_DESKTOP_SAVE_BYTES = 65 * 1024 ** 3;
const SHA256 = /^[a-f0-9]{64}$/u;
const api = Object.freeze({
	getEnvironment: () => ipcRenderer.invoke(CHANNELS.environment),
	...(SOAK_DEBUG_ENABLED ? { readSoakProcessMetrics: () => ipcRenderer.invoke(CHANNELS.soakDebugProcessMetrics).then(soakDebugProcessMetrics) } : {}),
	chooseFiles: (options) => ipcRenderer.invoke(CHANNELS.chooseFiles, {
		purpose: text(options?.purpose, 24),
		multiple: options?.multiple === true,
	}).then(sanitizeReadDescriptors),
	releaseRead: (id) => ipcRenderer.invoke(CHANNELS.releaseRead, opaqueId(id, 64)),
	chooseLinkedVideoOriginal: () => ipcRenderer.invoke(CHANNELS.chooseLinkedVideoOriginal).then((value) => nullableLinkedLocator(value, 'video')),
	loadLinkedVideoOriginal: (value) => {
		const request = linkedVideoLoadRequest(value);
		return ipcRenderer.invoke(CHANNELS.loadLinkedVideoOriginal, request)
			.then((result) => nullableLoadedLinkedLocator(result, 'video', request.playback));
	},
	reconcileLinkedVideoOriginals: (value) => ipcRenderer.invoke(CHANNELS.reconcileLinkedVideoOriginals, linkedVideoReferences(value)).then(safeInteger),
	releaseLinkedVideoOriginal: (reference) => ipcRenderer.invoke(CHANNELS.releaseLinkedVideoOriginal, linkedVideoReferences([reference])[0]).then(strictBoolean),
	chooseLinkedAudioOriginal: () => ipcRenderer.invoke(CHANNELS.chooseLinkedAudioOriginal).then((value) => nullableLinkedLocator(value, 'audio')),
	loadLinkedAudioOriginal: (value) => {
		const request = linkedAudioLoadRequest(value);
		return ipcRenderer.invoke(CHANNELS.loadLinkedAudioOriginal, request)
			.then((result) => nullableLoadedLinkedLocator(result, 'audio', request.range));
	},
	reconcileLinkedOriginals: (value) => ipcRenderer.invoke(CHANNELS.reconcileLinkedOriginals, linkedOriginalReferences(value)).then(safeInteger),
	releaseLinkedOriginal: (reference) => ipcRenderer.invoke(CHANNELS.releaseLinkedOriginal, linkedOriginalReferences([reference])[0]).then(strictBoolean),
	chooseSaveTarget: (options) => ipcRenderer.invoke(CHANNELS.chooseSaveTarget, {
		purpose: text(options?.purpose, 24),
		suggestedName: text(options?.suggestedName, 220),
	}),
	beginWrite: (options) => ipcRenderer.invoke(CHANNELS.beginWrite, saveDeclaration(options)),
	writeChunk: (options) => {
		const bytes = binary(options?.bytes);
		if (bytes.byteLength > MAX_CHUNK_BYTES) throw new RangeError('Save chunk is too large');
		return ipcRenderer.invoke(CHANNELS.writeChunk, {
			writeId: opaqueId(options?.writeId, 32),
			offset: safeInteger(options?.offset),
			bytes,
		});
	},
	patchFinalPrefix: (options) => {
		const bytes = binary(options?.bytes);
		if (bytes.byteLength !== FINAL_PREFIX_BYTES) throw new RangeError('Final prefix must be exactly 32 bytes');
		return ipcRenderer.invoke(CHANNELS.patchFinalPrefix, {
			writeId: opaqueId(options?.writeId, 32), bytes,
		}).then(finalPrefixAcknowledgement);
	},
	finishWrite: (writeId) => ipcRenderer.invoke(CHANNELS.finishWrite, opaqueId(writeId, 32)),
	abortWrite: (writeId) => ipcRenderer.invoke(CHANNELS.abortWrite, opaqueId(writeId, 32)),
	setLocale: (locale) => ipcRenderer.invoke(CHANNELS.setLocale, text(locale, 32)), getExternalFfmpegStatus: () => ipcRenderer.invoke(CHANNELS.externalFfmpegStatus).then(externalFfmpegStatus), chooseExternalFfmpeg: () => ipcRenderer.invoke(CHANNELS.externalFfmpegChoose).then(externalFfmpegStatus), clearExternalFfmpeg: () => ipcRenderer.invoke(CHANNELS.externalFfmpegClear).then(externalFfmpegStatus), rescanExternalFfmpeg: () => ipcRenderer.invoke(CHANNELS.externalFfmpegRescan).then(externalFfmpegStatus), installExternalFfmpeg: () => ipcRenderer.invoke(CHANNELS.externalFfmpegInstall).then(externalFfmpegStatus), getDesktopAudioCodecCapabilities: (value) => Promise.resolve().then(() => desktopAudioCodecCapabilityQuery(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopAudioCodecCapabilities, request).then((result) => desktopAudioCodecCapabilityResult(result, request))), runDesktopAudioCodecOperation: (value) => Promise.resolve().then(() => desktopAudioCodecRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopAudioCodecExecute, request).then((result) => desktopAudioCodecResult(result, request))), cancelDesktopAudioCodecOperation: (requestId) => Promise.resolve().then(() => desktopAudioCodecRequestId(requestId)).then((id) => ipcRenderer.invoke(CHANNELS.desktopAudioCodecCancel, id)).then((value) => strictBoolean(value, 'Desktop audio codec cancel result must be a boolean')), getDesktopVideoExportCapabilities: () => ipcRenderer.invoke(CHANNELS.desktopVideoCodecCapabilities).then(desktopVideoCapabilities), beginDesktopVideoCodecOperation: (value) => Promise.resolve().then(() => desktopVideoPlan(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecBegin, request)).then(desktopVideoBeginResult), writeDesktopVideoCodecInput: (value) => Promise.resolve().then(() => desktopVideoInputWrite(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecWrite, request).then((result) => desktopVideoOffsetResult(result, request.offset, request.bytes.byteLength))), closeDesktopVideoCodecInput: (value) => Promise.resolve().then(() => desktopVideoInputClose(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecClose, request).then((result) => desktopVideoOffsetResult(result, request.offset, 0))), executeDesktopVideoCodecOperation: (value) => Promise.resolve().then(() => desktopVideoIdRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecExecute, request)).then(desktopVideoExecuteResult), statDesktopVideoCodecOutput: (value) => Promise.resolve().then(() => desktopVideoIdRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecStat, request)).then(desktopVideoStatResult), readDesktopVideoCodecOutput: (value) => Promise.resolve().then(() => desktopVideoOutputRead(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecRead, request).then((result) => desktopVideoReadResult(result, request.maximumBytes))), deleteDesktopVideoCodecOperation: (value) => Promise.resolve().then(() => desktopVideoIdRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecDelete, request)).then((value) => strictBoolean(value, 'Desktop video delete result must be a boolean')), cancelDesktopVideoCodecOperation: (value) => Promise.resolve().then(() => desktopVideoOperationId(value)).then((id) => ipcRenderer.invoke(CHANNELS.desktopVideoCodecCancel, id)).then((value) => strictBoolean(value, 'Desktop video cancel result must be a boolean')),
	runWindowAction: (action) => ipcRenderer.invoke(CHANNELS.windowAction, windowAction(action)),
	checkForUpdates: () => ipcRenderer.invoke(CHANNELS.checkForUpdates),
	openExternal: (destination) => ipcRenderer.invoke(CHANNELS.openExternal, text(destination, 32)),
	editText: (command) => ipcRenderer.invoke(CHANNELS.editText, textEditCommand(command)),
	signalReady: () => ipcRenderer.send(CHANNELS.rendererReady),
	respondToClose: (response) => ipcRenderer.send(CHANNELS.respondToClose, {
		requestId: text(response?.requestId, 64),
		allow: response?.allow === true,
	}),
	probeHelperAvailability: () => ipcRenderer.invoke(CHANNELS.helperProbeAvailability).then((value) => Object.freeze({
		enabled: value?.enabled === true,
		quarantined: value?.quarantined === true,
	})),
	beginVideoSourceProbe: (request) => ipcRenderer.invoke(CHANNELS.helperProbeBegin, {
		capabilityId: opaqueId(request?.capabilityId, 64),
	}).then((value) => Object.freeze({ probeId: opaqueId(value?.probeId, 40) })),
	awaitVideoSourceProbe: (request) => ipcRenderer.invoke(CHANNELS.helperProbeAwait, {
		probeId: opaqueId(request?.probeId, 40),
	}).then(helperProbeCompletion),
	cancelVideoSourceProbe: (request) => ipcRenderer.invoke(CHANNELS.helperProbeCancel, {
		probeId: opaqueId(request?.probeId, 40),
	}).then((value) => Object.freeze({ cancelled: value?.cancelled === true })),
	nativeAudioHelperAvailability: () => ipcRenderer.invoke(CHANNELS.nativeAudioAvailability).then(nativeAudioAvailability),
	setNativeAudioHelperEnabled: (enabled) => ipcRenderer.invoke(CHANNELS.nativeAudioSetEnabled, enabled === true).then(nativeAudioEnabled),
	describeNativeAudioBackend: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioInventory, { backend: text(request?.backend, 32) }).then(nativeAudioInventory),
	nativePluginAvailability: () => ipcRenderer.invoke(CHANNELS.nativePluginAvailability).then(nativePluginStatus),
	setNativePluginConsent: (request) => ipcRenderer.invoke(CHANNELS.nativePluginConsent, {
		format: text(request?.format, 32),
		action: text(request?.action ?? 'grant', 32),
		rootId: text(request?.rootId ?? '', 256),
	}).then(nativePluginStatus),
	scanNativePlugins: (request) => ipcRenderer.invoke(CHANNELS.nativePluginScan, {
		format: text(request?.format, 32),
		rootId: text(request?.rootId, 256),
	}).then(nativePluginStatus),
	listNativePlugins: () => ipcRenderer.invoke(CHANNELS.nativePluginInventory).then(nativePluginStatus),
	clearNativePluginQuarantine: (request) => ipcRenderer.invoke(CHANNELS.nativePluginClearQuarantine, { digest: opaqueId(request?.digest, 64), clearance: text(request?.clearance, 16) }).then(nativePluginStatus),
	openNativeAudioSession: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionOpen, nativeAudioSessionOpenRequest(request)).then(nativePluginStatus), bindNativeAudioSession: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionBind, nativeAudioSessionIdRequest(request, true)).then(nativePluginStatus), nativeAudioSessionStatus: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionStatus, nativeAudioSessionIdRequest(request, false)).then(nativePluginStatus), calibrateNativeAudioSession: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionCalibrate, nativeAudioSessionCalibrationRequest(request)).then(nativePluginStatus), reportNativeAudioSessionTransfer: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionReport, nativeAudioSessionTransferRequest(request)).then(nativePluginStatus), reportNativeAudioSessionLoss: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionLoss, nativeAudioSessionLossRequest(request)).then(nativePluginStatus), closeNativeAudioSession: (request) => ipcRenderer.invoke(CHANNELS.nativeAudioSessionClose, nativeAudioSessionIdRequest(request, false)).then(strictBoolean),
		reviewNativePluginInstallation: (request) => ipcRenderer.invoke(CHANNELS.nativePluginReviewInstallation, nativePluginReviewRequest(request)).then(nativePluginStatus), instantiateNativePlugin: (request) => ipcRenderer.invoke(CHANNELS.nativePluginInstantiate, nativePluginInstantiationRequest(request)).then(nativePluginStatus), runNativePluginOffline: (request) => ipcRenderer.invoke(CHANNELS.nativePluginRunOffline, nativePluginInstanceRequest(request)).then(nativePluginStatus), setNativePluginBypassed: (request) => ipcRenderer.invoke(CHANNELS.nativePluginSetBypassed, nativePluginBypassRequest(request)).then(nativePluginStatus), persistNativePluginState: (request) => ipcRenderer.invoke(CHANNELS.nativePluginPersistState, nativePluginStatePersistRequest(request)).then(nativePluginStatus), restoreNativePluginState: (request) => ipcRenderer.invoke(CHANNELS.nativePluginRestoreState, nativePluginStateRestoreRequest(request)).then(nativePluginStateRestoreProjection), openNativePluginVendorUi: (request) => ipcRenderer.invoke(CHANNELS.nativePluginOpenVendorUi, nativePluginInstanceRequest(request)).then(nativePluginStatus), closeNativePluginVendorUi: (request) => ipcRenderer.invoke(CHANNELS.nativePluginCloseVendorUi, nativePluginVendorCloseRequest(request)).then(strictBoolean), closeNativePluginInstance: (request) => ipcRenderer.invoke(CHANNELS.nativePluginCloseInstance, nativePluginInstanceRequest(request)).then(strictBoolean),
	readNativeTierControls: () => ipcRenderer.invoke(CHANNELS.nativeTierControls).then(nativeTierControls),
	applyNativeTierControl: (request) => ipcRenderer.invoke(CHANNELS.nativeTierApply, nativeTierControlRequest(request)).then(nativeTierControls),
	persistentDelivery: Object.freeze({
		selectDestination: () => persistentDeliveryInvoke(CHANNELS.deliverySelect), reauthorizeDestination: (value) => persistentDeliveryInvoke(CHANNELS.deliveryReauthorize, persistentDeliveryRecord(value, ['grantId'], 'reauthorization')), currentProjectIdentity: (value) => persistentDeliveryInvoke(CHANNELS.deliveryProjectIdentity, persistentDeliveryRecord(value, ['projectId'], 'project identity')), enqueueBatch: (value) => persistentDeliveryInvoke(CHANNELS.deliveryEnqueueBatch, persistentDeliveryRecord(value, ['items', 'admission'], 'batch enqueue')),
		list: (value = {}) => persistentDeliveryInvoke(CHANNELS.deliveryList, persistentDeliveryOptionalRecord(value, ['currentProjectIdentity', 'limit', 'cursor'], 'list')), events: (value = {}) => persistentDeliveryInvoke(CHANNELS.deliveryEvents, persistentDeliveryOptionalRecord(value, ['afterSequence', 'limit'], 'events')), pause: () => persistentDeliveryInvoke(CHANNELS.deliveryPause), resume: () => persistentDeliveryInvoke(CHANNELS.deliveryResume), reorder: (value) => persistentDeliveryInvoke(CHANNELS.deliveryReorder, persistentDeliveryRecord(value, ['jobId', 'position'], 'reorder')), cancel: (value) => persistentDeliveryInvoke(CHANNELS.deliveryCancel, persistentDeliveryRecord(value, ['jobId'], 'cancel')), retry: (value) => persistentDeliveryInvoke(CHANNELS.deliveryRetry, persistentDeliveryRecord(value, ['jobId'], 'retry')),
	}),
	nativeServices: Object.freeze({
		capabilities: () => ipcRenderer.invoke(CHANNELS.framescaperNativeCapabilities).then(nativeCapabilitySnapshot), snapshot: () => ipcRenderer.invoke(CHANNELS.framescaperNativeSnapshot).then(nativeServicesSnapshot), control: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeControl, nativeQueueControlRequest(request)).then(nativeQueueProjection),
		reorder: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeReorder, nativeQueueReorderRequest(request)).then(nativeQueueProjections), remove: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeRemove, nativeQueueRemoveRequest(request)).then((value) => strictBoolean(value, 'Framescaper native queue removal result must be a boolean')), enqueue: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeEnqueue, nativeQueueEnqueueRequest(request)).then(nativeQueueProjection), stageRenderInputs: stageNativeRenderInputs, stageLiveRenderInputs: stageNativeLiveRenderInputs, writeLiveRenderInput: writeNativeLiveRenderInput, completeLiveRenderInput: completeNativeLiveRenderInput, abandonRenderInputs: abandonNativeRenderInputs,
		selectRoot: () => ipcRenderer.invoke(CHANNELS.framescaperNativeSelectRoot).then((value) => value === null ? null : nativeRootProjection(value)), reauthorizeQueueRoot: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeReauthorizeQueueRoot, nativeLifecycleIdRequest(request, 'jobId')).then((value) => value === null ? null : nativeQueueProjection(value)), revalidateRoot: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeRevalidateRoot, nativeLifecycleIdRequest(request, 'grantId')).then(strictBoolean), revokeRoot: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeRevokeRoot, nativeLifecycleIdRequest(request, 'grantId')).then(strictBoolean),
		createWatch: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeCreateWatch, nativeWatchCreateRequest(request)).then(nativeWatchProjection), setWatchEnabled: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeSetWatchEnabled, nativeWatchEnabledRequest(request)).then(nativeWatchProjection),
		removeWatch: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeRemoveWatch, nativeLifecycleIdRequest(request, 'ruleId')).then(strictBoolean), reconcileWatch: () => ipcRenderer.invoke(CHANNELS.framescaperNativeReconcileWatch).then(nativeServicesSnapshot), claimWatchImport: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeClaimWatchImport, nativeWatchImportClaimRequest(request)).then((value) => value === null ? null : nativeWatchImportClaim(value)), completeWatchImport: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeCompleteWatchImport, nativeWatchImportCompletionRequest(request)).then(strictBoolean),
		cleanupScratch: () => ipcRenderer.invoke(CHANNELS.framescaperNativeCleanupScratch).then(nativeJobIds), settleScratch: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeSettleScratch, nativeLifecycleIdRequest(request, 'jobId')).then(nativeScratchSettlement), publish: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativePublish, nativePublicationRequest(request)).then(nativePublicationResult), checkpoint: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeCheckpoint, nativeCheckpointRequest(request)).then(nativeCheckpointResult),
		externalDisplays: () => ipcRenderer.invoke(CHANNELS.framescaperNativeExternalDisplays).then(nativeExternalDisplays), setExternalDisplay: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeSetExternalDisplay, nativeExternalDisplayRequest(request)).then(nativeExternalDisplays), presentExternalDisplay: presentNativeExternalDisplayFrame, preferences: () => ipcRenderer.invoke(CHANNELS.framescaperNativePreferences).then(nativeServicePreferences), setPreference: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeSetPreference, nativePreferenceRequest(request)).then((value) => strictBoolean(value, 'Framescaper native-service preference update must be boolean')),
		selectImageSequence: () => ipcRenderer.invoke(CHANNELS.framescaperNativeSelectImageSequence, {}).then((value) => value === null ? null : nativeImageSequenceSelection(value)), readImageSequenceFile: (request) => { const value = nativeImageSequenceReadRequest(request); return ipcRenderer.invoke(CHANNELS.framescaperNativeReadImageSequenceFile, value).then((bytes) => { const result = binary(bytes); if (result.byteLength !== value.length) throw new Error('Framescaper image-sequence range read was short'); return result; }); }, releaseImageSequence: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeReleaseImageSequence, nativeImageSequenceReleaseRequest(request)).then((value) => strictBoolean(value, 'Framescaper image-sequence release must be boolean')), imageSequenceImport: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceImport, nativeImageSequenceImportRequest(request)), writeImageSequenceImportChunk: transferNativeImageSequenceImportChunk, readImageSequenceImportBody: (request) => { const value = nativeImageSequenceImportReadRequest(request); return ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceImport, { operation: 'read', ...value }).then((bytes) => { const result = binary(bytes); if (result.byteLength !== value.length) throw new Error('Framescaper image-sequence body range was short'); return result; }); }, decodeImageSequenceSource: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceDecode, { operation: 'decode', ...nativeImageSequenceDecodeRequest(request) }).then(nativeImageSequenceDecodeClaim), cancelImageSequenceDecode: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceDecode, { operation: 'cancel', ...nativeImageSequenceDecodeIdRequest(request, 'requestId') }).then((value) => strictBoolean(value, 'Framescaper image-sequence cancel result must be boolean')), readImageSequenceDecode: (request) => { const value = nativeImageSequenceDecodeReadRequest(request); return ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceDecode, { operation: 'read', ...value }).then((bytes) => { const result = binary(bytes); if (result.byteLength !== value.length) throw new Error('Framescaper decoded image-sequence range was short'); return result; }); }, releaseImageSequenceDecode: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceDecode, { operation: 'release', ...nativeImageSequenceDecodeIdRequest(request, 'claimId') }).then((value) => strictBoolean(value, 'Framescaper image-sequence claim release must be boolean')), claimProxyOutput: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeClaimProxyOutput, nativeProxyOutputJobRequest(request)).then(nativeProxyOutputClaim), readProxyOutput: (request) => { const value = nativeProxyOutputReadRequest(request); return ipcRenderer.invoke(CHANNELS.framescaperNativeReadProxyOutput, value).then((bytes) => { const result = binary(bytes); if (result.byteLength !== value.length) throw new Error('Framescaper proxy-output range read was short'); return result; }); }, releaseProxyOutput: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeReleaseProxyOutput, nativeProxyOutputClaimRequest(request)).then((value) => strictBoolean(value, 'Framescaper proxy-output release must be boolean')), scanOpenFxPlugin: () => ipcRenderer.invoke(CHANNELS.framescaperNativeOpenFxScan).then((value) => value === null ? null : nativeOpenFxProjection(value)), listOpenFxPlugins: () => ipcRenderer.invoke(CHANNELS.framescaperNativeOpenFxInventory).then(nativeOpenFxInventory), controlOpenFxPlugin: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeOpenFxControl, nativeOpenFxControlRequest(request)).then(nativeOpenFxProjection), openOpenFxFrameSession: (request) => ipcRenderer.invoke(CHANNELS.framescaperNativeOpenFxFrame, nativeOpenFxFrameSessionRequest(request)).then(nativeOpenFxFrameOfferV2),
		runOpenFxInteract: (request) => { const value = nativeOpenFxAuthoredInteractRequest(request); return ipcRenderer.invoke(CHANNELS.framescaperNativeOpenFxInteract, value).then((result) => nativeOpenFxAuthoredInteractResult(result, value)); },
	}),
	listAssistanceModels: () => ipcRenderer.invoke(CHANNELS.listAssistanceModels).then(assistanceStatus), installAssistanceModel: (modelId) => ipcRenderer.invoke(CHANNELS.installAssistanceModel, assistanceModelId(modelId)).then(assistanceModel), cancelAssistanceModelInstall: (modelId) => ipcRenderer.invoke(CHANNELS.cancelAssistanceModelInstall, assistanceModelId(modelId)).then(assistanceCancellation),
	installPreseededAssistanceModel: (modelId) => ipcRenderer.invoke(CHANNELS.installPreseededAssistanceModel, assistanceModelId(modelId)).then((value) => value === null ? null : assistanceModel(value)), reconcileAssistanceModels: () => ipcRenderer.invoke(CHANNELS.reconcileAssistanceModels).then(assistanceReconciliation), collectAssistanceModelGarbage: () => ipcRenderer.invoke(CHANNELS.collectAssistanceModelGarbage).then(assistanceGarbageCollection),
	listAssistanceModelNotices: () => ipcRenderer.invoke(CHANNELS.listAssistanceModelNotices).then(assistanceNotices), relocateAssistanceModels: () => ipcRenderer.invoke(CHANNELS.relocateAssistanceModels).then((value) => value === null ? null : assistanceRelocation), removeAssistanceModel: (modelId) => ipcRenderer.invoke(CHANNELS.removeAssistanceModel, assistanceModelId(modelId)).then(safeInteger),
	onAssistanceInstallProgress: (listener) => subscribe(CHANNELS.assistanceInstallProgress, listener, assistanceProgress), localAssistance: Object.freeze({ models: () => ipcRenderer.invoke(CHANNELS.assistanceOperationModels).then(assistanceOperationModels), createJob: () => ipcRenderer.invoke(CHANNELS.assistanceOperationCreate).then(assistanceOperationJob), stageInput: stageAssistanceOperationInput, reserveOutput: (value) => Promise.resolve().then(() => assistanceOutputReservationRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceOperationReserve, request)).then(assistanceOutputReservation), run: (value) => Promise.resolve().then(() => assistanceOperationRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceOperationRun, request).then((outcome) => assistanceOperationOutcome(outcome, request))), cancel: (jobId) => Promise.resolve().then(() => assistanceOperationId(jobId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceOperationCancel, id)).then(assistanceOperationCancellation), readOutput: readAssistanceOperationOutput, release: (jobId) => Promise.resolve().then(() => assistanceOperationId(jobId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceOperationRelease, id)).then((value) => strictBoolean(value, 'Assistance operation release must be boolean')), onProgress: (listener) => subscribe(CHANNELS.assistanceOperationProgress, listener, assistanceOperationProgress), semanticSearch: Object.freeze({ open: (value) => Promise.resolve().then(() => assistanceSemanticSearchAuthority(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceSemanticSearchOpen, request)).then(assistanceSemanticSearchSession), authorize: (value) => Promise.resolve().then(() => assistanceSemanticSearchAuthorization(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceSemanticSearchAuthorize, request)).then(assistanceSemanticSearchSession), revoke: (sessionId) => Promise.resolve().then(() => assistanceOperationId(sessionId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceSemanticSearchRevoke, id)).then((value) => strictBoolean(value, 'Assistance semantic-search revocation must be boolean')), query: (value) => Promise.resolve().then(() => assistanceSemanticSearchQuery(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceSemanticSearchQuery, request)).then(assistanceSemanticSearchQueryResult), cancelQuery: (queryId) => Promise.resolve().then(() => assistanceOperationId(queryId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceSemanticSearchCancelQuery, id)).then((value) => strictBoolean(value, 'Assistance semantic-search query cancellation must be boolean')) }), workflow: Object.freeze({ createJob: () => ipcRenderer.invoke(CHANNELS.assistanceWorkflowCreate).then(assistanceWorkflowJob), run: (value) => Promise.resolve().then(() => assistanceWorkflowRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceWorkflowRun, request).then((outcome) => assistanceWorkflowOutcome(outcome, request))), cancel: (jobId) => Promise.resolve().then(() => assistanceOperationId(jobId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceWorkflowCancel, id)).then(assistanceWorkflowCancellation), readOutput: readAssistanceWorkflowOutput, onProgress: (listener) => subscribe(CHANNELS.assistanceWorkflowProgress, listener, assistanceWorkflowProgress), custody: Object.freeze({ stageInput: stageAssistanceWorkflowInput, reserveOutput: (value) => Promise.resolve().then(() => assistanceWorkflowReservationRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceWorkflowReserve, request).then((result) => assistanceWorkflowCustodyHandle(result, request, 'output'))), bindProducer: (value) => Promise.resolve().then(() => assistanceWorkflowProducerRequest(value)).then((request) => ipcRenderer.invoke(CHANNELS.assistanceWorkflowBindProducer, assistanceWorkflowProducerIpcRequest(request)).then((result) => assistanceWorkflowCustodyHandle(result, request, 'input'))), release: (jobId) => Promise.resolve().then(() => assistanceOperationId(jobId)).then((id) => ipcRenderer.invoke(CHANNELS.assistanceWorkflowRelease, id)).then((value) => strictBoolean(value, 'Assistance workflow release must be boolean')) }) }) }),
	onOpenProject: (listener) => subscribe(CHANNELS.openProject, listener, sanitizeReadDescriptor),
	onMenuCommand: (listener) => subscribe(CHANNELS.menuCommand, listener, (value) => Object.freeze({ command: text(value?.command, 64) })),
	onCloseRequested: (listener) => subscribe(CHANNELS.closeRequested, listener, (value) => Object.freeze({
		requestId: text(value?.requestId, 64),
		reason: value?.reason === 'quit' ? 'quit' : 'window-close',
	})),
	onWindowStateChanged: (listener) => subscribe(CHANNELS.windowStateChanged, listener, windowState),
});
const FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE = Object.freeze({ kind: 'framescaper-project-library-handshake', version: 1, owner: 'framescaper', schemaFamily: 'framescaper', schemaVersion: 1,
	scapeFormatVersions: Object.freeze([1]), attachedScapeFormatVersion: 1, storageDatabaseName: 'kw-media-framescaper-editor-v1', desktopLibrarySchemaVersion: 1,
	desktopDatabaseUserVersion: 1, desktopLibraryScope: Object.freeze(['kw.media', 'framescaper-project-library', 'v1']) });
const framescaperProjectPublications = new Set(); let framescaperProjectState = 'pending'; let framescaperProjectConnection = null;
const framescaperProjectLibrary = Object.freeze({
	connect: connectFramescaperProjectLibrary, handshakeState: () => framescaperProjectState,
	listProjects: () => invokeFramescaperProject(CHANNELS.framescaperProjectList),
	readProjectBundle: (projectId) => invokeFramescaperProject(CHANNELS.framescaperProjectBundle, framescaperProjectId(projectId)),
	readBodyChunk: (value) => invokeFramescaperProject(CHANNELS.framescaperProjectBodyRead, structuredClone(value)).then(framescaperProjectChunkResult),
	beginPublication: beginFramescaperProjectPublication, writePublicationChunk: writeFramescaperProjectPublicationChunk,
	finishPublication: (value) => finishFramescaperProjectPublication(value, CHANNELS.framescaperProjectFinish, false),
	abortPublication: (value) => finishFramescaperProjectPublication(value, CHANNELS.framescaperProjectAbort, true),
	deleteProject: (value) => invokeFramescaperProject(CHANNELS.framescaperProjectDelete, structuredClone(value)),
	duplicateProject: (value) => invokeFramescaperProject(CHANNELS.framescaperProjectDuplicate, structuredClone(value)),
});
if (PRELOAD_PRODUCT_ID === 'soundscaper') globalThis.window?.addEventListener?.('message', transferPersistentDeliveryWorkerPort);
const apiWithoutPersistentDelivery = Object.freeze(Object.fromEntries(Object.entries(api).filter(([name]) => name !== 'persistentDelivery'))); const productApi = PRELOAD_PRODUCT_ID === 'soundscaper' ? api : apiWithoutPersistentDelivery; const bridge = Object.freeze({ v1: productApi }); const framescaperBridge = Object.freeze({ v1: Object.freeze({ ...apiWithoutPersistentDelivery, projectLibrary: framescaperProjectLibrary }) }); for (const [channel, type] of [[CHANNELS.nativeAudioRealtimePort, 'soundscaper-native-realtime-port-v1'], [CHANNELS.nativePluginRpcPort, 'soundscaper-native-plugin-rpc-port-v1'], [CHANNELS.framescaperNativeOpenFxFrameOffer, 'framescaper-openfx-frame-port-v1']]) ipcRenderer.on(channel, (event, offer) => { const ports = Array.from(event.ports ?? []); if (ports.length !== 1) { for (const port of ports) port.close(); return; } window.postMessage(Object.freeze({ type, offer: type === 'framescaper-openfx-frame-port-v1' ? nativeOpenFxFrameOfferV2(offer) : nativePluginStatus(offer) }), '*', ports); });
for (const name of ['scapeDesktop', 'soundscaperDesktop']) contextBridge.exposeInMainWorld(name, bridge);
contextBridge.exposeInMainWorld('framescaperDesktop', framescaperBridge);
function nativeOpenFxFrameSessionRequest(value) { nativeProjectIdentity(value, 'OpenFX frame session', 'framescaper'); nativeRecord(value, ['protocolVersion', 'schemaFamily', 'schemaVersion', 'planPayload', 'planFingerprint', 'instanceId', 'outputOrdinal', 'requestedBackend', 'transitionProgress', 'inputs', 'inputBinding', 'requestNonce'], 'OpenFX frame session request'); if (value.protocolVersion !== 1 || typeof value.planPayload !== 'string' || !value.planPayload.length || utf8Bytes(value.planPayload, 16 * 1024 * 1024) > 16 * 1024 * 1024 || !Array.isArray(value.inputs) || value.inputs.length > 16) throw new RangeError('OpenFX frame session plan exceeds its bounded control domain'); return structuredClone(value); } function nativeOpenFxFrameOfferV2(value) {
	nativeRecord(value, ['protocolVersion', 'sessionId', 'requestNonce'], 'OpenFX frame offer');
	return Object.freeze({ ...nativeOpenFxFrameOffer({ protocolVersion: value.protocolVersion,
		sessionId: value.sessionId }), requestNonce: opaqueId(value.requestNonce, 40) });
}
const ASSISTANCE_AVAILABILITY = ['installed', 'installable', 'unsupported-platform', 'insufficient-memory'];
function assistanceModelId(value) { const modelId = text(value, 64); if (!/^[a-z\d][a-z\d.-]*[a-z\d]$/u.test(modelId)) throw new TypeError('Unsupported assistance model id'); return modelId; }
function optionalBytes(value) { return value === null || value === undefined ? null : safeInteger(value); } function assistanceModel(value) { const availability = String(value?.availability || ''); if (!ASSISTANCE_AVAILABILITY.includes(availability)) throw new TypeError('Unsupported assistance availability'); return Object.freeze({ modelId: assistanceModelId(value?.modelId), version: text(value?.version, 64), task: text(value?.task, 64), availability, downloadBytes: optionalBytes(value?.downloadBytes), installedBytes: optionalBytes(value?.installedBytes), attributionRequired: value?.attributionRequired === true }); }
function assistanceStatus(value) { if (!value || !Array.isArray(value.models)) throw new TypeError('Malformed assistance status'); return Object.freeze({ runtimeAvailable: value.runtimeAvailable === true, runtimeReason: value.runtimeReason === null ? null : text(value.runtimeReason, 512), models: Object.freeze(value.models.map(assistanceModel)) }); } function assistanceProgress(value) { return Object.freeze({ modelId: assistanceModelId(value?.modelId), fileName: text(value?.fileName, 160), completedBytes: safeInteger(value?.completedBytes), totalBytes: safeInteger(value?.totalBytes) }); } function assistanceCancellation(value) { if (value?.contractVersion !== 1 || !['cancelled', 'not-active'].includes(value?.outcome)) throw new TypeError('Malformed assistance cancellation'); return Object.freeze({ contractVersion: 1, modelId: assistanceModelId(value.modelId), outcome: value.outcome }); } function assistanceModelIds(value, label) { if (!Array.isArray(value) || value.length > 256) throw new TypeError(`Malformed assistance ${label}`); return Object.freeze(value.map(assistanceModelId)); } function assistanceReconciliation(value) { if (!value || !Array.isArray(value.rejected) || value.rejected.length > 256) throw new TypeError('Malformed assistance reconciliation'); return Object.freeze({ installedModelIds: assistanceModelIds(value.installedModelIds, 'installed ids'), incompleteModelIds: assistanceModelIds(value.incompleteModelIds, 'incomplete ids'), rejected: Object.freeze(value.rejected.map((entry) => Object.freeze({ modelId: assistanceModelId(entry?.modelId), reason: text(entry?.reason, 512) }))) }); } function assistanceGarbageCollection(value) { return Object.freeze({ reclaimedBlobBytes: safeInteger(value?.reclaimedBlobBytes), discardedManifestCount: safeInteger(value?.discardedManifestCount), discardedPartialCount: safeInteger(value?.discardedPartialCount), discardedPartialBytes: safeInteger(value?.discardedPartialBytes), reclaimedBytes: safeInteger(value?.reclaimedBytes) }); } function assistanceNotices(value) { if (!Array.isArray(value) || value.length > 256) throw new TypeError('Malformed assistance notices'); return Object.freeze(value.map((notice) => { if (notice?.schemaVersion !== 1 || notice?.distributionKind !== 'identity-mirrored' || typeof notice.attributionRequired !== 'boolean' || !Array.isArray(notice.provenanceSources) || notice.provenanceSources.length === 0 || notice.provenanceSources.length > 32) throw new TypeError('Malformed assistance notice'); const sources = notice.provenanceSources.map((source) => { const url = new URL(text(source, 2048)); if (url.protocol !== 'https:' || url.username || url.password) throw new TypeError('Malformed assistance notice source'); return url.href; }); return Object.freeze({ schemaVersion: 1, modelId: assistanceModelId(notice.modelId), version: text(notice.version, 64), purpose: text(notice.purpose, 1024), codeLicense: text(notice.codeLicense, 128), weightsLicense: text(notice.weightsLicense, 128), attributionRequired: notice.attributionRequired === true, provenanceSources: Object.freeze(sources), upstreamRevision: text(notice.upstreamRevision, 256), distributionKind: 'identity-mirrored' }); })); } function assistanceRelocation(value) { if (value?.contractVersion !== 1 || typeof value.sourceRemoved !== 'boolean') throw new TypeError('Malformed assistance relocation'); return Object.freeze({ contractVersion: 1, totalBytes: safeInteger(value.totalBytes), fileCount: safeInteger(value.fileCount), sourceRemoved: value.sourceRemoved }); }
const ASSISTANCE_OPERATIONS = Object.freeze(['voice-activity-detection', 'speech-recognition', 'word-alignment', 'speaker-diarization', 'speech-enhancement', 'source-separation', 'audio-tagging', 'beat-tracking', 'text-embedding', 'image-text-embedding', 'optical-character-recognition', 'shot-detection', 'subject-detection', 'saliency-detection', 'editorial-generation']); const ASSISTANCE_MODEL_TASKS = Object.freeze([...ASSISTANCE_OPERATIONS, 'speaker-segmentation', 'speaker-embedding', 'face-detection', 'object-detection']); const ASSISTANCE_INPUT_ROLES = Object.freeze(['audio', 'voice-activity', 'video', 'frame-pack', 'transcript', 'text', 'editorial-context', 'highlight-video-signals', 'highlight-audio-signals', 'highlight-transcript-signals']); const ASSISTANCE_OUTPUT_ROLES = Object.freeze(['voice-activity', 'transcript', 'word-alignment', 'speaker-turns', 'enhanced-audio', 'separated-audio', 'audio-tags', 'beat-grid', 'embeddings', 'recognized-text', 'shot-boundaries', 'subject-tracks', 'saliency-map', 'editorial-proposal']); const ASSISTANCE_WORKFLOW_CUSTODY_ROLES = Object.freeze([...ASSISTANCE_INPUT_ROLES, ...ASSISTANCE_OUTPUT_ROLES, 'video-authority', 'reaction-ranges', 'captions', 'cleanup-proposals', 'attributed-transcript', 'text-chunks', 'transcript-index', 'beat-labels', 'tempo-map-diff', 'cut-proposals', 'video-index', 'tracked-subjects', 'reframe-path', 'highlight-signals', 'highlight-candidates', 'highlight-proposals']); const ASSISTANCE_PROGRESS_PHASES = Object.freeze(['queued', 'staging-input', 'loading-model', 'running', 'staging-output', 'finalizing']);
const ASSISTANCE_WORKFLOW_SPECS = Object.freeze({ 'transcribe-captions': [['detect-speech', true, ['audio'], ['voice-activity'], ['vad']], ['recognize-speech', true, ['audio', 'voice-activity'], ['transcript'], ['speech-recognizer']], ['align-words', false, ['audio', 'transcript'], ['word-alignment'], ['alignment']], ['assemble-captions', true, ['transcript', '?word-alignment'], ['captions'], []]], 'clean-filler-silence': [['detect-speech', true, ['audio'], ['voice-activity'], ['vad']], ['recognize-speech', false, ['audio', 'voice-activity'], ['transcript'], ['speech-recognizer']], ['propose-cleanup', true, ['voice-activity', '?transcript'], ['cleanup-proposals'], []]], 'identify-speakers': [['diarize-speakers', true, ['audio'], ['speaker-turns'], ['diarizer', 'speaker-embedding']], ['attribute-speakers', true, ['transcript', 'speaker-turns'], ['attributed-transcript'], []]], 'enhance-dialogue': [['enhance-dialogue', true, ['audio'], ['enhanced-audio'], ['enhancer']]], 'separate-dialogue-music-effects': [['separate-sources', true, ['audio'], ['dialogue', 'music', 'effects'], ['separator']]], 'mark-reactions': [['tag-reactions', true, ['audio'], ['audio-tags'], ['audio-tagger']], ['merge-reaction-ranges', true, ['audio-tags'], ['reaction-ranges'], []]], 'index-transcript': [['chunk-transcript', true, ['transcript'], ['text-chunks'], []], ['embed-transcript', true, ['text-chunks'], ['embeddings'], ['text-embedder']], ['publish-transcript-index', true, ['text-chunks', 'embeddings'], ['transcript-index'], []]], 'detect-beats-tempo': [['track-beats', true, ['audio'], ['beat-grid'], ['beat-tracker']], ['propose-tempo-map', true, ['beat-grid'], ['beat-labels', 'tempo-map-diff'], []]], 'mark-cuts': [['detect-shots', true, ['video'], ['shot-boundaries'], ['?accurate-shot-detector']], ['normalize-cuts', true, ['shot-boundaries'], ['cut-proposals'], []]], 'index-video': [['detect-shots', true, ['video'], ['shot-boundaries'], ['?accurate-shot-detector']], ['sample-shot-frames', true, ['video', 'video-authority', 'shot-boundaries'], ['frame-pack'], []], ['embed-visuals', true, ['frame-pack'], ['visual-embeddings'], ['visual-embedder']], ['recognize-text', false, ['frame-pack'], ['recognized-text'], ['text-detector', 'text-recognizer']], ['publish-video-index', true, ['visual-embeddings', '?recognized-text'], ['video-index'], []]], reframe: [['detect-subjects', true, ['frame-pack'], ['subject-tracks'], ['face-detector', 'object-detector']], ['detect-saliency', true, ['frame-pack'], ['saliency-map'], ['saliency-detector']], ['track-subjects', true, ['subject-tracks'], ['tracked-subjects'], []], ['plan-crops', true, ['tracked-subjects', 'saliency-map'], ['reframe-path'], []]], 'make-highlights': [['detect-highlight-shots', true, ['video'], ['shot-boundaries'], []], ['tag-highlight-reactions', false, ['audio'], ['audio-tags'], ['audio-tagger']], ['gather-signals', true, ['video', '?audio', '?transcript', '?shot-boundaries', '?audio-tags', '?reaction-ranges', '?embeddings'], ['highlight-signals'], []], ['rank-highlights', true, ['highlight-signals'], ['highlight-candidates'], []], ['rerank-editorial', false, ['highlight-candidates'], ['editorial-proposal'], ['editorial-generator']], ['assemble-highlights', true, ['highlight-candidates', '?editorial-proposal'], ['highlight-proposals'], []]], 'generate-editorial-text': [['generate-editorial-text', true, ['editorial-context'], ['editorial-proposal'], ['editorial-generator']]] });
function assistanceOperationId(value) { return opaqueId(value, 40); } function assistanceOperationName(value) { if (!ASSISTANCE_OPERATIONS.includes(value)) throw new TypeError('Unsupported assistance operation'); return value; } function assistanceModelTask(value) { if (!ASSISTANCE_MODEL_TASKS.includes(value)) throw new TypeError('Unsupported assistance model task'); return value; } function assistanceDigest(value) { if (typeof value !== 'string' || !SHA256.test(value)) throw new TypeError('Invalid assistance SHA-256 digest'); return value; } function assistancePositiveBytes(value) { const bytes = safeInteger(value); if (bytes < 1 || bytes > 16 * 1024 ** 4) throw new RangeError('Invalid assistance byte length'); return bytes; } function assistanceSemanticSearchAuthority(value) { const identity = nativeProjectIdentity(value, 'assistance semantic-search'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'projectId', 'projectRevision'], 'assistance semantic-search project authority'); if (typeof value.projectId !== 'string' || !/^[A-Za-z\d][A-Za-z\d._:-]{0,255}$/u.test(value.projectId)) throw new TypeError('Invalid assistance semantic-search project ID'); return Object.freeze({ ...identity, projectId: value.projectId, projectRevision: safeInteger(value.projectRevision) }); } function assistanceSemanticSearchSession(value) { nativeRecord(value, ['sessionVersion', 'sessionId', 'schemaFamily', 'schemaVersion', 'projectId', 'projectRevision', 'expiresAtEpochMs'], 'assistance semantic-search session'); if (value.sessionVersion !== 1) throw new TypeError('Invalid assistance semantic-search session version'); const authority = assistanceSemanticSearchAuthority({ schemaFamily: value.schemaFamily, schemaVersion: value.schemaVersion, projectId: value.projectId, projectRevision: value.projectRevision }), sessionId = assistanceOperationId(value.sessionId), expiresAtEpochMs = safeInteger(value.expiresAtEpochMs); if (expiresAtEpochMs < 1) throw new RangeError('Invalid assistance semantic-search session expiry'); return Object.freeze({ sessionVersion: 1, sessionId, ...authority, expiresAtEpochMs }); } function assistanceSemanticSearchAuthorization(value) { nativeRecord(value, ['session', 'schemaFamily', 'schemaVersion', 'projectId', 'projectRevision'], 'assistance semantic-search authorization'); const authority = assistanceSemanticSearchAuthority({ schemaFamily: value.schemaFamily, schemaVersion: value.schemaVersion, projectId: value.projectId, projectRevision: value.projectRevision }), session = assistanceSemanticSearchSession(value.session); if (session.schemaFamily !== authority.schemaFamily || session.schemaVersion !== authority.schemaVersion || session.projectId !== authority.projectId || session.projectRevision !== authority.projectRevision) throw new TypeError('Invalid assistance semantic-search session authority'); return Object.freeze({ session, ...authority }); } function assistanceSemanticSearchQuery(value) { nativeRecord(value, ['queryVersion', 'queryId', 'session', 'schemaFamily', 'schemaVersion', 'projectId', 'projectRevision', 'provider', 'query'], 'assistance semantic-search query'); if (value.queryVersion !== 1 || !['transcript', 'visual'].includes(value.provider)) throw new TypeError('Invalid assistance semantic-search query version or provider'); const authorization = assistanceSemanticSearchAuthorization({ session: value.session, schemaFamily: value.schemaFamily, schemaVersion: value.schemaVersion, projectId: value.projectId, projectRevision: value.projectRevision }), query = value.query; if (typeof query !== 'string' || !query.trim() || query.length > 512 || [...query].some((character) => { const code = character.charCodeAt(0); return code < 32 || code === 127; })) throw new TypeError('Invalid assistance semantic-search query text'); return Object.freeze({ queryVersion: 1, queryId: assistanceOperationId(value.queryId), ...authorization, provider: value.provider, query }); } function assistanceSemanticSearchQueryResult(value) { if (value?.outcome === 'unavailable') { nativeRecord(value, ['queryVersion', 'queryId', 'outcome', 'reason'], 'assistance semantic-search unavailable query result'); if (value.queryVersion !== 1 || !['model-unavailable', 'runtime-unavailable'].includes(value.reason)) throw new TypeError('Invalid assistance semantic-search unavailable query result'); return Object.freeze({ queryVersion: 1, queryId: assistanceOperationId(value.queryId), outcome: 'unavailable', reason: value.reason }); } nativeRecord(value, ['queryVersion', 'queryId', 'outcome', 'provider', 'embedding'], 'assistance semantic-search query result'); if (value.queryVersion !== 1 || value.outcome !== 'completed' || !['transcript', 'visual'].includes(value.provider) || !Array.isArray(value.embedding) || value.embedding.length !== 768 || value.embedding.some((entry) => typeof entry !== 'number' || !Number.isFinite(entry))) throw new TypeError('Invalid assistance semantic-search query result'); return Object.freeze({ queryVersion: 1, queryId: assistanceOperationId(value.queryId), outcome: 'completed', provider: value.provider, embedding: Object.freeze([...value.embedding]) }); }
function assistanceOperationModels(value) { if (!Array.isArray(value) || value.length > 256) throw new TypeError('Malformed authenticated assistance models'); return Object.freeze(value.map((model) => { if (!Array.isArray(model?.artifactSha256s) || model.artifactSha256s.length < 1 || model.artifactSha256s.length > 64) throw new TypeError('Malformed assistance model binding'); const digests = Object.freeze(model.artifactSha256s.map(assistanceDigest)); if (digests.some((digest, index) => index > 0 && digest <= digests[index - 1])) throw new TypeError('Assistance artifact digests must be sorted and unique'); return Object.freeze({ modelId: assistanceModelId(model.modelId), version: nativeBoundedText(model.version, 64, 'assistance model version'), task: assistanceModelTask(model.task), artifactSha256s: digests }); })); }
function assistanceOperationJob(value) { if (value?.contractVersion !== 1) throw new TypeError('Malformed assistance operation job'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId) }); } function assistanceOperationCancellation(value) { if (value?.contractVersion !== 1 || !['cancelled', 'not-active'].includes(value.outcome)) throw new TypeError('Malformed assistance operation cancellation'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId), outcome: value.outcome }); }
function assistanceInputClaim(value) { nativeRecord(value, ['claimVersion', 'claimId', 'jobId', 'role', 'mediaType', 'byteLength', 'sha256'], 'assistance input claim'); if (value.claimVersion !== 1 || !ASSISTANCE_INPUT_ROLES.includes(value.role)) throw new TypeError('Malformed assistance input claim'); return Object.freeze({ claimVersion: 1, claimId: assistanceOperationId(value.claimId), jobId: assistanceOperationId(value.jobId), role: value.role, mediaType: assistanceMediaType(value.mediaType), byteLength: assistancePositiveBytes(value.byteLength), sha256: assistanceDigest(value.sha256) }); } function assistanceOutputReservation(value) { nativeRecord(value, ['claimVersion', 'claimId', 'jobId', 'role', 'mediaType', 'maximumByteLength'], 'assistance output reservation'); if (value.claimVersion !== 1 || !ASSISTANCE_OUTPUT_ROLES.includes(value.role)) throw new TypeError('Malformed assistance output reservation'); return Object.freeze({ claimVersion: 1, claimId: assistanceOperationId(value.claimId), jobId: assistanceOperationId(value.jobId), role: value.role, mediaType: assistanceMediaType(value.mediaType), maximumByteLength: assistancePositiveBytes(value.maximumByteLength) }); } function assistanceOutputClaim(value) { nativeRecord(value, ['claimVersion', 'claimId', 'jobId', 'role', 'mediaType', 'byteLength', 'sha256'], 'assistance output claim'); if (value.claimVersion !== 1 || !ASSISTANCE_OUTPUT_ROLES.includes(value.role)) throw new TypeError('Malformed assistance output claim'); return Object.freeze({ claimVersion: 1, claimId: assistanceOperationId(value.claimId), jobId: assistanceOperationId(value.jobId), role: value.role, mediaType: assistanceMediaType(value.mediaType), byteLength: assistancePositiveBytes(value.byteLength), sha256: assistanceDigest(value.sha256) }); } function assistanceMediaType(value) { if (typeof value !== 'string' || value.length > 255 || !/^[a-z\d][a-z\d!#$&^_.+-]*\/[a-z\d][a-z\d!#$&^_.+-]*$/u.test(value)) throw new TypeError('Malformed assistance media type'); return value; }
function assistanceOutputReservationRequest(value) { nativeRecord(value, ['jobId', 'role', 'mediaType', 'maximumByteLength'], 'assistance output reservation request'); if (!ASSISTANCE_OUTPUT_ROLES.includes(value.role)) throw new TypeError('Unsupported assistance output role'); return Object.freeze({ jobId: assistanceOperationId(value.jobId), role: value.role, mediaType: assistanceMediaType(value.mediaType), maximumByteLength: assistancePositiveBytes(value.maximumByteLength) }); }
function assistanceSelectionFence(value) { const fields = ['projectId', 'schemaFamily', 'schemaVersion', 'revision', 'sequenceId', 'occurrenceIds', 'sourceId', 'sourceSha256', 'sourceStartFrame', 'sourceEndFrame', 'linkMembershipSha256', 'timingAuthoritySha256']; const identity = nativeProjectIdentity(value, 'assistance selection'); nativeRecord(value, fields, 'assistance selection fence'); if (identity.schemaVersion !== 1 || !Array.isArray(value.occurrenceIds) || value.occurrenceIds.length < 1 || value.occurrenceIds.length > 256 || value.sourceEndFrame <= value.sourceStartFrame) throw new TypeError('Malformed assistance selection fence'); return nativeDeepFreeze({ projectId: nativeBoundedText(value.projectId, 256, 'assistance project id'), ...identity, revision: safeInteger(value.revision), sequenceId: nativeBoundedText(value.sequenceId, 256, 'assistance sequence id'), occurrenceIds: value.occurrenceIds.map((id) => nativeBoundedText(id, 256, 'assistance occurrence id')), sourceId: nativeBoundedText(value.sourceId, 256, 'assistance source id'), sourceSha256: assistanceDigest(value.sourceSha256), sourceStartFrame: safeInteger(value.sourceStartFrame), sourceEndFrame: safeInteger(value.sourceEndFrame), linkMembershipSha256: assistanceDigest(value.linkMembershipSha256), timingAuthoritySha256: assistanceDigest(value.timingAuthoritySha256) }); }
function assistanceModelBinding(value) { nativeRecord(value, ['modelId', 'version', 'artifactSha256s'], 'assistance model binding'); const models = assistanceOperationModels([{ ...value, task: 'speech-recognition' }]); return Object.freeze({ modelId: models[0].modelId, version: models[0].version, artifactSha256s: models[0].artifactSha256s }); } function assistanceOperationRequest(value) { nativeRecord(value, ['contractVersion', 'jobId', 'operation', 'selectionFence', 'models', 'inputs', 'outputs'], 'assistance operation request'); if (value.contractVersion !== 1 || !Array.isArray(value.models) || !Array.isArray(value.inputs) || !Array.isArray(value.outputs) || value.models.length > 8 || value.inputs.length < 1 || value.inputs.length > 64 || value.outputs.length < 1 || value.outputs.length > 64) throw new TypeError('Malformed assistance operation request'); const jobId = assistanceOperationId(value.jobId), operation = assistanceOperationName(value.operation), inputs = Object.freeze(value.inputs.map(assistanceInputClaim)), outputs = Object.freeze(value.outputs.map(assistanceOutputReservation)); if ([...inputs, ...outputs].some((claim) => claim.jobId !== jobId)) throw new TypeError('Assistance claims must correlate with their job'); return Object.freeze({ contractVersion: 1, jobId, operation, selectionFence: assistanceSelectionFence(value.selectionFence), models: Object.freeze(value.models.map(assistanceModelBinding)), inputs, outputs }); }
function assistanceOperationResult(value, request) { if (value?.contractVersion !== 1 || value?.jobId !== request.jobId || value?.operation !== request.operation || !Array.isArray(value.outputs) || value.outputs.length !== request.outputs.length) throw new TypeError('Malformed assistance operation result'); const outputs = Object.freeze(value.outputs.map(assistanceOutputClaim)); for (const output of outputs) { const reservation = request.outputs.find(({ claimId }) => claimId === output.claimId); if (!reservation || output.jobId !== request.jobId || output.role !== reservation.role || output.mediaType !== reservation.mediaType || output.byteLength > reservation.maximumByteLength) throw new TypeError('Assistance result disagrees with its reservation'); } return Object.freeze({ contractVersion: 1, jobId: request.jobId, operation: request.operation, outputs }); }
function assistanceOperationOutcome(value, request) { if (value?.contractVersion !== 1 || value?.jobId !== request.jobId || value?.operation !== request.operation) throw new TypeError('Malformed assistance operation outcome'); if (value.outcome === 'completed') return Object.freeze({ contractVersion: 1, jobId: request.jobId, operation: request.operation, outcome: 'completed', result: assistanceOperationResult(value.result, request) }); if (value.outcome === 'consent-declined' && Object.keys(value).length === 4) return Object.freeze({ contractVersion: 1, jobId: request.jobId, operation: request.operation, outcome: 'consent-declined' }); if (value.outcome !== 'unavailable' || !['adapter-unavailable', 'runtime-unavailable', 'model-unavailable'].includes(value.reason)) throw new TypeError('Malformed unavailable assistance operation'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, operation: request.operation, outcome: 'unavailable', reason: value.reason }); }
function assistanceOperationProgress(value) { if (value?.contractVersion !== 1 || !ASSISTANCE_PROGRESS_PHASES.includes(value.phase) || !Number.isSafeInteger(value.sequence) || value.sequence < 0 || !((value.completed === null && value.total === null) || Number.isSafeInteger(value.completed) && Number.isSafeInteger(value.total) && value.completed >= 0 && value.total > 0 && value.completed <= value.total)) throw new TypeError('Malformed assistance operation progress'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId), operation: assistanceOperationName(value.operation), sequence: value.sequence, phase: value.phase, completed: value.completed, total: value.total }); }
const ASSISTANCE_ADVANCED_WORKFLOW_SPECS = Object.freeze({ 'voice-activity-detection': [['audio'], ['voice-activity'], ['model']], 'speech-recognition': [['audio', '?voice-activity'], ['transcript'], ['model']], 'word-alignment': [['audio', 'transcript'], ['word-alignment'], ['model']], 'speaker-diarization': [['audio'], ['speaker-turns'], ['diarizer', 'speaker-embedding']], 'speech-enhancement': [['audio'], ['enhanced-audio'], ['model']], 'source-separation': [['audio'], ['dialogue', 'music', 'effects'], ['model']], 'audio-tagging': [['audio'], ['audio-tags'], ['model']], 'beat-tracking': [['audio'], ['beat-grid'], ['model']], 'text-embedding': [['?transcript', '?text'], ['embeddings'], ['model']], 'image-text-embedding': [['?frame-pack', '?text'], ['embeddings'], ['model']], 'optical-character-recognition': [['frame-pack'], ['recognized-text'], ['model']], 'shot-detection': [['?video', '?frame-pack'], ['shot-boundaries'], ['?model']], 'subject-detection': [['frame-pack'], ['subject-tracks'], ['face-detector', 'object-detector']], 'saliency-detection': [['frame-pack'], ['saliency-map'], ['model']], 'editorial-generation': [['editorial-context'], ['editorial-proposal'], ['model']] }); function assistanceWorkflowSpec(value) { if (typeof value !== 'string') throw new TypeError('Unsupported assistance workflow'); if (Object.hasOwn(ASSISTANCE_WORKFLOW_SPECS, value)) { const spec = ASSISTANCE_WORKFLOW_SPECS[value]; return ['mark-cuts', 'index-video'].includes(value) ? [[spec[0][0], spec[0][1], ['?video', '?frame-pack'], spec[0][3], spec[0][4]], ...spec.slice(1)] : spec; } if (!value.startsWith('advanced:')) throw new TypeError('Unsupported assistance workflow'); const operation = assistanceOperationName(value.slice(9)), [inputs, outputs, models] = ASSISTANCE_ADVANCED_WORKFLOW_SPECS[operation]; return [[`run-${operation}`, true, inputs, outputs, models]]; }
function assistanceWorkflowArray(value, minimum, maximum, label) { if (!Array.isArray(value) || value.length < minimum || value.length > maximum || Reflect.ownKeys(value).length !== value.length + 1) throw new TypeError(`Malformed assistance workflow ${label}`); return value; } function assistanceWorkflowSlot(value) { if (typeof value !== 'string' || !/^[a-z\d](?:[a-z\d.-]{0,62}[a-z\d])?$/u.test(value)) throw new TypeError('Malformed assistance workflow slot'); return value; } function assistanceWorkflowDomainId(value) { if (typeof value !== 'string' || !/^[A-Za-z\d][A-Za-z\d._:-]{0,255}$/u.test(value)) throw new TypeError('Malformed assistance workflow identity'); return value; }
function assistanceWorkflowStages(value, spec) { const stages = assistanceWorkflowArray(value, 1, spec.length, 'stages').map(assistanceWorkflowSlot); if (new Set(stages).size !== stages.length || stages.some((stage) => !spec.some(([id]) => id === stage))) throw new TypeError('Malformed assistance workflow stages'); const selected = new Set(stages), canonical = spec.filter(([id, required]) => required || selected.has(id)).map(([id]) => id); if (canonical.length !== stages.length || canonical.some((id, index) => id !== stages[index])) throw new TypeError('Malformed assistance workflow required stages'); return Object.freeze(stages); }
function assistanceWorkflowSourceRange(value, occurrences) { nativeRecord(value, ['slotId', 'mediaKind', 'sourceId', 'sourceSha256', 'sourceSampleRate', 'occurrenceIds', 'sourceStartFrame', 'sourceEndFrame', 'linkMembershipSha256', 'timingAuthoritySha256', 'retimeKind'], 'assistance workflow source range'); const start = safeInteger(value.sourceStartFrame), end = safeInteger(value.sourceEndFrame), sourceSampleRate = value.sourceSampleRate === null ? null : safeInteger(value.sourceSampleRate); if (start < 0 || end <= start || !['audio', 'video'].includes(value.mediaKind) || !['identity', 'monotonic-forward'].includes(value.retimeKind) || value.mediaKind === 'audio' && (sourceSampleRate === null || sourceSampleRate < 8000 || sourceSampleRate > 768000) || value.mediaKind === 'video' && sourceSampleRate !== null) throw new TypeError('Malformed assistance workflow source range'); const occurrenceIds = assistanceWorkflowArray(value.occurrenceIds, 1, 256, 'occurrences').map(assistanceWorkflowDomainId); for (const id of occurrenceIds) { if (occurrences.has(id)) throw new TypeError('Repeated assistance workflow occurrence'); occurrences.add(id); } return Object.freeze({ slotId: assistanceWorkflowSlot(value.slotId), mediaKind: value.mediaKind, sourceId: assistanceWorkflowDomainId(value.sourceId), sourceSha256: assistanceDigest(value.sourceSha256), sourceSampleRate, occurrenceIds: Object.freeze(occurrenceIds), sourceStartFrame: start, sourceEndFrame: end, linkMembershipSha256: assistanceDigest(value.linkMembershipSha256), timingAuthoritySha256: assistanceDigest(value.timingAuthoritySha256), retimeKind: value.retimeKind }); }
function assistanceWorkflowFence(value) { const identity = nativeProjectIdentity(value, 'assistance workflow'); nativeRecord(value, ['fenceVersion', 'projectId', 'schemaFamily', 'schemaVersion', 'revision', 'sequenceId', 'sourceRanges', 'transcriptBodySha256', 'recipeSha256', 'settingsSha256', 'modelBindingsSha256'], 'assistance workflow fence'); if (value.fenceVersion !== 1 || identity.schemaVersion !== 1) throw new TypeError('Malformed assistance workflow fence'); const occurrences = new Set(), ranges = assistanceWorkflowArray(value.sourceRanges, 1, 64, 'source ranges').map((range) => assistanceWorkflowSourceRange(range, occurrences)); if (occurrences.size > 1024) throw new TypeError('Malformed assistance workflow occurrence bound'); const keys = ranges.map((range) => `${range.slotId}\0${range.sourceId}\0${String(range.sourceStartFrame).padStart(16, '0')}\0${String(range.sourceEndFrame).padStart(16, '0')}`); if (keys.some((key, index) => index > 0 && key <= keys[index - 1])) throw new TypeError('Malformed assistance workflow source order'); const revision = safeInteger(value.revision); if (revision < 0) throw new TypeError('Malformed assistance workflow project authority'); return Object.freeze({ fenceVersion: 1, projectId: assistanceWorkflowDomainId(value.projectId), ...identity, revision, sequenceId: assistanceWorkflowDomainId(value.sequenceId), sourceRanges: Object.freeze(ranges), transcriptBodySha256: value.transcriptBodySha256 === null ? null : assistanceDigest(value.transcriptBodySha256), recipeSha256: assistanceDigest(value.recipeSha256), settingsSha256: assistanceDigest(value.settingsSha256), modelBindingsSha256: assistanceDigest(value.modelBindingsSha256) }); }
function assistanceWorkflowClaim(value, direction, jobId, stages, spec) { nativeRecord(value, ['claimVersion', 'direction', 'claimId', 'jobId', 'stageId', 'slotId'], 'assistance workflow claim'); if (value.claimVersion !== 1 || value.direction !== direction || assistanceOperationId(value.jobId) !== jobId) throw new TypeError('Malformed assistance workflow claim'); const stageId = assistanceWorkflowSlot(value.stageId), slotId = assistanceWorkflowSlot(value.slotId), stage = spec.find(([id]) => id === stageId); if (!stages.includes(stageId) || !stage || !stage[direction === 'input' ? 2 : 3].some((slot) => slot.replace(/^\?/u, '') === slotId)) throw new TypeError(`Malformed assistance workflow ${direction} slot`); return Object.freeze({ claimVersion: 1, direction, claimId: assistanceOperationId(value.claimId), jobId, stageId, slotId }); }
function assistanceWorkflowModel(value, stages, spec) { nativeRecord(value, ['bindingVersion', 'stageId', 'slotId', 'modelId', 'version', 'artifactSha256s'], 'assistance workflow model'); if (value.bindingVersion !== 1) throw new TypeError('Malformed assistance workflow model binding'); const stageId = assistanceWorkflowSlot(value.stageId), slotId = assistanceWorkflowSlot(value.slotId), stage = spec.find(([id]) => id === stageId); if (!stages.includes(stageId) || !stage || !stage[4].some((slot) => slot.replace(/^\?/u, '') === slotId)) throw new TypeError('Malformed assistance workflow model slot'); const artifacts = assistanceWorkflowArray(value.artifactSha256s, 1, 64, 'model artifacts').map(assistanceDigest); if (artifacts.some((digest, index) => index > 0 && digest <= artifacts[index - 1])) throw new TypeError('Malformed assistance workflow model artifacts'); return Object.freeze({ bindingVersion: 1, stageId, slotId, modelId: assistanceModelId(value.modelId), version: nativeBoundedText(value.version, 128, 'assistance workflow model version'), artifactSha256s: Object.freeze(artifacts) }); }
function assistanceWorkflowRequired(stages, spec, inputs, outputs, models) { for (const stageId of stages) { const stage = spec.find(([id]) => id === stageId); for (const [bindings, slots, label] of [[inputs, stage[2], 'input'], [outputs, stage[3], 'output'], [models, stage[4], 'model']]) for (const slot of slots) if (!slot.startsWith('?') && !bindings.some((binding) => binding.stageId === stageId && binding.slotId === slot)) throw new TypeError(`Missing assistance workflow ${label} slot`); } }
function assistanceWorkflowJsonClone(value, depth, state) { state.nodes += 1; if (state.nodes > 4096 || depth > 16) throw new TypeError('Malformed assistance workflow settings bound'); if (value === null || typeof value === 'boolean') return value; if (typeof value === 'string') return nativeBoundedText(value, 65536, 'assistance workflow setting'); if (typeof value === 'number') { if (!Number.isFinite(value)) throw new TypeError('Malformed assistance workflow setting number'); return value; } if (Array.isArray(value)) { if (value.length > 256 || Reflect.ownKeys(value).length !== value.length + 1) throw new TypeError('Malformed assistance workflow setting array'); return Object.freeze(value.map((item) => assistanceWorkflowJsonClone(item, depth + 1, state))); } if (!value || typeof value !== 'object' || ArrayBuffer.isView(value)) throw new TypeError('Malformed assistance workflow setting'); const keys = Reflect.ownKeys(value); if (keys.length > 128 || keys.some((key) => typeof key !== 'string' || key.length < 1 || key.length > 128)) throw new TypeError('Malformed assistance workflow setting fields'); const clone = {}; for (const key of keys) clone[key] = assistanceWorkflowJsonClone(value[key], depth + 1, state); return Object.freeze(clone); } function assistanceWorkflowSettings(value, workflowId, settingsVersion) { const settings = assistanceWorkflowJsonClone(value, 0, { nodes: 0 }); if (!settings || typeof settings !== 'object' || Array.isArray(settings) || settings.workflowId !== workflowId || settings.settingsVersion !== settingsVersion || JSON.stringify(settings).length > 65536) throw new TypeError('Malformed assistance workflow settings'); return settings; } function assistanceWorkflowUniqueInputs(inputs) { const seen = new Map(); for (const binding of inputs) { const key = `${binding.stageId}\0${binding.slotId}`, claimIds = seen.get(key) ?? new Set(); if (claimIds.size > 0 && binding.slotId !== 'frame-pack' || claimIds.has(binding.claimId) || claimIds.size >= 64) throw new TypeError('Repeated assistance workflow input slot'); claimIds.add(binding.claimId); seen.set(key, claimIds); } } function assistanceWorkflowUniqueBindings(bindings) { if (new Set(bindings.map(({ stageId, slotId }) => `${stageId}\0${slotId}`)).size !== bindings.length) throw new TypeError('Repeated assistance workflow slot'); } function assistanceWorkflowRequest(value) { nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'recipeVersion', 'settingsVersion', 'settings', 'fence', 'stageIds', 'models', 'inputs', 'outputs'], 'assistance workflow request'); const recipeVersion = safeInteger(value.recipeVersion), settingsVersion = safeInteger(value.settingsVersion); if (value.contractVersion !== 1 || recipeVersion < 1 || settingsVersion < 1) throw new TypeError('Malformed assistance workflow request'); const jobId = assistanceOperationId(value.jobId), spec = assistanceWorkflowSpec(value.workflowId), settings = assistanceWorkflowSettings(value.settings, value.workflowId, settingsVersion), stageIds = assistanceWorkflowStages(value.stageIds, spec), inputs = assistanceWorkflowArray(value.inputs, 1, 256, 'input claims').map((claim) => assistanceWorkflowClaim(claim, 'input', jobId, stageIds, spec)), outputs = assistanceWorkflowArray(value.outputs, 1, 256, 'output claims').map((claim) => assistanceWorkflowClaim(claim, 'output', jobId, stageIds, spec)), models = assistanceWorkflowArray(value.models, 0, 64, 'model bindings').map((model) => assistanceWorkflowModel(model, stageIds, spec)); assistanceWorkflowUniqueInputs(inputs); assistanceWorkflowUniqueBindings(outputs); assistanceWorkflowUniqueBindings(models); assistanceWorkflowRequired(stageIds, spec, inputs, outputs, models); return Object.freeze({ contractVersion: 1, jobId, workflowId: value.workflowId, recipeVersion, settingsVersion, settings, fence: assistanceWorkflowFence(value.fence), stageIds, models: Object.freeze(models), inputs: Object.freeze(inputs), outputs: Object.freeze(outputs) }); }
function assistanceWorkflowJob(value) { nativeRecord(value, ['contractVersion', 'jobId'], 'assistance workflow job'); if (value.contractVersion !== 1) throw new TypeError('Malformed assistance workflow job'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId) }); } function assistanceWorkflowCancellation(value) { nativeRecord(value, ['contractVersion', 'jobId', 'outcome'], 'assistance workflow cancellation'); if (value.contractVersion !== 1 || !['cancelled', 'not-active'].includes(value.outcome)) throw new TypeError('Malformed assistance workflow cancellation'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId), outcome: value.outcome }); }
function assistanceWorkflowOutcome(value, request) { if (value?.contractVersion !== 1 || value?.jobId !== request.jobId || value?.workflowId !== request.workflowId) throw new TypeError('Malformed assistance workflow outcome'); if (value.outcome === 'consent-declined') { nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'outcome'], 'assistance workflow outcome'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, workflowId: request.workflowId, outcome: 'consent-declined' }); } if (value.outcome === 'unavailable') { nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'outcome', 'reason'], 'assistance workflow outcome'); if (!['workflow-runner-unavailable', 'stage-unavailable', 'model-unavailable'].includes(value.reason)) throw new TypeError('Malformed assistance workflow unavailable reason'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, workflowId: request.workflowId, outcome: 'unavailable', reason: value.reason }); } nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'outcome', 'result'], 'assistance workflow outcome'); if (value.outcome !== 'completed') throw new TypeError('Malformed assistance workflow outcome'); const result = value.result; nativeRecord(result, ['contractVersion', 'jobId', 'workflowId', 'stageIds', 'outputs'], 'assistance workflow result'); if (result.contractVersion !== 1 || result.jobId !== request.jobId || result.workflowId !== request.workflowId || JSON.stringify(result.stageIds) !== JSON.stringify(request.stageIds) || JSON.stringify(result.outputs) !== JSON.stringify(request.outputs)) throw new TypeError('Malformed assistance workflow result'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, workflowId: request.workflowId, outcome: 'completed', result: Object.freeze({ contractVersion: 1, jobId: request.jobId, workflowId: request.workflowId, stageIds: request.stageIds, outputs: request.outputs }) }); }
function assistanceWorkflowProgress(value) { nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'sequence', 'stageId', 'stageIndex', 'stageCount', 'phase', 'completed', 'total'], 'assistance workflow progress'); const spec = assistanceWorkflowSpec(value.workflowId), sequence = safeInteger(value.sequence), stageIndex = safeInteger(value.stageIndex), stageCount = safeInteger(value.stageCount), units = value.completed === null && value.total === null || Number.isFinite(value.completed) && Number.isFinite(value.total) && value.completed >= 0 && value.total > 0 && value.completed <= value.total; if (value.contractVersion !== 1 || sequence < 0 || stageIndex < 0 || stageCount < 1 || stageCount > spec.length || stageIndex >= stageCount || !spec.some(([id]) => id === value.stageId) || !ASSISTANCE_PROGRESS_PHASES.includes(value.phase) || !units) throw new TypeError('Malformed assistance workflow progress'); return Object.freeze({ contractVersion: 1, jobId: assistanceOperationId(value.jobId), workflowId: value.workflowId, sequence, stageId: value.stageId, stageIndex, stageCount, phase: value.phase, completed: value.completed, total: value.total }); }
function assistanceWorkflowSlotBinding(workflowIdValue, stageIdValue, direction, slotIdValue) { if (typeof workflowIdValue !== 'string') throw new TypeError('Malformed assistance workflow custody recipe'); const graph = assistanceWorkflowSpec(workflowIdValue), stageId = assistanceWorkflowSlot(stageIdValue), slotId = assistanceWorkflowSlot(slotIdValue), stageIndex = graph.findIndex(([id]) => id === stageId), slots = stageIndex < 0 ? [] : graph[stageIndex][direction === 'input' ? 2 : 3]; if (!['input', 'output'].includes(direction) || !slots.some((slot) => slot.replace(/^\?/u, '') === slotId)) throw new TypeError(`Malformed assistance workflow custody ${direction} slot`); return Object.freeze({ workflowId: workflowIdValue, stageId, slotId, stageIndex, graph }); } function assistanceWorkflowExternalCustodySpec(workflowId, stageId, slotId) { const highlightRole = workflowId === 'make-highlights' && stageId === 'gather-signals' && ['video', 'audio', 'transcript'].includes(slotId) ? `highlight-${slotId}-signals` : null; if (highlightRole !== null) return Object.freeze({ role: highlightRole, mediaTypes: Object.freeze([`application/vnd.soundscaper.${highlightRole}+json`]) }); const admitted = ['audio', 'voice-activity', 'video', 'video-authority', 'frame-pack', 'transcript', 'text', 'editorial-context', 'shot-boundaries', 'reaction-ranges', 'embeddings']; if (!admitted.includes(slotId)) throw new TypeError('The workflow slot has no direct external custody format'); const mediaTypes = slotId === 'audio' ? ['audio/wav', 'audio/x-wav', 'audio/flac'] : slotId === 'video' ? ['video/mp4', 'video/quicktime', 'video/webm', 'video/x-matroska'] : slotId === 'frame-pack' ? ['application/vnd.soundscaper.frame-pack'] : slotId === 'text' ? ['text/plain'] : slotId === 'embeddings' ? ['application/vnd.soundscaper.embedding-matrix-v1'] : ['application/json', `application/vnd.soundscaper.${slotId}+json`]; return Object.freeze({ role: slotId, mediaTypes: Object.freeze(mediaTypes) }); } function assistanceWorkflowOutputCustodySpec(slotId) { const role = ['dialogue', 'music', 'effects'].includes(slotId) ? 'separated-audio' : slotId === 'visual-embeddings' ? 'embeddings' : slotId; if (!ASSISTANCE_WORKFLOW_CUSTODY_ROLES.includes(role)) throw new TypeError('The workflow output slot has no custody format'); const mediaTypes = ['enhanced-audio', 'dialogue', 'music', 'effects'].includes(slotId) ? ['audio/wav', 'audio/flac'] : ['embeddings', 'visual-embeddings'].includes(slotId) ? ['application/vnd.soundscaper.embedding-matrix-v1'] : slotId === 'frame-pack' ? ['application/vnd.soundscaper.frame-pack'] : ['application/json', `application/vnd.soundscaper.${slotId}+json`]; return Object.freeze({ role, mediaTypes: Object.freeze(mediaTypes) }); } function assistanceWorkflowCustodyProducer(value) { nativeRecord(value, ['stageId', 'slotId', 'claimId'], 'assistance workflow custody producer'); return Object.freeze({ stageId: assistanceWorkflowSlot(value.stageId), slotId: assistanceWorkflowSlot(value.slotId), claimId: assistanceOperationId(value.claimId) }); } function assistanceWorkflowCustody(value) { nativeRecord(value, ['custodyVersion', 'workflowId', 'direction', 'jobId', 'stageId', 'slotId', 'claimId', 'role', 'mediaType', 'byteLength', 'sha256', 'maximumByteLength', 'producer'], 'assistance workflow custody claim'); if (value.custodyVersion !== 1 || !['input', 'output'].includes(value.direction)) throw new TypeError('Malformed assistance workflow custody claim'); const jobId = assistanceOperationId(value.jobId), claimId = assistanceOperationId(value.claimId), producer = value.producer === null ? null : assistanceWorkflowCustodyProducer(value.producer); let binding, expected; if (value.direction === 'output') { binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'output', value.slotId); expected = assistanceWorkflowOutputCustodySpec(binding.slotId); if (producer !== null || value.byteLength !== null || value.sha256 !== null || value.maximumByteLength === null) throw new TypeError('Malformed workflow output custody reservation'); } else if (producer === null) { binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'input', value.slotId); expected = assistanceWorkflowExternalCustodySpec(binding.workflowId, binding.stageId, binding.slotId); if (value.byteLength === null || value.sha256 === null || value.maximumByteLength !== null) throw new TypeError('Malformed external workflow input custody'); } else { binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'input', value.slotId); const produced = assistanceWorkflowSlotBinding(value.workflowId, producer.stageId, 'output', producer.slotId); expected = assistanceWorkflowOutputCustodySpec(produced.slotId); if (produced.stageIndex >= binding.stageIndex || producer.slotId !== binding.slotId || producer.claimId !== claimId || value.byteLength !== null || value.sha256 !== null || value.maximumByteLength === null) throw new TypeError('Malformed intermediate workflow custody'); } const mediaType = assistanceMediaType(value.mediaType); if (value.role !== expected.role || !expected.mediaTypes.includes(mediaType)) throw new TypeError('Workflow custody format disagrees with its slot'); const byteLength = value.byteLength === null ? null : assistancePositiveBytes(value.byteLength), sha256 = value.sha256 === null ? null : assistanceDigest(value.sha256), maximumByteLength = value.maximumByteLength === null ? null : assistancePositiveBytes(value.maximumByteLength); return Object.freeze({ custodyVersion: 1, workflowId: binding.workflowId, direction: value.direction, jobId, stageId: binding.stageId, slotId: binding.slotId, claimId, role: expected.role, mediaType, byteLength, sha256, maximumByteLength, producer }); } function assistanceWorkflowCustodyHandle(value, expected, direction) { nativeRecord(value, ['custody', 'workflowClaim'], 'assistance workflow custody handle'); const custody = assistanceWorkflowCustody(value.custody), workflowClaim = value.workflowClaim; nativeRecord(workflowClaim, ['claimVersion', 'direction', 'claimId', 'jobId', 'stageId', 'slotId'], 'assistance workflow custody claim identity'); if (custody.direction !== direction || custody.jobId !== expected.jobId || custody.workflowId !== expected.workflowId || custody.stageId !== expected.stageId || custody.slotId !== expected.slotId || workflowClaim.claimVersion !== 1 || workflowClaim.direction !== custody.direction || workflowClaim.claimId !== custody.claimId || workflowClaim.jobId !== custody.jobId || workflowClaim.stageId !== custody.stageId || workflowClaim.slotId !== custody.slotId) throw new TypeError('Assistance workflow custody handle is not correlated'); if (Object.hasOwn(expected, 'mediaType') && (custody.mediaType !== expected.mediaType || custody.byteLength !== expected.byteLength || custody.sha256 !== expected.sha256)) throw new TypeError('Staged workflow custody disagrees with its Blob'); if (Object.hasOwn(expected, 'maximumByteLength') && custody.maximumByteLength !== expected.maximumByteLength) throw new TypeError('Workflow custody reservation size disagrees'); if (Object.hasOwn(expected, 'producer') && (!custody.producer || custody.producer.stageId !== expected.producer.stageId || custody.producer.slotId !== expected.producer.slotId || custody.producer.claimId !== expected.producer.claimId || custody.claimId !== expected.producer.claimId || custody.maximumByteLength !== expected.producer.maximumByteLength)) throw new TypeError('Workflow custody producer is not correlated'); return Object.freeze({ custody, workflowClaim: Object.freeze({ claimVersion: 1, direction: custody.direction, claimId: custody.claimId, jobId: custody.jobId, stageId: custody.stageId, slotId: custody.slotId }) }); } function assistanceWorkflowStageInputRequest(value) { nativeRecord(value, ['jobId', 'workflowId', 'stageId', 'slotId', 'mediaType', 'byteLength', 'sha256', 'bytes'], 'assistance workflow staged input'); const binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'input', value.slotId), expected = assistanceWorkflowExternalCustodySpec(binding.workflowId, binding.stageId, binding.slotId); if (typeof Blob !== 'function' || !(value.bytes instanceof Blob)) throw new TypeError('Malformed assistance workflow staged Blob'); const actualBytes = Reflect.apply(Object.getOwnPropertyDescriptor(Blob.prototype, 'size').get, value.bytes, []), byteLength = assistancePositiveBytes(value.byteLength), mediaType = assistanceMediaType(value.mediaType); if (actualBytes !== byteLength || !expected.mediaTypes.includes(mediaType)) throw new TypeError('Assistance workflow staged Blob disagrees with its slot'); return Object.freeze({ jobId: assistanceOperationId(value.jobId), workflowId: binding.workflowId, stageId: binding.stageId, slotId: binding.slotId, mediaType, byteLength, sha256: assistanceDigest(value.sha256), bytes: Blob.prototype.slice.call(value.bytes, 0, byteLength, mediaType) }); } function assistanceWorkflowReservationRequest(value) { nativeRecord(value, ['jobId', 'workflowId', 'stageId', 'slotId', 'maximumByteLength'], 'assistance workflow output reservation'); const binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'output', value.slotId); assistanceWorkflowOutputCustodySpec(binding.slotId); return Object.freeze({ jobId: assistanceOperationId(value.jobId), workflowId: binding.workflowId, stageId: binding.stageId, slotId: binding.slotId, maximumByteLength: assistancePositiveBytes(value.maximumByteLength) }); } function assistanceWorkflowProducerRequest(value) { nativeRecord(value, ['jobId', 'workflowId', 'stageId', 'slotId', 'producer'], 'assistance workflow producer binding'); const binding = assistanceWorkflowSlotBinding(value.workflowId, value.stageId, 'input', value.slotId), jobId = assistanceOperationId(value.jobId), producer = assistanceWorkflowCustody(value.producer), produced = assistanceWorkflowSlotBinding(binding.workflowId, producer.stageId, 'output', producer.slotId); if (producer.direction !== 'output' || producer.jobId !== jobId || producer.workflowId !== binding.workflowId || produced.stageIndex >= binding.stageIndex || producer.slotId !== binding.slotId) throw new TypeError('Assistance workflow producer is not one exact earlier output'); return Object.freeze({ jobId, workflowId: binding.workflowId, stageId: binding.stageId, slotId: binding.slotId, producer }); } function assistanceWorkflowProducerIpcRequest(value) { return Object.freeze({ jobId: value.jobId, workflowId: value.workflowId, stageId: value.stageId, slotId: value.slotId, producerStageId: value.producer.stageId, producerSlotId: value.producer.slotId, producerClaimId: value.producer.claimId }); } async function stageAssistanceWorkflowInput(value) { const request = assistanceWorkflowStageInputRequest(value), offer = assistanceInputOffer(await ipcRenderer.invoke(CHANNELS.assistanceWorkflowStage, { operation: 'prepare', jobId: request.jobId, workflowId: request.workflowId, stageId: request.stageId, slotId: request.slotId, mediaType: request.mediaType, byteLength: request.byteLength, sha256: request.sha256 }), request), channel = new MessageChannel(), port = channel.port2; port.start(); try { ipcRenderer.postMessage(CHANNELS.assistanceWorkflowInputPort, Object.freeze({ jobId: request.jobId, streamId: offer.streamId, reservation: offer.reservation }), [channel.port1]); let offset = 0, sequence = 0; while (offset < request.byteLength) { const end = Math.min(request.byteLength, offset + offer.reservation.maximumChunkBytes), bytes = new Uint8Array(await Blob.prototype.slice.call(request.bytes, offset, end).arrayBuffer()), ack = await nativeRenderInputPortReply(port, Object.freeze({ dataPlaneVersion: 1, type: 'chunk', streamId: offer.streamId, sequence, offset, bytes }), [bytes.buffer]); if (ack?.dataPlaneVersion !== 1 || ack.type !== 'ack' || ack.streamId !== offer.streamId || ack.sequence !== sequence || ack.receivedBytes !== end) throw new Error('Malformed assistance workflow input acknowledgement'); offset = end; sequence += 1; } port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'complete', streamId: offer.streamId, byteLength: request.byteLength, sha256: request.sha256 })); return assistanceWorkflowCustodyHandle(await ipcRenderer.invoke(CHANNELS.assistanceWorkflowStage, { operation: 'await', jobId: request.jobId, streamId: offer.streamId }), request, 'input'); } finally { port.close(); } }
function assistanceWorkflowOutputReadRequest(value) { nativeRecord(value, ['jobId', 'workflowId', 'claim'], 'assistance workflow output read request'); const jobId = assistanceOperationId(value.jobId), binding = assistanceWorkflowSlotBinding(value.workflowId, value.claim?.stageId, 'output', value.claim?.slotId), claim = value.claim; nativeRecord(claim, ['claimVersion', 'direction', 'claimId', 'jobId', 'stageId', 'slotId'], 'assistance workflow output claim'); if (claim.claimVersion !== 1 || claim.direction !== 'output' || assistanceOperationId(claim.jobId) !== jobId || claim.stageId !== binding.stageId || claim.slotId !== binding.slotId) throw new TypeError('Malformed assistance workflow output claim'); return Object.freeze({ jobId, workflowId: binding.workflowId, claim: Object.freeze({ claimVersion: 1, direction: 'output', claimId: assistanceOperationId(claim.claimId), jobId, stageId: binding.stageId, slotId: binding.slotId }) }); } function assistanceWorkflowOutputOffer(value, request) { nativeRecord(value, ['contractVersion', 'jobId', 'workflowId', 'workflowClaim', 'mediaType', 'binding'], 'assistance workflow output offer'); if (value.contractVersion !== 1 || value.jobId !== request.jobId || value.workflowId !== request.workflowId || JSON.stringify(value.workflowClaim) !== JSON.stringify(request.claim)) throw new TypeError('Malformed assistance workflow output offer'); const expected = assistanceWorkflowOutputCustodySpec(request.claim.slotId), mediaType = assistanceMediaType(value.mediaType), binding = assistanceOutputBinding(value.binding, value.binding); assistancePositiveBytes(binding.byteLength); if (!expected.mediaTypes.includes(mediaType)) throw new TypeError('Assistance workflow output media type disagrees with its slot'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, workflowId: request.workflowId, workflowClaim: request.claim, mediaType, binding }); } async function assistanceWorkflowBlobDigest(blob) { return Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256', await blob.arrayBuffer()))).map((byte) => byte.toString(16).padStart(2, '0')).join(''); } async function readAssistanceWorkflowOutput(value) { const request = assistanceWorkflowOutputReadRequest(value), offer = assistanceWorkflowOutputOffer(await ipcRenderer.invoke(CHANNELS.assistanceWorkflowReadOutput, request), request), channel = new MessageChannel(), port = channel.port2; port.start(); return new Promise((resolve, reject) => { let sequence = 0, offset = 0, settled = false; const chunks = [], finish = (error, blob) => { if (settled) return; settled = true; clearTimeout(timeout); port.onmessage = null; port.onmessageerror = null; port.close(); if (error) reject(error); else resolve(blob); }, timeout = setTimeout(() => finish(new Error('Assistance workflow output transfer timed out')), 30000); port.onmessageerror = () => finish(new Error('Assistance workflow output MessagePort failed')); port.onmessage = (event) => { try { const message = event.data; if (message?.dataPlaneVersion !== 1 || message.streamId !== offer.binding.streamId) throw new TypeError('Malformed assistance workflow output message'); if (message.type === 'cancel') return finish(new Error('Assistance workflow output transfer was cancelled')); if (message.type === 'chunk') { nativeRecord(message, ['dataPlaneVersion', 'type', 'streamId', 'sequence', 'offset', 'bytes'], 'assistance workflow output chunk'); const bytes = binary(message.bytes); if (message.sequence !== sequence || message.offset !== offset || bytes.byteLength < 1 || bytes.byteLength > offer.binding.maximumChunkBytes || offset + bytes.byteLength > offer.binding.byteLength) throw new TypeError('Malformed assistance workflow output chunk'); chunks.push(bytes); offset += bytes.byteLength; port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'ack', streamId: offer.binding.streamId, sequence, receivedBytes: offset })); sequence += 1; return; } nativeRecord(message, ['dataPlaneVersion', 'type', 'streamId', 'byteLength', 'sha256'], 'assistance workflow output completion'); if (message.type !== 'complete' || message.byteLength !== offer.binding.byteLength || message.sha256 !== offer.binding.sha256 || offset !== offer.binding.byteLength) throw new TypeError('Malformed assistance workflow output completion'); port.onmessage = null; const blob = new Blob(chunks, { type: offer.mediaType }); void assistanceWorkflowBlobDigest(blob).then((sha256) => { if (sha256 !== offer.binding.sha256) throw new TypeError('Assistance workflow output digest mismatch'); finish(null, blob); }).catch((error) => finish(error)); } catch (error) { finish(error); } }; try { ipcRenderer.postMessage(CHANNELS.assistanceWorkflowOutputPort, Object.freeze({ jobId: request.jobId, workflowId: request.workflowId, workflowClaim: request.claim, streamId: offer.binding.streamId, binding: offer.binding }), [channel.port1]); } catch (error) { finish(error); } }); }
function assistanceInputReservation(value, request) { nativeRecord(value, ['dataPlaneVersion', 'transport', 'streamId', 'direction', 'authentication', 'byteLength', 'maximumChunkBytes', 'maximumInFlightChunks'], 'assistance input reservation'); if (value.dataPlaneVersion !== 1 || value.transport !== 'message-port' || value.direction !== 'host-to-helper' || value.authentication !== 'trailer-sha256-v1' || value.byteLength !== request.byteLength || !Number.isSafeInteger(value.maximumChunkBytes) || value.maximumChunkBytes < 1 || value.maximumChunkBytes > 16 * 1024 * 1024 || value.maximumInFlightChunks !== 1) throw new TypeError('Malformed assistance input reservation'); return Object.freeze({ ...value, streamId: assistanceOperationId(value.streamId) }); } function assistanceInputOffer(value, request) { if (value?.contractVersion !== 1 || value?.jobId !== request.jobId) throw new TypeError('Malformed assistance input offer'); const reservation = assistanceInputReservation(value.reservation, request); if (value.streamId !== reservation.streamId) throw new TypeError('Assistance input offer stream mismatch'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, streamId: reservation.streamId, reservation }); }
function assistanceOutputBinding(value, claim) { nativeRecord(value, ['dataPlaneVersion', 'transport', 'streamId', 'direction', 'byteLength', 'sha256', 'maximumChunkBytes', 'maximumInFlightChunks'], 'assistance output binding'); if (value.dataPlaneVersion !== 1 || value.transport !== 'message-port' || value.direction !== 'helper-to-host' || value.byteLength !== claim.byteLength || value.sha256 !== claim.sha256 || !Number.isSafeInteger(value.maximumChunkBytes) || value.maximumChunkBytes < 1 || value.maximumChunkBytes > 16 * 1024 * 1024 || value.maximumInFlightChunks !== 1) throw new TypeError('Malformed assistance output binding'); return Object.freeze({ ...value, streamId: assistanceOperationId(value.streamId), sha256: assistanceDigest(value.sha256) }); }
function assistanceStageInputRequest(value) { nativeRecord(value, ['jobId', 'role', 'mediaType', 'sha256', 'bytes'], 'assistance staged input'); if (!ASSISTANCE_INPUT_ROLES.includes(value.role) || typeof Blob !== 'function' || !(value.bytes instanceof Blob)) throw new TypeError('Malformed assistance staged input'); const byteLength = Reflect.apply(Object.getOwnPropertyDescriptor(Blob.prototype, 'size').get, value.bytes, []); return Object.freeze({ jobId: assistanceOperationId(value.jobId), role: value.role, mediaType: assistanceMediaType(value.mediaType), byteLength: assistancePositiveBytes(byteLength), sha256: assistanceDigest(value.sha256), bytes: Blob.prototype.slice.call(value.bytes, 0, byteLength) }); }
async function stageAssistanceOperationInput(value) { const request = assistanceStageInputRequest(value), offer = assistanceInputOffer(await ipcRenderer.invoke(CHANNELS.assistanceOperationStage, { operation: 'prepare', jobId: request.jobId, role: request.role, mediaType: request.mediaType, byteLength: request.byteLength, sha256: request.sha256 }), request), channel = new MessageChannel(), port = channel.port2; port.start(); try { ipcRenderer.postMessage(CHANNELS.assistanceOperationInputPort, Object.freeze({ jobId: request.jobId, streamId: offer.streamId, reservation: offer.reservation }), [channel.port1]); let offset = 0, sequence = 0; while (offset < request.byteLength) { const end = Math.min(request.byteLength, offset + offer.reservation.maximumChunkBytes), bytes = new Uint8Array(await Blob.prototype.slice.call(request.bytes, offset, end).arrayBuffer()), ack = await nativeRenderInputPortReply(port, Object.freeze({ dataPlaneVersion: 1, type: 'chunk', streamId: offer.streamId, sequence, offset, bytes }), [bytes.buffer]); if (ack?.dataPlaneVersion !== 1 || ack.type !== 'ack' || ack.streamId !== offer.streamId || ack.sequence !== sequence || ack.receivedBytes !== end) throw new Error('Malformed assistance input acknowledgement'); offset = end; sequence += 1; } port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'complete', streamId: offer.streamId, byteLength: request.byteLength, sha256: request.sha256 })); return assistanceInputClaim(await ipcRenderer.invoke(CHANNELS.assistanceOperationStage, { operation: 'await', jobId: request.jobId, streamId: offer.streamId })); } finally { port.close(); } }
function assistanceOutputReadRequest(value) { nativeRecord(value, ['jobId', 'claim'], 'assistance output read request'); const jobId = assistanceOperationId(value.jobId), claim = assistanceOutputClaim(value.claim); if (claim.jobId !== jobId) throw new TypeError('Assistance output read job mismatch'); return Object.freeze({ jobId, claim }); } function assistanceOutputOffer(value, request) { if (value?.contractVersion !== 1 || value?.jobId !== request.jobId) throw new TypeError('Malformed assistance output offer'); return Object.freeze({ contractVersion: 1, jobId: request.jobId, binding: assistanceOutputBinding(value.binding, request.claim) }); }
async function readAssistanceOperationOutput(value) { const request = assistanceOutputReadRequest(value), offer = assistanceOutputOffer(await ipcRenderer.invoke(CHANNELS.assistanceOperationReadOutput, request), request), channel = new MessageChannel(), port = channel.port2; port.start(); return new Promise((resolve, reject) => { let sequence = 0, offset = 0, settled = false; const chunks = [], finish = (error, blob) => { if (settled) return; settled = true; clearTimeout(timeout); port.onmessage = null; port.onmessageerror = null; port.close(); if (error) reject(error); else resolve(blob); }, timeout = setTimeout(() => finish(new Error('Assistance output transfer timed out')), 30000); port.onmessageerror = () => finish(new Error('Assistance output MessagePort failed')); port.onmessage = (event) => { try { const message = event.data; if (message?.dataPlaneVersion !== 1 || message.streamId !== offer.binding.streamId) throw new TypeError('Malformed assistance output message'); if (message.type === 'cancel') return finish(new Error('Assistance output transfer was cancelled')); if (message.type === 'chunk') { nativeRecord(message, ['dataPlaneVersion', 'type', 'streamId', 'sequence', 'offset', 'bytes'], 'assistance output chunk'); const bytes = binary(message.bytes); if (message.sequence !== sequence || message.offset !== offset || bytes.byteLength < 1 || bytes.byteLength > offer.binding.maximumChunkBytes || offset + bytes.byteLength > offer.binding.byteLength) throw new TypeError('Malformed assistance output chunk'); chunks.push(bytes); offset += bytes.byteLength; port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'ack', streamId: offer.binding.streamId, sequence, receivedBytes: offset })); sequence += 1; return; } nativeRecord(message, ['dataPlaneVersion', 'type', 'streamId', 'byteLength', 'sha256'], 'assistance output completion'); if (message.type !== 'complete' || message.byteLength !== offer.binding.byteLength || message.sha256 !== offer.binding.sha256 || offset !== offer.binding.byteLength) throw new TypeError('Malformed assistance output completion'); finish(null, new Blob(chunks, { type: request.claim.mediaType })); } catch (error) { finish(error); } }; try { ipcRenderer.postMessage(CHANNELS.assistanceOperationOutputPort, Object.freeze({ jobId: request.jobId, streamId: offer.binding.streamId, binding: offer.binding }), [channel.port1]); } catch (error) { finish(error); } }); }
function subscribe(channel, listener, sanitize) {
	if (typeof listener !== 'function') throw new TypeError('Event listener must be a function');
	const handler = (_event, value) => listener(sanitize(value));
	ipcRenderer.on(channel, handler);
	return () => ipcRenderer.removeListener(channel, handler);
}
function soakDebugProcessMetrics(value) {
	nativeRecord(value, ['schemaVersion', 'workingSetBytes', 'processes'], 'soak-debug process metrics');
	if (value.schemaVersion !== 1 || !Array.isArray(value.processes) || value.processes.length > 128) throw new TypeError('Invalid soak-debug process metrics');
	const processTypes = ['Browser', 'Tab', 'Utility', 'Zygote', 'Sandbox helper', 'GPU', 'Pepper Plugin', 'Pepper Plugin Broker', 'Unknown'];
	const processes = value.processes.map((entry) => { nativeRecord(entry, ['pid', 'type', 'workingSetBytes'], 'soak-debug process'); const pid = safeInteger(entry.pid), workingSetBytes = safeInteger(entry.workingSetBytes); if (pid < 1 || !processTypes.includes(entry.type)) throw new TypeError('Invalid soak-debug process metric'); return Object.freeze({ pid, type: entry.type, workingSetBytes }); });
	const workingSetBytes = safeInteger(value.workingSetBytes); if (processes.some((entry, index) => index > 0 && entry.pid <= processes[index - 1].pid) || processes.reduce((sum, entry) => sum + entry.workingSetBytes, 0) !== workingSetBytes) throw new TypeError('Invalid soak-debug process metric total');
	return Object.freeze({ schemaVersion: 1, workingSetBytes, processes: Object.freeze(processes) });
}
function sanitizeReadDescriptor(value) {
	const id = opaqueId(value?.id, 64);
	const readProfile = readDescriptorProfile(value?.readProfile);
	const name = readDescriptorName(value?.name);
	const mimeType = readDescriptorMimeType(value?.mimeType);
	assertReadDescriptorProfile(readProfile, name, mimeType);
	return Object.freeze({
		id,
		readProfile,
		url: trustedCapabilityUrl(value?.url, { id, readProfile, name }),
		name,
		size: readDescriptorSize(value?.size, readProfile),
		mimeType,
		lastModified: safeInteger(value?.lastModified),
	});
}
function sanitizeReadDescriptors(values) {
	if (!Array.isArray(values)) throw new TypeError('Expected read descriptors');
	return Object.freeze(values.map(sanitizeReadDescriptor));
}
function nullableLinkedLocator(value, kind) {
	if (value === null) return null;
	const name = readDescriptorName(value?.name);
	const mimeType = linkedOriginalMimeType(kind, value?.mimeType, name);
	const size = readDescriptorSize(value?.size, READ_PROFILE_MATERIALIZED_V1);
	if (size === 0) throw new RangeError(`Linked-${kind} size must be positive`);
	return Object.freeze({
		locatorId: opaqueId(value?.locatorId, 64), locatorRevision: linkedLocatorRevision(value?.locatorRevision),
		name, size, mimeType,
		lastModified: safeInteger(value?.lastModified),
	});
}
function linkedVideoLoadRequest(value) {
	return linkedOriginalLoadRequest(value, 'video', 'playback');
}
function linkedAudioLoadRequest(value) {
	return linkedOriginalLoadRequest(value, 'audio', 'range');
}
function linkedOriginalLoadRequest(value, kind, mode) {
	if (!value || typeof value !== 'object' || Array.isArray(value)) throw new TypeError(`A linked-${kind} load request is required`);
	const fields = ['locatorId', 'expectedRevision', mode]; const keys = Reflect.ownKeys(value);
	if (keys.length !== fields.length || keys.some((key) => !fields.includes(key)) || fields.some((field) => {
		const descriptor = Object.getOwnPropertyDescriptor(value, field); return !descriptor?.enumerable || !Object.hasOwn(descriptor, 'value');
	})) throw new TypeError(`A linked-${kind} load request contains an unsupported field`);
	if (value[mode] !== true && value[mode] !== false) throw new TypeError(`Linked-${kind} load mode must be a boolean`);
	const expectedRevision = value.expectedRevision === null ? null : linkedLocatorRevision(value.expectedRevision);
	if (value[mode] && expectedRevision === null) throw new TypeError(`Linked-${kind} ${mode} requires an exact locator revision`);
	return Object.freeze({ locatorId: opaqueId(value.locatorId, 64), expectedRevision, [mode]: value[mode] });
}
function linkedVideoReferences(value) {
	if (!Array.isArray(value) || value.length > 128) throw new RangeError('Linked-video reconciliation reference count exceeds its limit');
	const identifiers = new Set();
	return Object.freeze(value.map((reference) => {
		const fields = ['locatorId', 'locatorRevision'];
		const keys = reference && typeof reference === 'object' && !Array.isArray(reference) ? Reflect.ownKeys(reference) : [];
		if (keys.length !== fields.length || keys.some((key) => !fields.includes(key)) || fields.some((field) => {
			const descriptor = Object.getOwnPropertyDescriptor(reference, field); return !descriptor || !descriptor.enumerable || !Object.hasOwn(descriptor, 'value');
		})) throw new TypeError('Linked-video reconciliation reference contains an unsupported field');
		const locatorId = opaqueId(reference.locatorId, 64);
		if (identifiers.has(locatorId)) throw new Error('Linked-video reconciliation contains a duplicate identifier');
		identifiers.add(locatorId);
		return Object.freeze({ locatorId, locatorRevision: linkedLocatorRevision(reference.locatorRevision) });
	}));
}
function linkedOriginalReferences(value) {
	if (!Array.isArray(value) || value.length > 128) throw new RangeError('Linked-original reconciliation reference count exceeds its limit');
	const identifiers = new Set();
	return Object.freeze(value.map((reference) => {
		const fields = ['kind', 'locatorId', 'locatorRevision'];
		const keys = reference && typeof reference === 'object' && !Array.isArray(reference) ? Reflect.ownKeys(reference) : [];
		if (keys.length !== fields.length || keys.some((key) => !fields.includes(key)) || fields.some((field) => {
			const descriptor = Object.getOwnPropertyDescriptor(reference, field); return !descriptor?.enumerable || !Object.hasOwn(descriptor, 'value');
		})) throw new TypeError('Linked-original reconciliation reference contains an unsupported field');
		const locatorId = opaqueId(reference.locatorId, 64);
		if (identifiers.has(locatorId)) throw new Error('Linked-original reconciliation contains a duplicate identifier');
		identifiers.add(locatorId);
		return Object.freeze({ kind: linkedMediaKind(reference.kind), locatorId, locatorRevision: linkedLocatorRevision(reference.locatorRevision) });
	}));
}
async function nullableLoadedLinkedLocator(value, kind, playback) {
	if (value === null) return null;
	try {
		const expectedProfile = playback
			? kind === 'audio' ? READ_PROFILE_LINKED_AUDIO_RANGE_V1 : READ_PROFILE_LINKED_VIDEO_RANGE_V1
			: READ_PROFILE_MATERIALIZED_V1;
		const descriptor = sanitizeReadDescriptor(value?.descriptor);
		if (descriptor.readProfile !== expectedProfile || descriptor.size === 0) throw new TypeError(`Linked-${kind} reads require a positive ${expectedProfile} descriptor`);
		linkedOriginalMimeType(kind, descriptor.mimeType, descriptor.name);
		return Object.freeze({ locatorRevision: linkedLocatorRevision(value?.locatorRevision), descriptor });
	} catch (cause) {
		let id;
		try { id = opaqueId(value?.descriptor?.id, 64); } catch { throw cause; }
		try {
			if (await ipcRenderer.invoke(CHANNELS.releaseRead, id) !== true) throw new Error('Linked-video read cleanup was not acknowledged', { cause });
		} catch (cleanupError) {
			throw new AggregateError([cause, cleanupError], 'Linked-video read validation and cleanup both failed', { cause: cleanupError });
		}
		throw cause;
	}
}
function linkedLocatorRevision(value) { try { return opaqueId(value, 64); } catch { throw new TypeError('Invalid linked-original locator revision'); } }
function linkedOriginalMimeType(kind, value, name) {
	const mimeType = readDescriptorMimeType(value);
	if (kind === 'video' && /^video\/[a-z0-9][a-z0-9!#$&^_.+-]*$/u.test(mimeType)) return mimeType;
	if (kind === 'audio' && ((/\.wav$/iu.test(name) && mimeType === 'audio/wav')
		|| (/\.rf64$/iu.test(name) && mimeType === 'audio/rf64') || (/\.aiff?$/iu.test(name) && mimeType === 'audio/aiff'))) return mimeType;
	throw new TypeError(`Invalid linked-${kind} classic AIFF, WAV, or video MIME type`);
}
function linkedMediaKind(value) {
	if (value !== 'audio' && value !== 'video') throw new TypeError('Invalid linked-original media kind');
	return value;
}
function trustedCapabilityUrl(value, { id, readProfile, name }) {
	let url;
	try { url = new URL(String(value || '')); } catch { throw new TypeError('Invalid read capability URL'); }
	const expectedPath = `/_desktop/read/${readProfile}/${id}/${encodeURIComponent(name)}`;
	if (!['soundscaper-app:', 'framescaper-app:'].includes(url.protocol)
		|| url.hostname !== 'bundle' || url.port || url.username || url.password
		|| url.search || url.hash || url.pathname !== expectedPath) {
		throw new TypeError('Invalid read capability URL');
	}
	return url.href;
}
function opaqueId(value, length) {
	const id = String(value || '');
	if (id.length !== length || !/^[a-f0-9]+$/u.test(id)) throw new TypeError('Invalid opaque identifier');
	return id;
}
function externalFfmpegStatus(value) { const controlled = (entry) => [...entry].some((character) => { const code = character.charCodeAt(0); return code < 32 || code === 127; }); nativeRecord(value, ['state', 'location', 'version', 'detail', 'canInstall', 'canBrowse', 'canClear'], 'external FFmpeg status'); if (!['unconfigured', 'probing', 'ready', 'unsupported', 'quarantined', 'unavailable', 'installing', 'error'].includes(value.state) || !(value.location === null || typeof value.location === 'string' && value.location.length <= 4096 && !controlled(value.location)) || !(value.version === null || typeof value.version === 'string' && value.version.length <= 256 && !controlled(value.version)) || typeof value.detail !== 'string' || value.detail.length > 2048 || controlled(value.detail) || typeof value.canInstall !== 'boolean' || typeof value.canBrowse !== 'boolean' || typeof value.canClear !== 'boolean') throw new TypeError('Invalid external FFmpeg status'); return Object.freeze({ state: value.state, location: value.location, version: value.version, detail: value.detail, canInstall: value.canInstall, canBrowse: value.canBrowse, canClear: value.canClear }); } function text(value, maxLength) {
	return [...String(value || '')].filter((character) => character.charCodeAt(0) > 31).join('').slice(0, maxLength);
}
function textEditCommand(value) {
	const command = String(value || '');
	if (!['undo', 'redo', 'cut', 'copy', 'paste', 'selectAll'].includes(command)) throw new TypeError('Unsupported text edit command');
	return command;
}
function safeInteger(value) {
	const number = Number(value);
	if (!Number.isSafeInteger(number) || number < 0) throw new RangeError('Expected a non-negative safe integer');
	return number;
}
function readDescriptorProfile(value) {
	const profile = String(value || '');
	if (![READ_PROFILE_LINKED_AUDIO_RANGE_V1, READ_PROFILE_LINKED_VIDEO_RANGE_V1, READ_PROFILE_MATERIALIZED_V1, READ_PROFILE_SCAPE_RANGE_V1].includes(profile)) {
		throw new TypeError('Invalid read descriptor profile');
	}
	return profile;
}
function readDescriptorName(value) {
	const name = text(value, 255);
	if (!name || name === '.' || name === '..' || name.includes('/') || name.includes('\\')) {
		throw new TypeError('Invalid read descriptor name');
	}
	return name;
}
function readDescriptorMimeType(value) {
	const mimeType = text(value, 128);
	if (!mimeType) throw new TypeError('Invalid read descriptor MIME type');
	return mimeType;
}
function assertReadDescriptorProfile(readProfile, name, mimeType) {
	const hasScapeName = /\.(?:sscape|fscape|liscape|scape)$/iu.test(name);
	const hasScapeMime = mimeType === SCAPE_PROJECT_MIME_TYPE;
	if (readProfile === READ_PROFILE_SCAPE_RANGE_V1) {
		if (!hasScapeName || !hasScapeMime) throw new TypeError('Invalid Scape range read descriptor');
	} else if (hasScapeName || hasScapeMime) {
		throw new TypeError('Invalid materialized read descriptor profile');
	}
}
function readDescriptorSize(value, readProfile) {
	const size = safeInteger(value);
	const maximum = readProfile === READ_PROFILE_SCAPE_RANGE_V1
		? MAX_SCAPE_RANGE_READ_DESCRIPTOR_BYTES
		: MAX_MATERIALIZED_READ_DESCRIPTOR_BYTES;
	if (size > maximum) throw new RangeError('Read descriptor is too large for its profile');
	return size;
}
function saveDeclaration(options) {
	const targetId = opaqueId(options?.targetId, 48);
	const exactSize = options?.size !== undefined;
	if (exactSize === (options?.maximumSize !== undefined)) {
		throw new RangeError('Expected exactly one exact size or admitted maximum');
	}
	const declaration = exactSize
		? { targetId, size: saveSize(options.size) }
		: { targetId, maximumSize: saveSize(options.maximumSize) };
	if (options?.finalPrefixByteLength === undefined) return declaration;
	if (options.finalPrefixByteLength !== FINAL_PREFIX_BYTES) throw new RangeError('Final prefix must be exactly 32 bytes');
	if (!exactSize) throw new RangeError('A final prefix requires an exact-size save');
	if (declaration.size < FINAL_PREFIX_BYTES) throw new RangeError('A final-prefix save must be at least 32 bytes');
	return { ...declaration, finalPrefixByteLength: FINAL_PREFIX_BYTES };
}
function saveSize(value) {
	const size = safeInteger(value);
	if (size > MAX_DESKTOP_SAVE_BYTES) throw new RangeError('Save size is too large');
	return size;
}
function positiveSafeInteger(value) { const number = safeInteger(value); if (number === 0) throw new RangeError('Expected a positive safe integer'); return number; } function nativeAudioAvailability(value) { const payload = value?.payload; return Object.freeze({ enabled: value?.enabled === true, quarantined: value?.quarantined === true, payload: Object.freeze({ status: payload?.status === 'available' ? 'available' : 'unavailable', reason: payload?.reason == null ? null : text(payload.reason, 64), detail: text(payload?.detail ?? '', 512) }), backends: Object.freeze(bounded(value?.backends, 16).map((backend) => text(backend, 32))), routePreference: value?.routePreference == null ? null : nativeAudioSessionOpenRequest(value.routePreference) }); }
function nativeAudioInventory(value) { const inventory = value?.inventory; if (value?.status !== 'described') return Object.freeze({ status: 'failed', code: text(value?.code ?? 'helper-failed', 32), message: text(value?.message ?? '', 512) }); return Object.freeze({ status: 'described', inventory: Object.freeze({ backend: text(inventory?.backend, 32), status: text(inventory?.status, 32), detail: text(inventory?.detail ?? '', 512), devices: Object.freeze(bounded(inventory?.devices, 128).map((device) => Object.freeze({ handle: text(device?.handle, 256), label: text(device?.label, 256), direction: text(device?.direction, 16), channelCount: device?.channelCount == null ? undefined : nativeBoundedInteger(device.channelCount, 1, 32, 'device channel count'), isDefault: device?.isDefault === true }))) }) }); } function bounded(value, maximum) { return Array.isArray(value) ? value.slice(0, maximum) : []; }
function nativePluginStatus(value, depth = 0) { if (typeof value === 'string') return text(value, 1024); if (typeof value === 'number' || typeof value === 'boolean' || value === null) return value; if (depth >= 8) return null; if (Array.isArray(value)) return Object.freeze(bounded(value, 512).map((entry) => nativePluginStatus(entry, depth + 1))); if (!value || typeof value !== 'object') return null; const output = {}; for (const key of Object.keys(value).slice(0, 64)) { if (PLUGIN_PATH_KEYS.has(key)) continue; output[text(key, 64)] = nativePluginStatus(value[key], depth + 1); } return Object.freeze(output); }
function helperProbeCompletion(value) { if (value?.status === 'probed') { const timingAsset = binary(value.timingAsset);
		if (timingAsset.byteLength < 32 || timingAsset.byteLength > 16_000_032) throw new RangeError('Helper probe timing asset is out of range'); const characteristics = JSON.stringify(value.characteristics ?? null);
		if (utf8Bytes(characteristics, 65_536) > 65_536) throw new RangeError('Helper probe characteristics exceed their bound');
		return Object.freeze({ status: 'probed', timingAsset, nominalRate: Object.freeze({ num: positiveSafeInteger(value.nominalRate?.num), den: positiveSafeInteger(value.nominalRate?.den) }), characteristics: JSON.parse(characteristics) }); }
	if (value?.status === 'failed') return Object.freeze({ status: 'failed', code: text(value.code, 64), message: text(value.message, 2048) }); throw new TypeError('Desktop returned an unsupported helper probe completion'); }
function nativeAudioEnabled(value) { return strictBoolean(value, 'Desktop native-audio setting result must be a boolean'); }
function nativeAudioSessionOpenRequest(value) { nativeRecord(value, ['candidates', 'direction', 'mode', 'sampleRate', 'periodFrames', 'channelCount'], 'native audio open request'); const candidates = nativeArray(value.candidates, 4, 'audio candidates').map((candidate) => { nativeRecord(candidate, ['backend', 'deviceHandle'], 'audio candidate'); if (!['coreaudio', 'wasapi', 'asio', 'pipewire', 'alsa', 'jack'].includes(candidate.backend) || typeof candidate.deviceHandle !== 'string' || !candidate.deviceHandle.length || candidate.deviceHandle.length > 1024 || /[\0/\\]/u.test(candidate.deviceHandle)) throw new TypeError('Invalid native audio candidate'); return Object.freeze({ backend: candidate.backend, deviceHandle: candidate.deviceHandle }); }); if (!candidates.length || !['input', 'output', 'duplex'].includes(value.direction) || !['shared', 'exclusive'].includes(value.mode)) throw new TypeError('Invalid native audio open mode'); return Object.freeze({ candidates: Object.freeze(candidates), direction: value.direction, mode: value.mode, sampleRate: nativeBoundedInteger(value.sampleRate, 8000, 768000, 'sample rate'), periodFrames: nativeBoundedInteger(value.periodFrames, 1, 16384, 'period frames'), channelCount: nativeBoundedInteger(value.channelCount, 1, 32, 'channel count') }); } function nativeAudioSessionIdRequest(value, binding) { nativeRecord(value, binding ? ['sessionId', 'queueCapacity'] : ['sessionId'], 'native audio session request'); const request = { sessionId: nativeRuntimeId(value.sessionId, 'audio session') }; if (binding) request.queueCapacity = nativeBoundedInteger(value.queueCapacity, 8, 8, 'queue capacity'); return Object.freeze(request); } function nativeAudioSessionCalibrationRequest(value) { nativeRecord(value, ['sessionId', 'calibrationFrames'], 'native audio calibration request'); return Object.freeze({ sessionId: nativeRuntimeId(value.sessionId, 'audio session'), calibrationFrames: nativeBoundedInteger(value.calibrationFrames, 0, 1048576, 'calibration offset') }); } function nativeAudioSessionTransferRequest(value) { nativeRecord(value, ['sessionId', 'framesTransferred', 'lostFrames'], 'native audio transfer report'); return Object.freeze({ sessionId: nativeRuntimeId(value.sessionId, 'audio session'), framesTransferred: nativeBoundedInteger(value.framesTransferred, 0, Number.MAX_SAFE_INTEGER, 'transferred frames'), lostFrames: nativeBoundedInteger(value.lostFrames, 0, Number.MAX_SAFE_INTEGER, 'lost frames') }); } function nativeAudioSessionLossRequest(value) { nativeRecord(value, ['sessionId', 'reason'], 'native audio loss report'); if (!['device-loss', 'device-fault', 'short-transfer', 'output-overrun', 'pool-violation', 'peer-loss', 'malformed-message'].includes(value.reason)) throw new TypeError('Invalid native audio loss reason'); return Object.freeze({ sessionId: nativeRuntimeId(value.sessionId, 'audio session'), reason: value.reason }); }
function nativePluginReviewRequest(value) { nativeRecord(value, ['installationId', 'action'], 'native plug-in installation review'); if (!['allow', 'select', 'revoke'].includes(value.action)) throw new TypeError('Invalid native plug-in installation action'); return Object.freeze({ installationId: nativeInstallationId(value.installationId), action: value.action }); } function nativeInstallationId(value) { if (typeof value !== 'string' || !/^i[a-f0-9]{15}$/u.test(value)) throw new TypeError('Invalid native plug-in installation id'); return value; } function nativePluginInstantiationRequest(value) { nativeRecord(value, ['installationId', 'instanceId', 'sampleRate'], 'native plug-in instantiate request'); return Object.freeze({ installationId: nativeInstallationId(value.installationId), instanceId: value.instanceId === null ? null : nativeRuntimeId(value.instanceId, 'plug-in instance'), sampleRate: nativeBoundedInteger(value.sampleRate, 8000, 768000, 'plug-in sample rate') }); } function nativePluginInstanceRequest(value) { nativeRecord(value, ['instanceId'], 'native plug-in instance request'); return Object.freeze({ instanceId: nativeRuntimeId(value.instanceId, 'plug-in instance') }); } function nativePluginBypassRequest(value) { nativeRecord(value, ['instanceId', 'bypassed'], 'native plug-in bypass request'); if (typeof value.bypassed !== 'boolean') throw new TypeError('Invalid native plug-in bypass flag'); return Object.freeze({ instanceId: nativeRuntimeId(value.instanceId, 'plug-in instance'), bypassed: value.bypassed }); }
function nativePluginStatePersistRequest(value) { nativeRecord(value, ['instanceId', 'generation', 'bytes', 'authentication'], 'native plug-in state persist request'); const bytes = binary(value.bytes); if (bytes.byteLength > 16 * 1024 * 1024) throw new RangeError('Native plug-in state exceeds 16 MiB'); const authentication = value.authentication; nativeRecord(authentication, ['requestId', 'byteLength', 'sha256', 'mac'], 'native plug-in state authentication'); return Object.freeze({ instanceId: nativeRuntimeId(value.instanceId, 'plug-in instance'), generation: nativeBoundedInteger(value.generation, 0, 0xffffffff, 'state generation'), bytes, authentication: Object.freeze({ requestId: nativeRuntimeId(authentication.requestId, 'state request'), byteLength: nativeBoundedInteger(authentication.byteLength, 0, 16 * 1024 * 1024, 'state proof byte length'), sha256: nativeDigest(authentication.sha256, 'state proof'), mac: nativeDigest(authentication.mac, 'state proof authentication') }) }); } function nativePluginStateRestoreRequest(value) { nativeRecord(value, ['instanceId', 'generation', 'stateBody'], 'native plug-in state restore request'); const body = value.stateBody; nativeRecord(body, ['kind', 'bodyId', 'byteLength', 'sha256'], 'native plug-in state body'); if (body.kind !== 'native-plugin-state' || typeof body.sha256 !== 'string' || !SHA256.test(body.sha256) || body.bodyId !== `native-plugin-state:${body.sha256}`) throw new TypeError('Invalid native plug-in state body identity'); return Object.freeze({ instanceId: nativeRuntimeId(value.instanceId, 'plug-in instance'), generation: nativeBoundedInteger(value.generation, 0, 0xffffffff, 'state generation'), stateBody: Object.freeze({ kind: body.kind, bodyId: body.bodyId, byteLength: nativeBoundedInteger(body.byteLength, 0, 16 * 1024 * 1024, 'state byte length'), sha256: body.sha256 }) }); } function nativePluginStateRestoreProjection(value) { nativeRecord(value, ['projectState', 'bytes'], 'native plug-in state restoration'); const bytes = binary(value.bytes); if (bytes.byteLength > 16 * 1024 * 1024) throw new RangeError('Restored native plug-in state exceeds 16 MiB'); return Object.freeze({ projectState: nativePluginStatus(value.projectState), bytes }); } function nativePluginVendorCloseRequest(value) { nativeRecord(value, ['instanceId', 'windowHandleId'], 'native plug-in vendor window close request'); return Object.freeze({ instanceId: nativeRuntimeId(value.instanceId, 'plug-in instance'), windowHandleId: nativeRuntimeId(value.windowHandleId, 'vendor window') }); } function nativeRuntimeId(value, label) { if (typeof value !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/u.test(value)) throw new TypeError(`Invalid native ${label} id`); return value; } function nativeBoundedInteger(value, minimum, maximum, label) { if (!Number.isSafeInteger(value) || value < minimum || value > maximum) throw new RangeError(`Invalid native ${label}`); return value; }
function nativeImageSequenceDecodeRequest(value) { const identity = nativeProjectIdentity(value, 'image-sequence decode', 'framescaper'); nativeRecord(value, ['requestId', 'schemaFamily', 'schemaVersion', 'projectId', 'projectRevision', 'sourceId'], 'image-sequence decode request'); const projectId = nativeImageSequenceProjectId(value.projectId), sourceId = nativeImageSequenceProjectId(value.sourceId); if (!Number.isSafeInteger(value.projectRevision) || value.projectRevision < 0) throw new RangeError('Invalid image-sequence project revision'); return Object.freeze({ requestId: nativeImageSequenceId(value.requestId), ...identity, projectId, projectRevision: value.projectRevision, sourceId }); } function nativeImageSequenceDecodeIdRequest(value, field) { nativeRecord(value, [field], 'image-sequence decode identity'); return Object.freeze({ [field]: nativeImageSequenceId(value[field]) }); } function nativeImageSequenceDecodeReadRequest(value) { nativeRecord(value, ['claimId', 'offset', 'length'], 'image-sequence decode read'); if (!Number.isSafeInteger(value.offset) || value.offset < 0 || !Number.isSafeInteger(value.length) || value.length < 1 || value.length > 16 * 1024 * 1024) throw new RangeError('Invalid decoded image-sequence range'); return Object.freeze({ claimId: nativeImageSequenceId(value.claimId), offset: value.offset, length: value.length }); } function nativeImageSequenceDecodeClaim(value) { nativeRecord(value, ['claimId', 'sourceId', 'byteLength', 'sha256', 'frameCount', 'width', 'height', 'frameRate'], 'image-sequence decode claim'); nativeRecord(value.frameRate, ['num', 'den'], 'image-sequence decode rate'); return Object.freeze({ claimId: nativeImageSequenceId(value.claimId), sourceId: nativeImageSequenceProjectId(value.sourceId), byteLength: positiveSafeInteger(value.byteLength), sha256: nativeDigest(value.sha256, 'decoded image sequence'), frameCount: positiveSafeInteger(value.frameCount), width: positiveSafeInteger(value.width), height: positiveSafeInteger(value.height), frameRate: Object.freeze({ num: positiveSafeInteger(value.frameRate.num), den: positiveSafeInteger(value.frameRate.den) }) }); } function nativeImageSequenceProjectId(value) { if (typeof value !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/u.test(value)) throw new TypeError('Invalid image-sequence project identity'); return value; }
function windowAction(value) { const action = text(value, 32); if (!['minimize', 'toggle-maximize', 'toggle-fullscreen', 'quit', 'reload', 'toggle-dev-tools'].includes(action)) throw new TypeError('Unsupported window action'); return action; }
function windowState(value) { return Object.freeze({ maximized: value?.maximized === true, fullscreen: value?.fullscreen === true }); }
const NATIVE_CAPABILITY_ROWS = Object.freeze({ 'queue/persistent-render-queue': true, 'watch/watch-folders': true, 'codec/encode-mov-prores-proxy': true, 'operation/image-sequence-import': true, 'display/external-display': true, 'ofx/isolated-host': true });
function nativeCapabilitySnapshot(value) { nativeRecord(value, ['snapshotVersion', 'masterEnabled', 'buildFingerprint', 'entries'], 'native capability snapshot'); if (value.snapshotVersion !== 1 || typeof value.masterEnabled !== 'boolean' || (value.buildFingerprint !== null && !SHA256.test(value.buildFingerprint)) || !Array.isArray(value.entries) || value.entries.length !== 6) throw new TypeError('Invalid Framescaper native capability snapshot'); const seen = new Set(); const entries = value.entries.map((entry) => { nativeRecord(entry, ['domain', 'id', 'state', 'reason', 'userEnabled', 'buildFingerprint', 'detail'], 'native capability entry'); const key = `${entry.domain}/${entry.id}`; if (!NATIVE_CAPABILITY_ROWS[key] || seen.has(key) || !['disabled', 'unavailable', 'available', 'degraded', 'quarantined'].includes(entry.state) || !['quarantined-after-repeated-failure', 'master-switch-off', 'build-does-not-support', 'driver-probe-failed', 'self-test-failed', 'degraded-after-failure', 'ready'].includes(entry.reason) || typeof entry.userEnabled !== 'boolean' || (entry.buildFingerprint !== null && !SHA256.test(entry.buildFingerprint)) || (entry.detail !== null && (typeof entry.detail !== 'string' || !entry.detail.length || entry.detail.length > 512))) throw new TypeError('Invalid Framescaper native capability entry'); seen.add(key); return Object.freeze({ ...entry }); }); return Object.freeze({ snapshotVersion: 1, masterEnabled: value.masterEnabled, buildFingerprint: value.buildFingerprint, entries: Object.freeze(entries) }); }
function nativeServicesSnapshot(value) { if (value?.snapshotVersion !== 1 || typeof value?.runtimeAvailable !== 'boolean' || typeof value?.nativeMediaEnabled !== 'boolean') throw new TypeError('Invalid Framescaper native-services snapshot');
	return Object.freeze({ snapshotVersion: 1, runtimeAvailable: value.runtimeAvailable, nativeMediaEnabled: value.nativeMediaEnabled, queue: nativeQueueProjections(value.queue), roots: Object.freeze(nativeArray(value.roots, 1024, 'roots').map(nativeRootProjection)), watchRules: Object.freeze(nativeArray(value.watchRules, 1024, 'watch rules').map(nativeWatchProjection)) }); }
function nativeQueueProjections(value) { return Object.freeze(nativeArray(value, 100000, 'queue').map(nativeQueueProjection)); }
function nativeQueueProjection(value) { const identity = nativeProjectIdentity(value, 'native queue projection', 'framescaper'); nativeRecord(value, ['jobId', 'schemaFamily', 'schemaVersion', 'taskKind', 'projectId', 'relativeDestination', 'state', 'position', 'progress', 'attempt', 'lastFailureCode'], 'native queue projection'); const destination = nativeRelativeDestination(value.relativeDestination); const taskKind = text(value.taskKind, 32); const state = text(value.state, 32);
	if (!['encoded-export', 'image-sequence-export', 'proxy-generation'].includes(taskKind) || !['queued', 'running', 'paused', 'blocked', 'needs-authorization', 'completed', 'failed', 'cancelled'].includes(state)) throw new TypeError('Invalid Framescaper native queue projection');
	const progress = value?.progress === null ? null : Number(value?.progress); if (progress !== null && (!Number.isFinite(progress) || progress < 0 || progress > 1)) throw new TypeError('Invalid Framescaper native queue progress');
	return Object.freeze({ ...identity, jobId: opaqueId(value.jobId, 40), taskKind, projectId: text(value.projectId, 4096), relativeDestination: destination, state, position: safeInteger(value.position), progress, attempt: safeInteger(value.attempt), lastFailureCode: value.lastFailureCode === null ? null : text(value.lastFailureCode, 4096) }); }
function nativeRootProjection(value) { return Object.freeze({ grantId: nativeIdentifier(value?.grantId, 'grant id'), displayName: text(value?.displayName, 4096), revoked: value?.revoked === true }); }
function nativeWatchProjection(value) { const identity = nativeProjectIdentity(value, 'watch projection', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'ruleId', 'grantId', 'projectId', 'binId', 'extensions', 'importMode', 'generateProxies', 'enabled'], 'watch projection'); const mode = value.importMode; if (mode !== 'link' && mode !== 'copy' || typeof value.generateProxies !== 'boolean' || typeof value.enabled !== 'boolean') throw new TypeError('Invalid Framescaper watch projection');
	return Object.freeze({ ...identity, ruleId: nativeIdentifier(value.ruleId, 'watch rule id'), grantId: nativeIdentifier(value.grantId, 'grant id'), projectId: text(value.projectId, 4096), binId: value.binId === null ? null : text(value.binId, 128), extensions: Object.freeze(nativeArray(value.extensions, 32, 'watch extensions').map((item) => text(item, 32))), importMode: mode, generateProxies: value.generateProxies, enabled: value.enabled }); }
function nativeIdentifier(value, label, length = null) { if (typeof value !== 'string' || (length === null ? value.length < 16 || value.length > 64 : value.length !== length) || !/^[a-f0-9]+$/u.test(value)) throw new TypeError(`Invalid Framescaper native ${label}`); return value; }
function nativeDigest(value, label) { if (typeof value !== 'string' || !SHA256.test(value)) throw new TypeError(`Invalid Framescaper native ${label} digest`); return value; }
function nativeLifecycleIdRequest(value, field) { nativeRecord(value, [field], `${field} request`); return Object.freeze({ [field]: nativeIdentifier(value[field], field, field === 'jobId' || field === 'stageId' ? 40 : null) }); }
function nativeWatchCreateRequest(value) { const identity = nativeProjectIdentity(value, 'watch create request', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'grantId', 'projectId', 'binId', 'extensions', 'importMode', 'generateProxies'], 'watch create request'); const extensions = Object.freeze(nativeArray(value.extensions, 32, 'watch extensions').map((item) => text(item, 32))); if (!extensions.length || !text(value.projectId, 4096) || (value.binId !== null && !text(value.binId, 128)) || !['link', 'copy'].includes(value.importMode) || typeof value.generateProxies !== 'boolean') throw new TypeError('Invalid Framescaper watch create request'); return Object.freeze({ ...identity, grantId: nativeIdentifier(value.grantId, 'grant id'), projectId: text(value.projectId, 4096), binId: value.binId === null ? null : text(value.binId, 128), extensions, importMode: value.importMode, generateProxies: value.generateProxies }); }
function nativeWatchEnabledRequest(value) { nativeRecord(value, ['ruleId', 'enabled'], 'watch enabled request'); if (typeof value.enabled !== 'boolean') throw new TypeError('Invalid Framescaper watch enabled request'); return Object.freeze({ ruleId: nativeIdentifier(value.ruleId, 'watch rule id'), enabled: value.enabled }); } function nativeWatchImportClaimRequest(value) { const identity = nativeProjectIdentity(value, 'watch import claim request', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'projectId', 'projectRevision'], 'watch import claim request'); return Object.freeze({ ...identity, projectId: nativeBoundedText(value.projectId, 128, 'watch project id'), projectRevision: safeInteger(value.projectRevision) }); } function nativeWatchImportCompletionRequest(value) { const identity = nativeProjectIdentity(value, 'watch import completion request', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'claimId', 'projectId', 'binId', 'sourceId', 'contentSha256', 'expectedProjectRevision', 'committedProjectRevision', 'success'], 'watch import completion request'); if (value.binId !== 'project-bin' || typeof value.success !== 'boolean' || (value.success && value.sourceId === null)) throw new TypeError('Invalid watch import completion result'); return Object.freeze({ ...identity, claimId: nativeIdentifier(value.claimId, 'watch claim id'), projectId: nativeBoundedText(value.projectId, 128, 'watch project id'), binId: 'project-bin', sourceId: value.sourceId === null ? null : nativeRuntimeId(value.sourceId, 'watch source'), contentSha256: nativeDigest(value.contentSha256, 'watch content'), expectedProjectRevision: safeInteger(value.expectedProjectRevision), committedProjectRevision: safeInteger(value.committedProjectRevision), success: value.success }); } function nativeWatchImportClaim(value) { const identity = nativeProjectIdentity(value, 'watch import claim', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'claimId', 'projectId', 'projectRevision', 'binId', 'generateProxies', 'existingSourceId', 'importMode', 'locatorId', 'locatorRevision', 'name', 'size', 'mimeType', 'lastModified', 'contentSha256'], 'watch import claim'); if (!['link', 'copy'].includes(value.importMode) || !String(value.mimeType).startsWith('video/') || value.binId !== 'project-bin' || typeof value.generateProxies !== 'boolean') throw new TypeError('Invalid Framescaper watch import claim'); const size = safeInteger(value.size); if (!size) throw new RangeError('Invalid Framescaper watch import size'); return Object.freeze({ ...identity, claimId: nativeIdentifier(value.claimId, 'watch claim id'), projectId: nativeBoundedText(value.projectId, 128, 'watch project id'), projectRevision: safeInteger(value.projectRevision), binId: 'project-bin', generateProxies: value.generateProxies, existingSourceId: value.existingSourceId === null ? null : nativeRuntimeId(value.existingSourceId, 'watch source'), importMode: value.importMode, locatorId: nativeIdentifier(value.locatorId, 'watch locator id'), locatorRevision: nativeIdentifier(value.locatorRevision, 'watch locator revision'), name: nativeBoundedText(value.name, 255, 'watch file name', /[\0/\\]/u), size, mimeType: nativeBoundedText(value.mimeType, 128, 'watch MIME type'), lastModified: safeInteger(value.lastModified), contentSha256: nativeDigest(value.contentSha256, 'watch content') }); }
function nativeJobIds(value) { return Object.freeze(nativeArray(value, 100000, 'scratch cleanup jobs').map((jobId) => nativeIdentifier(jobId, 'job id', 40))); }
function nativeScratchSettlement(value) { if (value !== 'released' && value !== 'retained') throw new TypeError('Invalid Framescaper native scratch settlement'); return value; }
function nativePublicationRequest(value) { const identity = nativeProjectIdentity(value, 'publication request', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'jobId', 'currentPlanFingerprint', 'finalized', 'declaredByteLength', 'declaredSha256'], 'publication request'); if (typeof value.finalized !== 'boolean') throw new TypeError('Invalid Framescaper publication finalization'); return Object.freeze({ ...identity, jobId: nativeIdentifier(value.jobId, 'job id', 40), currentPlanFingerprint: nativeDigest(value.currentPlanFingerprint, 'plan'), finalized: value.finalized, declaredByteLength: safeInteger(value.declaredByteLength), declaredSha256: nativeDigest(value.declaredSha256, 'output') }); }
function nativePublicationResult(value) { nativeRecord(value, ['outcome', 'relativeDestination', 'byteLength', 'sha256'], 'publication result'); if (!['published', 'already-published'].includes(value.outcome)) throw new TypeError('Invalid Framescaper publication outcome'); return Object.freeze({ outcome: value.outcome, relativeDestination: nativeRelativeDestination(value.relativeDestination), byteLength: safeInteger(value.byteLength), sha256: nativeDigest(value.sha256, 'output') }); }
function nativeCheckpointRequest(value) { const identity = nativeProjectIdentity(value, 'checkpoint request', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'jobId', 'sourceInventoryDigest', 'plannedFrameCount', 'manifest'], 'checkpoint request'); if (!Array.isArray(value.manifest) || value.manifest.length > 16384 || utf8Bytes(JSON.stringify(value), 65536) > 65536) throw new RangeError('Framescaper checkpoint control envelope exceeds 64 KiB'); const plannedFrameCount = safeInteger(value.plannedFrameCount); const manifest = nativeArray(value.manifest, 2000000, 'checkpoint manifest').map(nativeCheckpointFrame); if (manifest.length > plannedFrameCount) throw new RangeError('Invalid Framescaper checkpoint manifest length'); return Object.freeze({ ...identity, jobId: nativeIdentifier(value.jobId, 'job id', 40), sourceInventoryDigest: nativeDigest(value.sourceInventoryDigest, 'source inventory'), plannedFrameCount, manifest: Object.freeze(manifest) }); }
function nativeCheckpointFrame(value, index) { nativeRecord(value, ['frameIndex', 'relativePath', 'byteLength', 'sha256', 'planFingerprint', 'sourceInventoryDigest'], 'checkpoint frame'); if (value.frameIndex !== index) throw new TypeError('Invalid Framescaper checkpoint frame index'); return Object.freeze({ frameIndex: index, relativePath: nativeRelativeDestination(value.relativePath), byteLength: safeInteger(value.byteLength), sha256: nativeDigest(value.sha256, 'frame'), planFingerprint: nativeDigest(value.planFingerprint, 'frame plan'), sourceInventoryDigest: nativeDigest(value.sourceInventoryDigest, 'frame source inventory') }); }
function nativeCheckpointResult(value) { nativeRecord(value, ['verifiedFrameCount', 'plannedFrameCount', 'complete'], 'checkpoint result'); const verifiedFrameCount = safeInteger(value.verifiedFrameCount); const plannedFrameCount = safeInteger(value.plannedFrameCount); if (verifiedFrameCount > plannedFrameCount || typeof value.complete !== 'boolean') throw new TypeError('Invalid Framescaper checkpoint result'); return Object.freeze({ verifiedFrameCount, plannedFrameCount, complete: value.complete }); }
function nativeExternalDisplayRequest(value) { nativeRecord(value, ['displayId'], 'external display request'); return Object.freeze({ displayId: value.displayId === null ? null : nativeBoundedText(value.displayId, 128, 'display id') }); }
function nativeExternalDisplays(value) { nativeRecord(value, ['displays', 'activeDisplayId'], 'external display projection'); const displays = Object.freeze(nativeArray(value.displays, 64, 'external displays').map(nativeExternalDisplay)); if (value.activeDisplayId !== null && !displays.some((display) => display.displayId === value.activeDisplayId)) throw new TypeError('Invalid active Framescaper external display'); return Object.freeze({ displays, activeDisplayId: value.activeDisplayId }); }
function nativeExternalDisplay(value) { nativeRecord(value, ['displayId', 'label', 'primary', 'width', 'height', 'hdrCapable', 'colorManaged'], 'external display'); if (typeof value.primary !== 'boolean' || typeof value.hdrCapable !== 'boolean' || typeof value.colorManaged !== 'boolean') throw new TypeError('Invalid Framescaper external display flags'); return Object.freeze({ displayId: nativeBoundedText(value.displayId, 128, 'display id'), label: nativeBoundedText(value.label, 256, 'display label'), primary: value.primary, width: nativeDimension(value.width), height: nativeDimension(value.height), hdrCapable: value.hdrCapable, colorManaged: value.colorManaged }); }
function nativeExternalDisplayFrame(value) { nativeRecord(value, ['sequence', 'evaluationFingerprint', 'width', 'height', 'dynamicRange', 'rgbaSha256', 'rgba'], 'external display frame'); if (!['sdr', 'hdr'].includes(value.dynamicRange)) throw new TypeError('Invalid Framescaper external display dynamic range'); const width = nativeDimension(value.width); const height = nativeDimension(value.height); const rgba = binary(value.rgba); if (width * height * 4 > 64 * 1024 * 1024 || rgba.byteLength !== width * height * 4) throw new RangeError('Invalid Framescaper external display RGBA geometry'); return Object.freeze({ sequence: safeInteger(value.sequence), evaluationFingerprint: nativeDigest(value.evaluationFingerprint, 'evaluated frame'), width, height, dynamicRange: value.dynamicRange, rgbaSha256: nativeDigest(value.rgbaSha256, 'RGBA frame'), rgba }); }
/* Clean-display pixels use a one-frame MessagePort data plane.
 * Control IPC carries only exact geometry and digests; one acknowledgement
 * supplies backpressure before the completion-bound projection is accepted.
 */
function presentNativeExternalDisplayFrame(value) { const frame = nativeExternalDisplayFrame(value); const streamId = frame.rgbaSha256.slice(0, 40); const channel = new MessageChannel(); const port = channel.port2; const descriptor = Object.freeze({ sequence: frame.sequence, evaluationFingerprint: frame.evaluationFingerprint, width: frame.width, height: frame.height, dynamicRange: frame.dynamicRange, rgbaSha256: frame.rgbaSha256 }); const binding = Object.freeze({ dataPlaneVersion: 1, transport: 'message-port', streamId, direction: 'host-to-helper', byteLength: frame.rgba.byteLength, sha256: frame.rgbaSha256, maximumChunkBytes: 16 * 1024 * 1024, maximumInFlightChunks: 1 });
	return new Promise((resolve, reject) => { let awaiting = 'ack'; let settled = false; let offset = 0; let sequence = 0; const finish = (error, result) => { if (settled) return; settled = true; clearTimeout(timeout); port.onmessage = null; port.onmessageerror = null; port.close(); if (error) reject(error); else resolve(result); }; const sendChunk = () => { const end = Math.min(binding.byteLength, offset + binding.maximumChunkBytes); const bytes = frame.rgba.slice(offset, end); port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'chunk', streamId, sequence, offset, bytes }), [bytes.buffer]); }; const timeout = setTimeout(() => { try { port.postMessage({ dataPlaneVersion: 1, type: 'cancel', streamId, reason: 'host-abort' }); } catch { /* lost */ } finish(new Error('External-display frame transfer timed out')); }, 15000);
		port.onmessage = (event) => { try { const message = event.data; if (awaiting === 'ack') { nativeRecord(message, ['dataPlaneVersion', 'type', 'streamId', 'sequence', 'receivedBytes'], 'external display frame acknowledgement'); const expectedBytes = Math.min(binding.byteLength, offset + binding.maximumChunkBytes); if (message.dataPlaneVersion !== 1 || message.type !== 'ack' || message.streamId !== streamId || message.sequence !== sequence || message.receivedBytes !== expectedBytes) throw new TypeError('Invalid external-display frame acknowledgement'); offset = expectedBytes; sequence += 1; if (offset < binding.byteLength) { sendChunk(); return; } awaiting = 'result'; port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'complete', streamId, byteLength: binding.byteLength, sha256: frame.rgbaSha256 })); return; } nativeRecord(message, message?.type === 'result' ? ['dataPlaneVersion', 'type', 'streamId', 'projection'] : ['dataPlaneVersion', 'type', 'streamId', 'message'], 'external display frame result'); if (message.dataPlaneVersion !== 1 || message.streamId !== streamId) throw new TypeError('Invalid external-display frame result'); if (message.type === 'failure') return finish(new Error(nativeBoundedText(message.message, 512, 'frame failure'))); if (message.type !== 'result') throw new TypeError('Invalid external-display frame result'); finish(null, nativeExternalDisplays(message.projection)); } catch (error) { finish(error); } }; port.onmessageerror = () => finish(new Error('External-display frame MessagePort failed')); port.start();
		try { ipcRenderer.postMessage(CHANNELS.framescaperNativeFramePort, Object.freeze({ frame: descriptor, binding }), [channel.port1]); sendChunk(); } catch (error) { finish(error); }
	}); }
function nativeBoundedText(value, maximum, label, forbidden = /\0/u) { if (typeof value !== 'string' || !value.length || value.length > maximum || forbidden.test(value)) throw new TypeError(`Invalid Framescaper native ${label}`); return value; }
function nativeDimension(value) { const result = safeInteger(value); if (!result || result > 32768) throw new RangeError('Invalid Framescaper native display dimension'); return result; }
function nativeRelativeDestination(value) { if (typeof value !== 'string' || !value.length || value.length > 4096 || value.startsWith('/') || value.includes('\\') || value.includes(':') || value.split('/').some((part) => !part || part === '.' || part === '..')) throw new TypeError('Invalid Framescaper native relative destination'); return value; }
function nativeQueueControlRequest(value) { nativeRecord(value, ['jobId', 'action'], 'native queue control request'); const action = value.action;
	if (!['pause', 'resume', 'cancel', 'retry'].includes(action)) throw new TypeError('Invalid native queue action');
	return Object.freeze({ jobId: opaqueId(value.jobId, 40), action }); }
function nativeQueueReorderRequest(value) { nativeRecord(value, ['jobId', 'index'], 'native queue reorder request'); return Object.freeze({ jobId: opaqueId(value.jobId, 40), index: safeInteger(value.index) }); }
function nativeQueueRemoveRequest(value) { nativeRecord(value, ['jobId'], 'native queue remove request'); return Object.freeze({ jobId: opaqueId(value.jobId, 40) }); } async function abandonNativeRenderInputs(value) { const request = nativeLifecycleIdRequest(value, 'stageId'); return ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputAbandon, request).then((result) => { if (strictBoolean(result, 'Framescaper render-input abandonment must be acknowledged') !== true) throw new Error('Framescaper render-input abandonment was not acknowledged'); return true; }); } async function stageNativeRenderInputs(value) { const request = nativeRenderInputStageRequest(value); const admission = nativeRenderInputAdmission(await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputBegin, request.control), request.inputs); try { for (const [index, input] of request.inputs.entries()) await transferNativeRenderInput(admission.stageId, index, admission.inputs[index].binding, input.bytes); const result = await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputFinalize, Object.freeze({ stageId: admission.stageId })); nativeRecord(result, ['stageId'], 'render-input finalization'); if (result.stageId !== admission.stageId) throw new Error('Framescaper render-input finalization changed stage identity'); return Object.freeze({ stageId: admission.stageId }); } catch (error) { const cleanupError = await renderInputAbandonFailure(admission.stageId); if (cleanupError !== null) throw new AggregateError([error, cleanupError], 'Framescaper render-input staging failed and could not be abandoned', { cause: error }); throw error; } } async function stageNativeLiveRenderInputs(value) { const request = nativeLiveRenderInputStageRequest(value); const admission = nativeLiveRenderInputAdmission(await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputBeginLive, request.control), request); try { const result = await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputFinalize, Object.freeze({ stageId: admission.stageId })); nativeRecord(result, ['stageId'], 'live render-input finalization'); if (result.stageId !== admission.stageId) throw new Error('Framescaper live render-input finalization changed stage identity'); return Object.freeze({ stageId: admission.stageId, carrierByteLength: admission.carrierByteLength, scratchByteLength: admission.scratchByteLength }); } catch (error) { const cleanupError = await renderInputAbandonFailure(admission.stageId); if (cleanupError !== null) throw new AggregateError([error, cleanupError], 'Framescaper live render-input staging failed and could not be abandoned', { cause: error }); throw error; } } function nativeLiveRenderInputStageRequest(value) { const identity = nativeProjectIdentity(value, 'live render-input stage', 'framescaper'); nativeRecord(value, ['liveRenderVersion', 'schemaFamily', 'schemaVersion', 'planVersion', 'planFingerprint', 'planPayload', 'projectId', 'projectRevision', 'inputFingerprints', 'restartJobId', 'carrierByteLength', 'audio'], 'live render-input stage request'); if (value.liveRenderVersion !== 1 || value.planVersion !== 14 || typeof value.planPayload !== 'string' || !value.planPayload.length || utf8Bytes(value.planPayload, 65536) > 65536 || !Number.isSafeInteger(value.carrierByteLength) || value.carrierByteLength < 1 || value.carrierByteLength > 16 * 1024 ** 4) throw new TypeError('Invalid Framescaper live V14 render-input plan'); const includesAudio = nativeSelectedRenderPlanIncludesAudio(value.planPayload, 14); const audio = value.audio === null ? null : nativeLiveAudioReservation(value.audio); if ((audio !== null) !== includesAudio) throw new TypeError('Invalid Framescaper live V14 audio authority'); const inputFingerprints = Object.freeze(nativeArray(value.inputFingerprints, 4096, 'live render-input originals').map((entry) => { nativeRecord(entry, ['sourceId', 'sha256'], 'live render-input original'); return Object.freeze({ sourceId: nativeBoundedText(entry.sourceId, 128, 'source id'), sha256: nativeDigest(entry.sha256, 'original') }); })); return Object.freeze({ control: Object.freeze({ liveRenderVersion: 1, ...identity, planVersion: 14, planFingerprint: nativeDigest(value.planFingerprint, 'plan'), planPayload: value.planPayload, projectId: nativeBoundedText(value.projectId, 128, 'project id'), projectRevision: safeInteger(value.projectRevision), inputFingerprints, restartJobId: value.restartJobId === null ? null : opaqueId(value.restartJobId, 40), carrierByteLength: value.carrierByteLength, audio }), audio }); } function nativeLiveAudioReservation(value) { nativeRecord(value, ['role', 'byteLength'], 'live render-input audio reservation'); if (value.role !== 'staged-audio-mix' || !Number.isSafeInteger(value.byteLength) || value.byteLength < 1 || value.byteLength > 16 * 1024 ** 4) throw new TypeError('Invalid Framescaper live audio reservation'); return Object.freeze({ role: value.role, byteLength: value.byteLength }); } function nativeLiveRole(value) { if (!['evaluated-rgba-frame-pack', 'staged-audio-mix'].includes(value)) throw new TypeError('Invalid Framescaper live input role'); return value; } function nativeLiveRenderInputAdmission(value, request) { nativeRecord(value, ['liveRenderVersion', 'stageId', 'carrierByteLength', 'scratchByteLength', 'streams'], 'live render-input admission'); const expected = request.audio === null ? [{ role: 'evaluated-rgba-frame-pack', byteLength: request.control.carrierByteLength }] : [{ role: 'evaluated-rgba-frame-pack', byteLength: request.control.carrierByteLength }, request.audio]; const expectedScratch = expected.reduce((sum, row) => safeInteger(sum + row.byteLength), 64 * 1024); if (value.liveRenderVersion !== 1 || value.carrierByteLength !== request.control.carrierByteLength || value.scratchByteLength !== expectedScratch || !Array.isArray(value.streams) || JSON.stringify(value.streams) !== JSON.stringify(expected)) throw new TypeError('Invalid Framescaper live render-input admission'); return Object.freeze({ stageId: opaqueId(value.stageId, 40), carrierByteLength: value.carrierByteLength, scratchByteLength: value.scratchByteLength, streams: Object.freeze(expected) }); } async function writeNativeLiveRenderInput(value) { nativeRecord(value, ['stageId', 'role', 'sequence', 'offset', 'bytes'], 'live render-input chunk'); const bytes = binary(value.bytes); if (!bytes.byteLength || bytes.byteLength > 16 * 1024 * 1024) throw new RangeError('Invalid Framescaper live render-input chunk length'); const request = Object.freeze({ stageId: opaqueId(value.stageId, 40), role: nativeLiveRole(value.role), sequence: safeInteger(value.sequence), offset: safeInteger(value.offset), bytes }); const result = await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputWriteLive, request); nativeRecord(result, ['sequence', 'receivedBytes'], 'live render-input acknowledgement'); if (result.sequence !== request.sequence || result.receivedBytes !== request.offset + bytes.byteLength) throw new Error('Invalid Framescaper live render-input acknowledgement'); return Object.freeze({ sequence: result.sequence, receivedBytes: result.receivedBytes }); } async function completeNativeLiveRenderInput(value) { nativeRecord(value, ['stageId', 'role', 'byteLength', 'sha256'], 'live render-input completion'); const request = Object.freeze({ stageId: opaqueId(value.stageId, 40), role: nativeLiveRole(value.role), byteLength: safeInteger(value.byteLength), sha256: nativeDigest(value.sha256, 'live carrier') }); const result = await ipcRenderer.invoke(CHANNELS.framescaperNativeRenderInputCompleteLive, request); nativeRecord(result, ['byteLength', 'sha256'], 'live render-input completion result'); if (result.byteLength !== request.byteLength || result.sha256 !== request.sha256) throw new Error('Framescaper live render-input completion changed identity'); return Object.freeze({ byteLength: request.byteLength, sha256: request.sha256 }); } async function renderInputAbandonFailure(stageId) { try { await abandonNativeRenderInputs({ stageId }); return null; } catch (error) { return error; } } function nativeRenderInputStageRequest(value) { const identity = nativeProjectIdentity(value, 'render-input stage', 'framescaper'); nativeRecord(value, ['stageVersion', 'schemaFamily', 'schemaVersion', 'planVersion', 'planFingerprint', 'planPayload', 'projectId', 'projectRevision', 'inputFingerprints', 'derivedInputs'], 'render-input stage request'); if (value.stageVersion !== 1 || ![7, 8, 14].includes(value.planVersion) || typeof value.planPayload !== 'string' || !value.planPayload.length || utf8Bytes(value.planPayload, 65536) > 65536) throw new TypeError('Invalid Framescaper V7/V8/V14 render-input plan'); const includesAudio = nativeSelectedRenderPlanIncludesAudio(value.planPayload, value.planVersion); const roles = value.planVersion === 7 || value.planVersion === 14 ? ['evaluated-rgba-frame-pack', ...(includesAudio ? ['staged-audio-mix'] : [])] : includesAudio ? ['staged-audio-mix'] : []; if (!roles.length) throw new TypeError('A silent Framescaper V8 plan has no derived-input stage'); const inputFingerprints = Object.freeze(nativeArray(value.inputFingerprints, 4096, 'render-input originals').map((entry) => { nativeRecord(entry, ['sourceId', 'sha256'], 'render-input original'); return Object.freeze({ sourceId: nativeBoundedText(entry.sourceId, 128, 'source id'), sha256: nativeDigest(entry.sha256, 'original') }); })); const inputs = Object.freeze(nativeArray(value.derivedInputs, 2, 'derived render inputs').map((entry, index) => nativeRenderInput(entry, index, roles[index]))); if (inputs.length !== roles.length) throw new TypeError('Invalid Framescaper derived-input role order'); return Object.freeze({ control: Object.freeze({ stageVersion: 1, ...identity, planVersion: value.planVersion, planFingerprint: nativeDigest(value.planFingerprint, 'plan'), planPayload: value.planPayload, projectId: nativeBoundedText(value.projectId, 128, 'project id'), projectRevision: safeInteger(value.projectRevision), inputFingerprints, derivedInputs: Object.freeze(inputs.map(({ role, byteLength, sha256 }) => Object.freeze({ role, byteLength, sha256 }))) }), inputs }); }
function nativeRenderInput(value, index, role) { nativeRecord(value, ['role', 'byteLength', 'sha256', 'bytes'], `derived render input ${String(index)}`); if (value.role !== role || !['evaluated-rgba-frame-pack', 'staged-audio-mix'].includes(role) || typeof Blob !== 'function' || !(value.bytes instanceof Blob)) throw new TypeError('Invalid Framescaper derived render input'); const size = Reflect.apply(Object.getOwnPropertyDescriptor(Blob.prototype, 'size').get, value.bytes, []); if (!Number.isSafeInteger(size) || size < 1 || size !== value.byteLength || size > 16 * 1024 ** 3) throw new RangeError('Invalid Framescaper derived render input length'); return Object.freeze({ role, byteLength: size, sha256: nativeDigest(value.sha256, 'derived input'), bytes: Blob.prototype.slice.call(value.bytes, 0, size) }); } function nativeSelectedRenderPlanIncludesAudio(payload, version) { let plan; try { plan = JSON.parse(payload); } catch { throw new TypeError('Invalid Framescaper render-input plan JSON'); } if (!plan || typeof plan !== 'object' || plan.version !== version) throw new TypeError('Invalid Framescaper render-input plan identity'); if (version === 14) { if (!plan.output || typeof plan.output !== 'object' || typeof plan.output.includeAudio !== 'boolean') throw new TypeError('Invalid Framescaper V14 audio authority'); return plan.output.includeAudio; } if (version === 8 && !Array.isArray(plan.inputs)) throw new TypeError('Invalid Framescaper V8 render-input plan identity'); const inputs = Array.isArray(plan.inputs) ? plan.inputs : []; const count = inputs.filter((input) => input && typeof input === 'object' && input.kind === 'staged-audio-mix').length; if (count > 1) throw new TypeError('Invalid Framescaper staged-audio plan inventory'); return count === 1; } function nativeRenderInputAdmission(value, inputs) { nativeRecord(value, ['stageVersion', 'stageId', 'inputs'], 'render-input admission'); const stageId = opaqueId(value.stageId, 40); if (value.stageVersion !== 1 || !Array.isArray(value.inputs) || value.inputs.length !== inputs.length) throw new TypeError('Invalid Framescaper render-input admission'); return Object.freeze({ stageId, inputs: Object.freeze(value.inputs.map((entry, index) => { nativeRecord(entry, ['inputIndex', 'role', 'binding'], 'render-input stream admission'); if (entry.inputIndex !== index || entry.role !== inputs[index].role) throw new TypeError('Invalid Framescaper render-input stream order'); return Object.freeze({ inputIndex: index, role: entry.role, binding: nativeRenderInputBinding(entry.binding) }); })) }); } function nativeRenderInputBinding(value) { nativeRecord(value, ['dataPlaneVersion', 'transport', 'streamId', 'direction', 'byteLength', 'sha256', 'maximumChunkBytes', 'maximumInFlightChunks'], 'render-input data-plane binding'); if (value.dataPlaneVersion !== 1 || value.transport !== 'message-port' || value.direction !== 'host-to-helper' || !Number.isSafeInteger(value.byteLength) || value.byteLength < 1 || !Number.isSafeInteger(value.maximumChunkBytes) || value.maximumChunkBytes < 1 || value.maximumChunkBytes > 16 * 1024 * 1024 || value.maximumInFlightChunks !== 1) throw new TypeError('Invalid Framescaper render-input data-plane binding'); return Object.freeze({ ...value, streamId: opaqueId(value.streamId, 40), sha256: nativeDigest(value.sha256, 'stream') }); } async function transferNativeRenderInput(stageId, inputIndex, binding, blob) { if (binding.byteLength !== blob.size) throw new Error('Framescaper render-input bytes changed after admission'); const channel = new MessageChannel(); const port = channel.port2; port.start(); try { ipcRenderer.postMessage(CHANNELS.framescaperNativeRenderInputPort, Object.freeze({ stageId, inputIndex, binding }), [channel.port1]); let offset = 0; let sequence = 0; while (offset < binding.byteLength) { const end = Math.min(binding.byteLength, offset + binding.maximumChunkBytes); const bytes = new Uint8Array(await Blob.prototype.slice.call(blob, offset, end).arrayBuffer()); const ack = await nativeRenderInputPortReply(port, Object.freeze({ dataPlaneVersion: 1, type: 'chunk', streamId: binding.streamId, sequence, offset, bytes }), [bytes.buffer]); nativeRecord(ack, ['dataPlaneVersion', 'type', 'streamId', 'sequence', 'receivedBytes'], 'render-input acknowledgement'); if (ack.dataPlaneVersion !== 1 || ack.type !== 'ack' || ack.streamId !== binding.streamId || ack.sequence !== sequence || ack.receivedBytes !== end) throw new Error('Invalid Framescaper render-input acknowledgement'); offset = end; sequence += 1; } port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'complete', streamId: binding.streamId, byteLength: binding.byteLength, sha256: binding.sha256 })); } finally { port.close(); } }
function nativeRenderInputPortReply(port, message, transfer) { return new Promise((resolve, reject) => { let settled = false; const finish = (error, value) => { if (settled) return; settled = true; clearTimeout(timeout); port.onmessage = null; port.onmessageerror = null; if (error) reject(error); else resolve(value); }; const timeout = setTimeout(() => finish(new Error('Framescaper render-input transfer timed out')), 30000); port.onmessage = (event) => finish(null, event.data); port.onmessageerror = () => finish(new Error('Framescaper render-input MessagePort failed')); try { port.postMessage(message, transfer); } catch (error) { finish(error); } }); } function nativeQueueEnqueueRequest(value) { const identity = nativeProjectIdentity(value, 'native queue enqueue', 'framescaper'); nativeRecord(value, ['schemaFamily', 'schemaVersion', 'taskKind', 'planVersion', 'derivedInputStageId', 'planFingerprint', 'planPayload', 'projectId', 'projectRevision', 'inputFingerprints', 'rootGrantId', 'relativeDestination', 'reservations', 'recoveryClass'], 'native queue enqueue request');
	if (!['encoded-export', 'image-sequence-export', 'proxy-generation'].includes(value.taskKind) || ![7, 8, 9, 10, 11, 12, 14].includes(value.planVersion) || typeof value.planPayload !== 'string' || !value.planPayload.length || utf8Bytes(value.planPayload, 65536) > 65536 || !['atomic-restart', 'verified-frame-checkpoint'].includes(value.recoveryClass)) throw new TypeError('Invalid Framescaper native queue enqueue plan');
	const inputFingerprints = Object.freeze(nativeArray(value.inputFingerprints, 4096, 'queue input fingerprints').map((entry) => { nativeRecord(entry, ['sourceId', 'sha256'], 'queue input fingerprint'); return Object.freeze({ sourceId: nativeBoundedText(entry.sourceId, 128, 'source id'), sha256: nativeDigest(entry.sha256, 'input') }); })); const reservations = value.reservations; nativeRecord(reservations, ['cpuCores', 'processTreeRssBytes', 'scratchBytes', 'minimumFreeBytes', 'hardwareBackend'], 'queue reservations');
	const derivedInputStageId = value.derivedInputStageId === null ? null : opaqueId(value.derivedInputStageId, 40); if (value.taskKind === 'proxy-generation') { if (value.planVersion !== 14 || derivedInputStageId !== null) throw new TypeError('Baseline V14 proxy generation cannot name a derived-input stage'); } else if (value.planVersion >= 9 && value.planVersion !== 14) { if (derivedInputStageId !== null) throw new TypeError(`Unified V${String(value.planVersion)} cannot name a derived-input stage`); } else if (value.planVersion !== 14) { if (![7, 8].includes(value.planVersion)) throw new TypeError('Invalid Framescaper native derived-input stage'); const stageRequired = value.planVersion === 7 || nativeSelectedRenderPlanIncludesAudio(value.planPayload, value.planVersion); if ((derivedInputStageId !== null) !== stageRequired) throw new TypeError(stageRequired ? `Invalid Framescaper native derived-input stage for V${String(value.planVersion)}` : 'Silent Framescaper V8 cannot name a derived-input stage'); } return Object.freeze({ ...identity, taskKind: value.taskKind, planVersion: value.planVersion, derivedInputStageId, planFingerprint: nativeDigest(value.planFingerprint, 'plan'), planPayload: value.planPayload, projectId: nativeBoundedText(value.projectId, 128, 'project id'), projectRevision: safeInteger(value.projectRevision), inputFingerprints, rootGrantId: nativeIdentifier(value.rootGrantId, 'grant id'), relativeDestination: nativeRelativeDestination(value.relativeDestination), reservations: Object.freeze({ cpuCores: safeInteger(reservations.cpuCores), processTreeRssBytes: safeInteger(reservations.processTreeRssBytes), scratchBytes: safeInteger(reservations.scratchBytes), minimumFreeBytes: safeInteger(reservations.minimumFreeBytes), hardwareBackend: reservations.hardwareBackend === null ? null : nativeBoundedText(reservations.hardwareBackend, 128, 'hardware backend') }), recoveryClass: value.recoveryClass }); }
const NATIVE_SERVICE_PREFERENCES = ['native-media', 'hardware-decode', 'hardware-encode', 'ofx-consent']; function nativeServicePreferences(value) { const fields = ['nativeMediaEnabled', 'hardwareDecodeEnabled', 'hardwareEncodeEnabled', 'ofxConsentEnabled'];
	nativeRecord(value, fields, 'native-service preferences'); for (const key of fields) if (typeof value[key] !== 'boolean') throw new TypeError('Framescaper native-service preference must be boolean'); return Object.freeze({ ...value }); }
function nativePreferenceRequest(value) { nativeRecord(value, ['preference', 'enabled'], 'native-service preference request');
	if (!NATIVE_SERVICE_PREFERENCES.includes(value.preference) || typeof value.enabled !== 'boolean') throw new TypeError('Unsupported Framescaper native-service preference'); return Object.freeze({ preference: value.preference, enabled: value.enabled }); }
function nativeImageSequenceSelection(value) { nativeRecord(value, ['selectionId', 'files'], 'image-sequence selection'); const selectionId = nativeImageSequenceId(value.selectionId); const files = nativeArray(value.files, 1000000, 'image-sequence files'); if (!files.length || Reflect.ownKeys(files).length !== files.length + 1) throw new TypeError('Invalid Framescaper image-sequence file inventory'); const ids = new Set(); return Object.freeze({ selectionId, files: Object.freeze(files.map((file) => { nativeRecord(file, ['fileId', 'name', 'byteLength'], 'image-sequence file'); const fileId = nativeImageSequenceId(file.fileId); if (ids.has(fileId) || typeof file.name !== 'string' || !file.name.length || file.name.length > 512 || /[\\/\0]/u.test(file.name) || !Number.isSafeInteger(file.byteLength) || file.byteLength < 1 || file.byteLength > 512 * 1024 * 1024) throw new TypeError('Invalid pathless Framescaper image-sequence file'); ids.add(fileId); return Object.freeze({ fileId, name: file.name, byteLength: file.byteLength }); })) }); }
function nativeImageSequenceReadRequest(value) { nativeRecord(value, ['selectionId', 'fileId', 'offset', 'length'], 'image-sequence read request'); if (!Number.isSafeInteger(value.offset) || value.offset < 0 || !Number.isSafeInteger(value.length) || value.length < 1 || value.length > 16 * 1024 * 1024) throw new RangeError('Invalid Framescaper image-sequence range'); return Object.freeze({ selectionId: nativeImageSequenceId(value.selectionId), fileId: nativeImageSequenceId(value.fileId), offset: value.offset, length: value.length }); }
	function nativeImageSequenceReleaseRequest(value) { nativeRecord(value, ['selectionId'], 'image-sequence release request'); return Object.freeze({ selectionId: nativeImageSequenceId(value.selectionId) }); } function nativeImageSequenceId(value) { if (typeof value !== 'string' || !/^[a-f0-9]{40}$/u.test(value)) throw new TypeError('Invalid Framescaper image-sequence opaque id'); return value; } function nativeImageSequenceImportRequest(value) { const fields = { begin: ['operation', 'schemaFamily', 'schemaVersion', 'projectId', 'projectRevision'], 'prepare-write': ['operation', 'transactionId', 'asset', 'offset', 'binding'], 'await-write': ['operation', 'transactionId', 'asset', 'offset', 'streamId'], commit: ['operation', 'transactionId', 'asset', 'reference'], read: ['operation', 'transactionId', 'asset', 'offset', 'length'], admit: ['operation', 'transactionId', 'admission'], complete: ['operation', 'transactionId', 'sourceId', 'inventorySha256', 'sourcePackSha256'], discard: ['operation', 'transactionId'] }; if (!value || typeof value !== 'object' || typeof value.operation !== 'string' || !Object.hasOwn(fields, value.operation)) throw new TypeError('Invalid Framescaper image-sequence import operation'); if (value.operation === 'begin') nativeProjectIdentity(value, 'image-sequence import opening', 'framescaper'); if (value.operation === 'admit') nativeProjectIdentity(value.admission, 'image-sequence admission', 'framescaper'); nativeRecord(value, fields[value.operation], 'image-sequence import request'); const result = structuredClone(value); if (utf8Bytes(JSON.stringify(result), 65536) > 65536) throw new RangeError('Framescaper image-sequence import control exceeds 64 KiB'); return result; } function nativeImageSequenceImportReadRequest(value) { nativeRecord(value, ['transactionId', 'asset', 'offset', 'length'], 'image-sequence body read'); if (!['pack', 'inventory'].includes(value.asset) || !Number.isSafeInteger(value.offset) || value.offset < 0 || !Number.isSafeInteger(value.length) || value.length < 1 || value.length > 16 * 1024 * 1024) throw new RangeError('Invalid Framescaper image-sequence body range'); return Object.freeze({ transactionId: nativeImageSequenceId(value.transactionId), asset: value.asset, offset: value.offset, length: value.length }); } function nativeImageSequenceImportChunkRequest(value) { nativeRecord(value, ['transactionId', 'asset', 'offset', 'bytes'], 'image-sequence import chunk'); const bytes = binary(value.bytes); if (!['pack', 'inventory'].includes(value.asset) || !Number.isSafeInteger(value.offset) || value.offset < 0 || bytes.byteLength < 1 || bytes.byteLength > 16 * 1024 * 1024) throw new RangeError('Invalid Framescaper image-sequence import chunk'); return Object.freeze({ transactionId: nativeImageSequenceId(value.transactionId), asset: value.asset, offset: value.offset, bytes }); } async function transferNativeImageSequenceImportChunk(value) { const request = nativeImageSequenceImportChunkRequest(value); const digest = Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256', request.bytes))).map((byte) => byte.toString(16).padStart(2, '0')).join(''); const binding = Object.freeze({ dataPlaneVersion: 1, transport: 'message-port', streamId: digest.slice(0, 40), direction: 'host-to-helper', byteLength: request.bytes.byteLength, sha256: digest, maximumChunkBytes: 16 * 1024 * 1024, maximumInFlightChunks: 1 }); const control = Object.freeze({ transactionId: request.transactionId, asset: request.asset, offset: request.offset, binding }); await ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceImport, { operation: 'prepare-write', ...control }); const channel = new MessageChannel(); const port = channel.port2; port.start(); try { ipcRenderer.postMessage(CHANNELS.framescaperNativeImageSequenceImportPort, control, [channel.port1]); const bytes = request.bytes.slice(); const acknowledgement = await nativeRenderInputPortReply(port, Object.freeze({ dataPlaneVersion: 1, type: 'chunk', streamId: binding.streamId, sequence: 0, offset: 0, bytes }), [bytes.buffer]); nativeRecord(acknowledgement, ['dataPlaneVersion', 'type', 'streamId', 'sequence', 'receivedBytes'], 'image-sequence import acknowledgement'); if (acknowledgement.dataPlaneVersion !== 1 || acknowledgement.type !== 'ack' || acknowledgement.streamId !== binding.streamId || acknowledgement.sequence !== 0 || acknowledgement.receivedBytes !== binding.byteLength) throw new Error('Invalid Framescaper image-sequence import acknowledgement'); port.postMessage(Object.freeze({ dataPlaneVersion: 1, type: 'complete', streamId: binding.streamId, byteLength: binding.byteLength, sha256: digest })); return await ipcRenderer.invoke(CHANNELS.framescaperNativeImageSequenceImport, { operation: 'await-write', transactionId: request.transactionId, asset: request.asset, offset: request.offset, streamId: binding.streamId }); } finally { port.close(); } } function nativeProxyOutputJobRequest(value) { nativeRecord(value, ['jobId'], 'proxy-output job request'); return Object.freeze({ jobId: opaqueId(value.jobId, 40) }); } function nativeProxyOutputClaimRequest(value) { nativeRecord(value, ['claimId'], 'proxy-output claim request'); return Object.freeze({ claimId: opaqueId(value.claimId, 40) }); } function nativeProxyOutputReadRequest(value) { nativeRecord(value, ['claimId', 'offset', 'length'], 'proxy-output read request'); if (!Number.isSafeInteger(value.offset) || value.offset < 0 || !Number.isSafeInteger(value.length) || value.length < 1 || value.length > 1024 * 1024) throw new RangeError('Invalid Framescaper proxy-output range'); return Object.freeze({ claimId: opaqueId(value.claimId, 40), offset: value.offset, length: value.length }); } function nativeProxyOutputClaim(value) { nativeRecord(value, ['claimId', 'byteLength', 'sha256', 'mimeType'], 'proxy-output claim'); if (value.mimeType !== 'video/quicktime') throw new TypeError('Invalid Framescaper proxy-output MIME type'); return Object.freeze({ claimId: opaqueId(value.claimId, 40), byteLength: safeInteger(value.byteLength), sha256: nativeDigest(value.sha256, 'proxy output'), mimeType: value.mimeType }); } const NATIVE_OPENFX_CONTEXTS = ['generator', 'filter', 'transition', 'paint', 'retimer', 'general']; const NATIVE_OPENFX_PARAMETER_TYPES = ['integer', 'integer2d', 'integer3d', 'double', 'double2d', 'double3d', 'rgb', 'rgba', 'boolean', 'choice', 'string', 'group', 'page', 'pushbutton', 'parametric', 'custom'];
function nativeOpenFxProjection(value) { nativeRecord(value, ['pluginHandle', 'pluginId', 'vendor', 'version', 'binarySha256', 'supportedContexts', 'parameters', 'components', 'pixelDepths', 'threading', 'state', 'quarantined'], 'OpenFX plug-in projection'); nativeRecord(value.version, ['major', 'minor'], 'OpenFX plug-in version'); const contexts = nativeArray(value.supportedContexts, 6, 'OpenFX contexts').map((entry) => { const context = nativeBoundedText(entry, 16, 'OpenFX context'); if (!NATIVE_OPENFX_CONTEXTS.includes(context)) throw new TypeError('Invalid OpenFX context'); return context; }); const parameters = nativeArray(value.parameters, 4096, 'OpenFX parameters').map((parameter) => { nativeRecord(parameter, ['name', 'type', 'animates'], 'OpenFX parameter'); if (!NATIVE_OPENFX_PARAMETER_TYPES.includes(parameter.type) || typeof parameter.animates !== 'boolean') throw new TypeError('Invalid OpenFX parameter'); return Object.freeze({ name: nativeBoundedText(parameter.name, 64, 'OpenFX parameter name'), type: parameter.type, animates: parameter.animates }); }); if (!Number.isSafeInteger(value.version.major) || value.version.major < 0 || !Number.isSafeInteger(value.version.minor) || value.version.minor < 0 || !['unsafe', 'instance-safe', 'fully-safe'].includes(value.threading) || !['discovered', 'consented', 'enabled', 'revoked', 'quarantined'].includes(value.state) || typeof value.quarantined !== 'boolean') throw new TypeError('Invalid OpenFX plug-in projection'); const components = nativeArray(value.components, 3, 'OpenFX components').map((entry) => { if (!['RGBA', 'RGB', 'Alpha'].includes(entry)) throw new TypeError('Invalid OpenFX component'); return entry; }); const pixelDepths = nativeArray(value.pixelDepths, 3, 'OpenFX pixel depths').map((entry) => { if (!['byte', 'short', 'float'].includes(entry)) throw new TypeError('Invalid OpenFX pixel depth'); return entry; }); return Object.freeze({ pluginHandle: opaqueId(value.pluginHandle, 40), pluginId: nativeBoundedText(value.pluginId, 128, 'OpenFX plug-in id'), vendor: value.vendor === null ? null : nativeBoundedText(value.vendor, 128, 'OpenFX vendor'), version: Object.freeze({ major: value.version.major, minor: value.version.minor }), binarySha256: nativeDigest(value.binarySha256, 'OpenFX binary'), supportedContexts: Object.freeze(contexts), parameters: Object.freeze(parameters), components: Object.freeze(components), pixelDepths: Object.freeze(pixelDepths), threading: value.threading, state: value.state, quarantined: value.quarantined }); } function nativeOpenFxInventory(value) { return Object.freeze(nativeArray(value, 1024, 'OpenFX inventory').map(nativeOpenFxProjection)); } function nativeOpenFxControlRequest(value) { nativeRecord(value, ['pluginHandle', 'action'], 'OpenFX control request'); if (!['enable', 'revoke', 'clear-quarantine'].includes(value.action)) throw new TypeError('Invalid OpenFX control action'); return Object.freeze({ pluginHandle: opaqueId(value.pluginHandle, 40), action: value.action }); } function nativeOpenFxFrameOffer(value) { nativeRecord(value, ['protocolVersion', 'sessionId'], 'OpenFX frame offer'); if (value.protocolVersion !== 1) throw new TypeError('Invalid OpenFX frame protocol'); return Object.freeze({ protocolVersion: 1, sessionId: opaqueId(value.sessionId, 40) }); }
function nativeOpenFxInteractRequest(value) { nativeRecord(value, ['protocolVersion', 'pluginHandle', 'context', 'target', 'parameterName', 'events'], 'OpenFX Interact request'); if (value.protocolVersion !== 1 || !NATIVE_OPENFX_CONTEXTS.includes(value.context) || !['overlay', 'custom-parameter'].includes(value.target) || (value.target === 'overlay' ? value.parameterName !== null : typeof value.parameterName !== 'string' || !/^[A-Za-z_][A-Za-z\d_]{0,63}$/u.test(value.parameterName))) throw new TypeError('Invalid OpenFX Interact request'); const events = nativeArray(value.events, 256, 'OpenFX Interact events'); if (Reflect.ownKeys(events).length !== events.length + 1) throw new TypeError('Invalid OpenFX Interact event array'); let previous = -1; const admitted = events.map((event) => { nativeRecord(event, event?.kind === 'pointer' ? ['kind', 'phase', 'sequence', 'x', 'y', 'button', 'modifiers'] : event?.kind === 'keyboard' ? ['kind', 'phase', 'sequence', 'key', 'code', 'modifiers'] : ['kind', 'sequence', 'focused'], 'OpenFX Interact event'); if (!Number.isSafeInteger(event.sequence) || event.sequence < 0 || event.sequence <= previous) throw new TypeError('Invalid OpenFX Interact sequence'); previous = event.sequence; if (event.kind === 'focus') { if (typeof event.focused !== 'boolean') throw new TypeError('Invalid OpenFX focus event'); return Object.freeze({ kind: 'focus', sequence: event.sequence, focused: event.focused }); } const modifiers = nativeArray(event.modifiers, 4, 'OpenFX Interact modifiers').map((entry) => nativeBoundedText(entry, 7, 'OpenFX modifier')); if (Reflect.ownKeys(event.modifiers).length !== event.modifiers.length + 1 || modifiers.some((entry, index) => !['alt', 'control', 'meta', 'shift'].includes(entry) || (index > 0 && entry <= modifiers[index - 1]))) throw new TypeError('Invalid OpenFX modifiers'); if (event.kind === 'pointer') { if (!['motion', 'down', 'up'].includes(event.phase) || !Number.isFinite(event.x) || event.x < 0 || event.x > 1 || !Number.isFinite(event.y) || event.y < 0 || event.y > 1 || !Number.isSafeInteger(event.button) || event.button < 0 || event.button > 7) throw new TypeError('Invalid OpenFX pointer event'); return Object.freeze({ kind: 'pointer', phase: event.phase, sequence: event.sequence, x: event.x, y: event.y, button: event.button, modifiers: Object.freeze(modifiers) }); } if (event.kind !== 'keyboard' || !['down', 'up'].includes(event.phase)) throw new TypeError('Invalid OpenFX keyboard event'); return Object.freeze({ kind: 'keyboard', phase: event.phase, sequence: event.sequence, key: nativeBoundedText(event.key, 64, 'OpenFX key'), code: nativeBoundedText(event.code, 64, 'OpenFX code'), modifiers: Object.freeze(modifiers) }); }); return Object.freeze({ protocolVersion: 1, pluginHandle: opaqueId(value.pluginHandle, 40), context: value.context, target: value.target, parameterName: value.parameterName, events: Object.freeze(admitted) }); }
function nativeOpenFxInteractResult(value, request) { nativeRecord(value, ['protocolVersion', 'width', 'height', 'rowBytes', 'target', 'parameterName', 'acceptedSequences', 'redrawRequested', 'rgba'], 'OpenFX Interact result'); const sequences = nativeArray(value.acceptedSequences, 256, 'OpenFX accepted sequences'); const requested = new Set(request.events.map((event) => event.sequence)); if (value.protocolVersion !== 1 || value.width !== 64 || value.height !== 64 || value.rowBytes !== 256 || value.target !== request.target || value.parameterName !== request.parameterName || typeof value.redrawRequested !== 'boolean' || Reflect.ownKeys(sequences).length !== sequences.length + 1 || sequences.some((sequence, index) => !Number.isSafeInteger(sequence) || sequence < 0 || (index > 0 && sequence <= sequences[index - 1]) || !requested.has(sequence))) throw new TypeError('Invalid OpenFX Interact result'); const rgba = binary(value.rgba); if (rgba.byteLength !== 64 * 64 * 4) throw new TypeError('Invalid OpenFX Interact RGBA surface'); return Object.freeze({ protocolVersion: 1, width: 64, height: 64, rowBytes: 256, target: value.target, parameterName: value.parameterName, acceptedSequences: Object.freeze([...sequences]), redrawRequested: value.redrawRequested, rgba }); }
function nativeOpenFxParameter(value, label = 'OpenFX parameter state') { nativeRecord(value, ['name', 'type', 'value', 'keyframes'], label); const name = nativeBoundedText(value.name, 64, 'OpenFX parameter name'); if (!/^[A-Za-z_][A-Za-z\d_]{0,63}$/u.test(name) || !NATIVE_OPENFX_PARAMETER_TYPES.includes(value.type)) throw new TypeError(`Invalid ${label}`); const valueless = ['group', 'page', 'pushbutton'].includes(value.type), counts = { double: 1, integer2d: 2, double2d: 2, integer3d: 3, double3d: 3, rgb: 3, rgba: 4 }; if (valueless ? value.value !== null : value.type === 'boolean' ? typeof value.value !== 'boolean' : ['integer', 'choice'].includes(value.type) ? !Number.isSafeInteger(value.value) : ['string', 'custom'].includes(value.type) ? typeof value.value !== 'string' || utf8Bytes(value.value, value.type === 'custom' ? 65536 : 4096) > (value.type === 'custom' ? 65536 : 4096) : value.type === 'parametric' ? !Array.isArray(value.value) || value.value.length > 8192 || Reflect.ownKeys(value.value).length !== value.value.length + 1 || value.value.some((point) => !Array.isArray(point) || point.length !== 2 || Reflect.ownKeys(point).length !== 3 || point.some((item) => typeof item !== 'number' || !Number.isFinite(item))) : !Array.isArray(value.value) || value.value.length !== counts[value.type] || Reflect.ownKeys(value.value).length !== value.value.length + 1 || value.value.some((item) => typeof item !== 'number' || !Number.isFinite(item))) throw new TypeError(`Invalid ${label} value`); const keyframes = nativeArray(value.keyframes, 8192, 'OpenFX keyframes'); if (Reflect.ownKeys(keyframes).length !== keyframes.length + 1 || valueless && keyframes.length) throw new TypeError(`Invalid ${label} keyframes`); let previous = -1; for (const keyframe of keyframes) { nativeRecord(keyframe, ['frame', 'value'], 'OpenFX keyframe'); if (!Number.isSafeInteger(keyframe.frame) || keyframe.frame < 0 || keyframe.frame <= previous || typeof keyframe.value !== 'number' || !Number.isFinite(keyframe.value)) throw new TypeError('Invalid OpenFX keyframe'); previous = keyframe.frame; } return nativeDeepFreeze(structuredClone(value)); }
function nativeOpenFxEffect(value) { nativeRecord(value, ['schemaVersion', 'instanceId', 'pluginId', 'binarySha256', 'context', 'attachment', 'inputs', 'parameters', 'customEncodings', 'enabled', 'freshness', 'frozenFallback'], 'authored OpenFX effect'); const id = (entry, label) => { if (typeof entry !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9 ._:-]{0,127}$/u.test(entry)) throw new TypeError(`Invalid authored OpenFX ${label}`); return entry; }, freshness = (entry, label) => { nativeRecord(entry, ['authoredStateSha256', 'inputIdentitiesSha256', 'renderPlanFingerprintSha256', 'nativeEffectFingerprintSha256'], label); for (const digest of Object.values(entry)) nativeDigest(digest, label); }; if (value.schemaVersion !== 1 || !NATIVE_OPENFX_CONTEXTS.includes(value.context) || typeof value.enabled !== 'boolean') throw new TypeError('Invalid authored OpenFX effect'); id(value.instanceId, 'instance'); id(value.pluginId, 'plug-in'); nativeDigest(value.binarySha256, 'OpenFX binary'); nativeRecord(value.attachment, ['kind', 'targetId'], 'authored OpenFX attachment'); if (value.attachment.kind !== value.context) throw new TypeError('Invalid authored OpenFX attachment'); id(value.attachment.targetId, 'attachment target'); const inputs = nativeArray(value.inputs, 16, 'OpenFX inputs'), inputNames = new Set(); if (Reflect.ownKeys(inputs).length !== inputs.length + 1) throw new TypeError('Invalid authored OpenFX inputs'); for (const input of inputs) { nativeRecord(input, ['name', 'sourceRef'], 'authored OpenFX input'); const name = nativeBoundedText(input.name, 64, 'OpenFX input name'); if (!/^[A-Za-z_][A-Za-z\d_]{0,63}$/u.test(name) || inputNames.has(name)) throw new TypeError('Invalid authored OpenFX input'); inputNames.add(name); id(input.sourceRef, 'input reference'); } const parameters = nativeArray(value.parameters, 4096, 'OpenFX parameters'), parameterTypes = new Map(); if (Reflect.ownKeys(parameters).length !== parameters.length + 1) throw new TypeError('Invalid authored OpenFX parameters'); for (const parameter of parameters) { const admitted = nativeOpenFxParameter(parameter); if (parameterTypes.has(admitted.name)) throw new TypeError('Duplicate authored OpenFX parameter'); parameterTypes.set(admitted.name, admitted.type); } nativeRecord(value.customEncodings, Object.keys(value.customEncodings ?? {}), 'OpenFX custom encodings'); let encodingBytes = 0; for (const [name, encoding] of Object.entries(value.customEncodings)) { if (parameterTypes.get(name) !== 'custom' || typeof encoding !== 'string' || (encodingBytes += utf8Bytes(encoding, 65536)) > 65536) throw new TypeError('Invalid OpenFX custom encoding'); } freshness(value.freshness, 'OpenFX freshness'); if (value.frozenFallback !== null) { nativeRecord(value.frozenFallback, ['externalMediaSourceId', 'renderedAssetSha256', 'frameCount', 'freshness'], 'OpenFX frozen fallback'); id(value.frozenFallback.externalMediaSourceId, 'frozen source'); nativeDigest(value.frozenFallback.renderedAssetSha256, 'frozen render'); if (!Number.isSafeInteger(value.frozenFallback.frameCount) || value.frozenFallback.frameCount < 1) throw new TypeError('Invalid OpenFX frozen fallback'); freshness(value.frozenFallback.freshness, 'OpenFX frozen freshness'); } return nativeDeepFreeze(structuredClone(value)); }
function nativeOpenFxAuthoredInteractRequest(value) { nativeRecord(value, ['protocolVersion', 'project', 'pluginHandle', 'effect', 'effectStateSha256', 'context', 'target', 'parameterName', 'events'], 'authored OpenFX Interact request'); const projectIdentity = nativeProjectIdentity(value.project, 'OpenFX Interact', 'framescaper'); nativeRecord(value.project, ['schemaFamily', 'schemaVersion', 'id', 'revision'], 'OpenFX Interact project'); if (typeof value.project.id !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9 ._:-]{0,127}$/u.test(value.project.id) || !Number.isSafeInteger(value.project.revision) || value.project.revision < 0) throw new TypeError('Invalid OpenFX Interact project'); const effect = nativeOpenFxEffect(value.effect), action = nativeOpenFxInteractRequest({ protocolVersion: value.protocolVersion, pluginHandle: value.pluginHandle, context: value.context, target: value.target, parameterName: value.parameterName, events: value.events }); if (nativeDigest(value.effectStateSha256, 'OpenFX effect state') !== value.effectStateSha256 || action.context !== effect.context || action.target === 'custom-parameter' && !effect.parameters.some((parameter) => parameter.name === action.parameterName && parameter.type === 'custom')) throw new TypeError('Invalid authored OpenFX Interact authority'); return nativeDeepFreeze({ protocolVersion: 1, project: { ...projectIdentity, id: value.project.id, revision: value.project.revision }, pluginHandle: action.pluginHandle, effect, effectStateSha256: value.effectStateSha256, context: action.context, target: action.target, parameterName: action.parameterName, events: action.events }); }
function nativeOpenFxAuthoredInteractResult(value, request) { nativeRecord(value, ['protocolVersion', 'project', 'instanceId', 'effectStateSha256', 'width', 'height', 'rowBytes', 'target', 'parameterName', 'acceptedSequences', 'redrawRequested', 'surfaceDisposition', 'parameterMutations', 'rgba'], 'authored OpenFX Interact result'); const projectIdentity = nativeProjectIdentity(value.project, 'OpenFX Interact result', 'framescaper'); nativeRecord(value.project, ['schemaFamily', 'schemaVersion', 'id', 'revision'], 'OpenFX Interact result project'); const base = nativeOpenFxInteractResult({ protocolVersion: value.protocolVersion, width: value.width, height: value.height, rowBytes: value.rowBytes, target: value.target, parameterName: value.parameterName, acceptedSequences: value.acceptedSequences, redrawRequested: value.redrawRequested, rgba: value.rgba }, request); if (value.project.id !== request.project.id || value.project.revision !== request.project.revision || projectIdentity.schemaFamily !== request.project.schemaFamily || projectIdentity.schemaVersion !== request.project.schemaVersion || value.instanceId !== request.effect.instanceId || value.effectStateSha256 !== request.effectStateSha256 || !['drawn', 'retained'].includes(value.surfaceDisposition)) throw new TypeError('Invalid OpenFX Interact result authority'); const mutations = nativeArray(value.parameterMutations, 4096, 'OpenFX parameter mutations'), names = new Set(); if (Reflect.ownKeys(mutations).length !== mutations.length + 1) throw new TypeError('Invalid OpenFX Interact mutations'); const admitted = mutations.map((mutation) => { nativeRecord(mutation, ['parameter'], 'OpenFX parameter mutation'); const parameter = nativeOpenFxParameter(mutation.parameter, 'OpenFX parameter mutation'); const authored = request.effect.parameters.find((candidate) => candidate.name === parameter.name); if (!authored || authored.type !== parameter.type || names.has(parameter.name) || request.context === 'retimer' && parameter.name === 'SourceTime' || request.context === 'transition' && parameter.name === 'Transition') throw new TypeError('Invalid OpenFX Interact parameter mutation authority'); names.add(parameter.name); return nativeDeepFreeze({ parameter }); }); if (value.surfaceDisposition === 'retained' && base.rgba.some((byte) => byte !== 0)) throw new TypeError('Invalid retained OpenFX Interact surface'); return nativeDeepFreeze({ protocolVersion: 1, project: { ...projectIdentity, id: value.project.id, revision: value.project.revision }, instanceId: value.instanceId, effectStateSha256: value.effectStateSha256, width: 64, height: 64, rowBytes: 256, target: base.target, parameterName: base.parameterName, acceptedSequences: base.acceptedSequences, redrawRequested: base.redrawRequested, surfaceDisposition: value.surfaceDisposition, parameterMutations: admitted, rgba: base.rgba }); }
function nativeDeepFreeze(value) { if (value && typeof value === 'object' && !ArrayBuffer.isView(value) && !Object.isFrozen(value)) { for (const child of Object.values(value)) nativeDeepFreeze(child); Object.freeze(value); } return value; } function nativeRecord(value, fields, label) { const keys = value && typeof value === 'object' && !Array.isArray(value) ? Reflect.ownKeys(value) : []; if (keys.length !== fields.length || keys.some((key) => { if (typeof key !== 'string' || !fields.includes(key)) return true; const descriptor = Object.getOwnPropertyDescriptor(value, key); return !descriptor?.enumerable || !Object.hasOwn(descriptor, 'value'); })) throw new TypeError(`Invalid ${label} fields`); return value; } function nativeProjectIdentity(value, label, expectedFamily = null) { if (!value || typeof value !== 'object' || Array.isArray(value)) throw new TypeError(`Invalid ${label} project identity`); const familyDescriptor = Object.getOwnPropertyDescriptor(value, 'schemaFamily'), versionDescriptor = Object.getOwnPropertyDescriptor(value, 'schemaVersion'); if (!familyDescriptor?.enumerable || !Object.hasOwn(familyDescriptor, 'value') || !versionDescriptor?.enumerable || !Object.hasOwn(versionDescriptor, 'value')) throw new TypeError(`Invalid ${label} project identity properties`); const schemaFamily = familyDescriptor.value, schemaVersion = versionDescriptor.value; if (!['soundscaper', 'framescaper'].includes(schemaFamily) || !Number.isSafeInteger(schemaVersion) || schemaVersion < 1) throw new RangeError(`Invalid ${label} project identity`); if (expectedFamily !== null && (schemaFamily !== expectedFamily || schemaVersion !== 1)) throw new RangeError(`Unsupported ${label} project identity`); return Object.freeze({ schemaFamily, schemaVersion }); }
function nativeArray(value, maximum, label) { if (!Array.isArray(value) || value.length > maximum) throw new TypeError(`Invalid Framescaper native ${label}`); return value; } function nativeTierControls(value) { return Object.freeze({ probeHelperEnabled: value?.probeHelperEnabled === true, probeHelperQuarantined: value?.probeHelperQuarantined === true, audioHelperEnabled: value?.audioHelperEnabled === true, audioHelperQuarantined: value?.audioHelperQuarantined === true, nativeEffectDiscoveryEnabled: value?.nativeEffectDiscoveryEnabled === true }); }
function desktopAudioCodecCapabilityTuple(value) { nativeRecord(value, ['operation', 'format', 'sampleRate', 'channelCount', 'settings'], 'desktop audio codec capability tuple'); const formats = ['flac', 'mp3', 'ogg-vorbis', 'opus', 'wavpack', 'mp2', 'aac-m4a']; if (!['audio-decode', 'audio-encode'].includes(value.operation) || !formats.includes(value.format) || !Number.isSafeInteger(value.sampleRate) || value.sampleRate < 8_000 || value.sampleRate > 192_000 || !Number.isSafeInteger(value.channelCount) || value.channelCount < 1 || value.channelCount > 8) throw new TypeError('Invalid desktop audio codec capability tuple'); let settings; try { settings = desktopAudioCodecSettings(value.operation, value.format, value.settings, value.operation === 'audio-decode' ? null : 48_000, value.operation === 'audio-decode' ? null : 2); } catch { throw new TypeError('Invalid desktop audio codec capability tuple settings'); } return Object.freeze({ operation: value.operation, format: value.format, sampleRate: value.sampleRate, channelCount: value.channelCount, settings }); } function desktopAudioCodecCapabilityQuery(value) { nativeRecord(value, ['schemaVersion', 'operations'], 'desktop audio codec capability query'); const source = nativeArray(value.operations, 14, 'audio codec capability operations'), operations = Object.freeze(source.map(desktopAudioCodecCapabilityTuple)), identities = operations.map((entry) => `${entry.operation}:${entry.format}:${entry.sampleRate}:${entry.channelCount}:${JSON.stringify(entry.settings)}`); if (value.schemaVersion !== 2 || !operations.length || Reflect.ownKeys(source).length !== source.length + 1 || new Set(identities).size !== identities.length) throw new TypeError('Invalid desktop audio codec capability query'); return Object.freeze({ schemaVersion: 2, operations }); } function desktopAudioCodecCapabilityResult(value, query) { nativeRecord(value, ['schemaVersion', 'capabilities'], 'desktop audio codec capability result'); const source = nativeArray(value.capabilities, 14, 'audio codec capabilities'); if (value.schemaVersion !== 2 || source.length !== query.operations.length || Reflect.ownKeys(source).length !== source.length + 1) throw new TypeError('Invalid desktop audio codec capability result correlation'); const capabilities = Object.freeze(source.map((candidate, index) => { nativeRecord(candidate, ['operation', 'format', 'sampleRate', 'channelCount', 'settings', 'available', 'provider', 'reason'], 'desktop audio codec capability result entry'); const tuple = desktopAudioCodecCapabilityTuple({ operation: candidate.operation, format: candidate.format, sampleRate: candidate.sampleRate, channelCount: candidate.channelCount, settings: candidate.settings }), expected = query.operations[index], providers = ['bundled', 'operating-system', 'external-ffmpeg'], reasons = ['configure-external-ffmpeg', 'unsupported-by-configured-ffmpeg', 'unsupported-settings']; if (tuple.operation !== expected.operation || tuple.format !== expected.format || tuple.sampleRate !== expected.sampleRate || tuple.channelCount !== expected.channelCount || JSON.stringify(tuple.settings) !== JSON.stringify(expected.settings) || typeof candidate.available !== 'boolean' || (candidate.available ? !providers.includes(candidate.provider) || candidate.reason !== null : candidate.provider !== null || !reasons.includes(candidate.reason))) throw new TypeError('Invalid desktop audio codec capability result authority'); return Object.freeze({ ...tuple, available: candidate.available, provider: candidate.provider, reason: candidate.reason }); })); return Object.freeze({ schemaVersion: 2, capabilities }); } function desktopAudioCodecRequest(value) { const fields = ['operation', 'format', 'input', 'sampleRate', 'channelCount', 'settings', 'maximumOutputBytes', 'requestId']; nativeRecord(value, fields, 'desktop audio codec request'); const operation = value.operation, format = value.format, formats = ['flac', 'mp3', 'ogg-vorbis', 'opus', 'wavpack', 'mp2', 'aac-m4a'], input = binary(value.input), integer = (candidate, minimum, maximum, label) => { if (!Number.isSafeInteger(candidate) || candidate < minimum || candidate > maximum) throw new RangeError(`Invalid desktop audio codec ${label}`); return candidate; }; if (!['audio-decode', 'audio-encode'].includes(operation) || !formats.includes(format) || input.byteLength < 1 || input.byteLength > 32 * 1024 * 1024) throw new TypeError('Invalid desktop audio codec request'); const decode = operation === 'audio-decode', sampleRate = decode ? null : integer(value.sampleRate, 8_000, 192_000, 'sample rate'), channelCount = decode ? null : integer(value.channelCount, 1, format === 'mp3' || format === 'mp2' ? 2 : 8, 'channel count'), maximumOutputBytes = integer(value.maximumOutputBytes, 1, 128 * 1024 * 1024, 'output bound'), settings = desktopAudioCodecSettings(operation, format, value.settings, sampleRate, channelCount); if (decode && (value.sampleRate !== null || value.channelCount !== null)) throw new TypeError('Invalid desktop audio source geometry authority'); if (!decode) { const rates = format === 'opus' ? [8_000, 12_000, 16_000, 24_000, 48_000] : format === 'mp2' ? [32_000, 44_100, 48_000] : format === 'mp3' ? [8_000, 11_025, 12_000, 16_000, 22_050, 24_000, 32_000, 44_100, 48_000] : format === 'aac-m4a' ? [8_000, 11_025, 12_000, 16_000, 22_050, 24_000, 32_000, 44_100, 48_000, 64_000, 88_200, 96_000] : [8_000, 11_025, 12_000, 16_000, 22_050, 24_000, 32_000, 44_100, 48_000, 88_200, 96_000, 176_400, 192_000]; if (!rates.includes(sampleRate) || input.byteLength % (4 * channelCount)) throw new RangeError('Invalid desktop audio codec PCM geometry'); } return Object.freeze({ operation, format, input, sampleRate, channelCount, settings, maximumOutputBytes, requestId: desktopAudioCodecRequestId(value.requestId) }); }
function desktopAudioCodecSettings(operation, format, value, sampleRate, channelCount) { if (operation === 'audio-decode') { nativeRecord(value, ['sampleFormat'], 'desktop audio codec decode settings'); if (value.sampleFormat !== 'f32le') throw new TypeError('Invalid desktop audio codec decode sample format'); return Object.freeze({ sampleFormat: 'f32le' }); } if (format === 'flac') { nativeRecord(value, ['compressionLevel', 'bitDepth'], 'desktop audio codec FLAC encode settings'); if (!Number.isSafeInteger(value.compressionLevel) || value.compressionLevel < 0 || value.compressionLevel > 12 || ![16, 24].includes(value.bitDepth)) throw new RangeError('Invalid desktop audio codec FLAC encode settings'); return Object.freeze({ compressionLevel: value.compressionLevel, bitDepth: value.bitDepth }); } const field = format === 'wavpack' ? 'compressionLevel' : format === 'ogg-vorbis' ? 'quality' : 'bitrateKbps'; nativeRecord(value, [field], 'desktop audio codec encode settings'); const bitrate = value[field], values = format === 'mp3' ? [32, 40, 48, 56, 64, 80, 96, 112, 128, 160, 192, 224, 256, 320] : format === 'opus' ? [16, 24, 32, 48, 64, 80, 96, 112, 128, 160, 192, 256] : format === 'mp2' ? [32, 48, 56, 64, 80, 96, 112, 128, 160, 192, 224, 256, 320, 384] : [32, 48, 64, 80, 96, 112, 128, 160, 192, 224, 256, 320], mp3Maximum = sampleRate <= 12_000 ? 64 : sampleRate <= 24_000 ? 160 : 320, tupleAdmitted = format !== 'mp3' && format !== 'aac-m4a' || format === 'mp3' && bitrate <= mp3Maximum || format === 'aac-m4a' && bitrate * 1_000 * 1_024 <= 6_144 * channelCount * sampleRate, admitted = field === 'compressionLevel' ? Number.isSafeInteger(bitrate) && bitrate >= 0 && bitrate <= 8 : field === 'quality' ? Number.isSafeInteger(bitrate) && bitrate >= 0 && bitrate <= 10 : Number.isSafeInteger(bitrate) && values.includes(bitrate) && tupleAdmitted; if (!admitted) throw new RangeError('Invalid desktop audio codec encode settings'); return Object.freeze({ [field]: value[field] }); }
function desktopAudioCodecResult(value, request) { nativeRecord(value, ['operation', 'bytes', 'metadata', 'requestId'], 'desktop audio codec result'); const bytes = binary(value.bytes); if (value.operation !== request.operation || value.requestId !== request.requestId || bytes.byteLength < 1 || bytes.byteLength > request.maximumOutputBytes) throw new TypeError('Invalid desktop audio codec result authority'); if (value.operation === 'audio-decode') { const fields = ['kind', 'sourceFormat', 'sampleFormat', 'interleaving', 'sampleRate', 'channelCount', 'frameCount']; nativeRecord(value.metadata, fields, 'desktop audio codec decoded metadata'); const channels = value.metadata.channelCount, maximumChannels = request.format === 'mp3' || request.format === 'mp2' ? 2 : 8; if (value.metadata.kind !== 'decoded-audio' || value.metadata.sourceFormat !== request.format || value.metadata.sampleFormat !== 'f32le' || value.metadata.interleaving !== 'interleaved' || !Number.isSafeInteger(value.metadata.sampleRate) || value.metadata.sampleRate < 8000 || value.metadata.sampleRate > 192000 || !Number.isSafeInteger(channels) || channels < 1 || channels > maximumChannels || value.metadata.frameCount !== bytes.byteLength / (4 * channels) || !Number.isSafeInteger(value.metadata.frameCount)) throw new TypeError('Invalid desktop audio codec decoded metadata'); } else { const fields = ['kind', 'format', 'mimeType', 'fileExtension', 'sampleRate', 'channelCount', 'frameCount', ...(request.format === 'flac' ? ['sourceSampleFormat', 'encodedSampleFormat', 'pcmConversion'] : [])], descriptors = { flac: ['audio/flac', '.flac'], mp3: ['audio/mpeg', '.mp3'], 'ogg-vorbis': ['audio/ogg', '.ogg'], opus: ['audio/ogg', '.opus'], wavpack: ['audio/wavpack', '.wv'], mp2: ['audio/mpeg', '.mp2'], 'aac-m4a': ['audio/mp4', '.m4a'] }, descriptor = descriptors[request.format], encodedSampleFormat = request.format === 'flac' ? `s${request.settings.bitDepth}` : null; nativeRecord(value.metadata, fields, 'desktop audio codec encoded metadata'); if (value.metadata.kind !== 'encoded-audio' || value.metadata.format !== request.format || value.metadata.mimeType !== descriptor[0] || value.metadata.fileExtension !== descriptor[1] || value.metadata.sampleRate !== request.sampleRate || value.metadata.channelCount !== request.channelCount || value.metadata.frameCount !== request.input.byteLength / (4 * request.channelCount) || !Number.isSafeInteger(value.metadata.frameCount) || request.format === 'flac' && (value.metadata.sourceSampleFormat !== 'f32le' || value.metadata.encodedSampleFormat !== encodedSampleFormat || value.metadata.pcmConversion !== `clamp-unit-range-to-signed-${request.settings.bitDepth}`)) throw new TypeError('Invalid desktop audio codec encoded metadata'); } return Object.freeze({ operation: value.operation, bytes, metadata: Object.freeze({ ...value.metadata }), requestId: value.requestId }); }
function desktopAudioCodecRequestId(value) { if (typeof value !== 'string' || !/^desktop-audio-[a-f0-9]{32}$/u.test(value)) throw new TypeError('Invalid desktop audio codec request ID'); return value; } function desktopVideoCapabilities(value) { nativeRecord(value, ['schemaVersion', 'formats'], 'desktop video capabilities'); nativeRecord(value.formats, ['mp4', 'webm'], 'desktop video capability formats'); if (value.schemaVersion !== 1) throw new TypeError('Invalid desktop video capability schema'); const format = (candidate, name) => { nativeRecord(candidate, ['available', 'provider', 'reason'], `desktop video ${name} capability`); const providers = ['bundled', 'operating-system', 'external-ffmpeg']; if (typeof candidate.available !== 'boolean' || candidate.available && (!providers.includes(candidate.provider) || candidate.reason !== null) || !candidate.available && (candidate.provider !== null || typeof candidate.reason !== 'string' || utf8Bytes(candidate.reason, 4096) > 4096)) throw new TypeError(`Invalid desktop video ${name} capability`); return Object.freeze({ available: candidate.available, provider: candidate.provider, reason: candidate.reason }); }; return Object.freeze({ schemaVersion: 1, formats: Object.freeze({ mp4: format(value.formats.mp4, 'MP4'), webm: format(value.formats.webm, 'WebM') }) }); } function desktopVideoPlan(value) { const fields = ['schemaVersion', 'format', 'quality', 'width', 'height', 'frameRate', 'frameCount', 'sampleRate', 'durationFrames', 'videoInputBytes', 'audioInputBytes', 'ringCapacityBytes', 'audioRingCapacityBytes', 'maximumOutputBytes']; nativeRecord(value, fields, 'desktop video operation plan'); nativeRecord(value.frameRate, ['num', 'den'], 'desktop video frame rate'); const integer = (candidate, minimum, maximum, label) => { if (!Number.isSafeInteger(candidate) || candidate < minimum || candidate > maximum) throw new RangeError(`Invalid desktop video ${label}`); return candidate; }, width = integer(value.width, 1, 16384, 'width'), height = integer(value.height, 1, 16384, 'height'), frameCount = integer(value.frameCount, 1, 2000000, 'frame count'), videoInputBytes = integer(value.videoInputBytes, 1, 1024 ** 4, 'video input bytes'), audioInputBytes = value.audioInputBytes === null ? null : integer(value.audioInputBytes, 1, 2 * 1024 ** 3, 'audio input bytes'), ringCapacityBytes = integer(value.ringCapacityBytes, 4096, 8 * 1024 ** 2, 'ring capacity'), audioRingCapacityBytes = value.audioRingCapacityBytes === null ? null : integer(value.audioRingCapacityBytes, 4096, 8 * 1024 ** 2, 'audio ring capacity'); if (value.schemaVersion !== 1 || !['mp4', 'webm'].includes(value.format) || !['draft', 'balanced', 'high'].includes(value.quality) || width * height * 4 * frameCount !== videoInputBytes || (audioInputBytes === null) !== (audioRingCapacityBytes === null)) throw new TypeError('Invalid desktop video operation plan authority'); return Object.freeze({ schemaVersion: 1, format: value.format, quality: value.quality, width, height, frameRate: Object.freeze({ num: integer(value.frameRate.num, 1, Number.MAX_SAFE_INTEGER, 'frame rate numerator'), den: integer(value.frameRate.den, 1, Number.MAX_SAFE_INTEGER, 'frame rate denominator') }), frameCount, sampleRate: integer(value.sampleRate, 8000, 192000, 'sample rate'), durationFrames: integer(value.durationFrames, 1, Number.MAX_SAFE_INTEGER, 'duration frames'), videoInputBytes, audioInputBytes, ringCapacityBytes, audioRingCapacityBytes, maximumOutputBytes: integer(value.maximumOutputBytes, 1, 2 * 1024 ** 3, 'maximum output bytes') }); } function desktopVideoOperationId(value) { if (typeof value !== 'string' || !/^desktop-video-[a-f0-9]{32}$/u.test(value)) throw new TypeError('Invalid desktop video operation ID'); return value; } function desktopVideoIdRequest(value) { nativeRecord(value, ['operationId'], 'desktop video operation request'); return Object.freeze({ operationId: desktopVideoOperationId(value.operationId) }); } function desktopVideoBeginResult(value) { nativeRecord(value, ['operationId'], 'desktop video begin result'); return Object.freeze({ operationId: desktopVideoOperationId(value.operationId) }); } function desktopVideoInputWrite(value) { nativeRecord(value, ['operationId', 'role', 'offset', 'bytes'], 'desktop video input write'); const bytes = binary(value.bytes); if (!['video', 'audio'].includes(value.role) || !Number.isSafeInteger(value.offset) || value.offset < 0 || bytes.byteLength < 1 || bytes.byteLength > 1024 * 1024) throw new TypeError('Invalid desktop video input write'); return Object.freeze({ operationId: desktopVideoOperationId(value.operationId), role: value.role, offset: value.offset, bytes }); } function desktopVideoInputClose(value) { nativeRecord(value, ['operationId', 'role', 'offset'], 'desktop video input close'); if (!['video', 'audio'].includes(value.role) || !Number.isSafeInteger(value.offset) || value.offset < 0) throw new TypeError('Invalid desktop video input close'); return Object.freeze({ operationId: desktopVideoOperationId(value.operationId), role: value.role, offset: value.offset }); } function desktopVideoOffsetResult(value, offset, byteLength) { nativeRecord(value, ['offset'], 'desktop video input acknowledgement'); if (value.offset !== offset + byteLength) throw new Error('Desktop video input acknowledgement drifted'); return Object.freeze({ offset: value.offset }); } function desktopVideoExecuteResult(value) { nativeRecord(value, ['exitCode'], 'desktop video execute result'); if (value.exitCode !== 0) throw new Error('Desktop video execution did not succeed'); return Object.freeze({ exitCode: 0 }); } function desktopVideoStatResult(value) { nativeRecord(value, ['byteLength'], 'desktop video stat result'); if (!Number.isSafeInteger(value.byteLength) || value.byteLength < 1 || value.byteLength > 2 * 1024 ** 3) throw new TypeError('Invalid desktop video stat result'); return Object.freeze({ byteLength: value.byteLength }); } function desktopVideoOutputRead(value) { nativeRecord(value, ['operationId', 'offset', 'maximumBytes'], 'desktop video output read'); if (!Number.isSafeInteger(value.offset) || value.offset < 0 || !Number.isSafeInteger(value.maximumBytes) || value.maximumBytes < 1 || value.maximumBytes > 1024 * 1024) throw new TypeError('Invalid desktop video output read'); return Object.freeze({ operationId: desktopVideoOperationId(value.operationId), offset: value.offset, maximumBytes: value.maximumBytes }); } function desktopVideoReadResult(value, maximumBytes) { const bytes = binary(value); if (bytes.byteLength < 1 || bytes.byteLength > maximumBytes) throw new TypeError('Invalid desktop video output range'); return bytes; }
function nativeTierControlRequest(value) { const action = text(value?.action, 48); const setters = ['set-probe-helper-enabled', 'set-audio-helper-enabled', 'set-native-effect-discovery-enabled']; const clears = ['clear-probe-helper-quarantine', 'clear-audio-helper-quarantine']; if (!setters.includes(action) && !clears.includes(action)) throw new TypeError('Unsupported native-tier control action'); if (setters.includes(action) && typeof value?.enabled !== 'boolean') throw new TypeError('Native-tier enabled value must be a boolean'); return Object.freeze(setters.includes(action) ? { action, enabled: value.enabled } : { action }); }
function connectFramescaperProjectLibrary() { if (framescaperProjectState === 'admitted') return Promise.resolve(FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE); if (framescaperProjectState === 'refused') return Promise.reject(new Error('Framescaper project-library handshake was refused')); framescaperProjectConnection ??= ipcRenderer.invoke(CHANNELS.framescaperProjectHandshake, FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE).then((value) => { try { const admitted = framescaperProjectHandshake(value); framescaperProjectState = 'admitted'; return admitted; } catch (cause) { framescaperProjectState = 'refused'; throw new TypeError('Framescaper project-library handshake was refused', { cause }); } }, (cause) => { framescaperProjectState = 'refused'; throw new TypeError('Framescaper project-library handshake was refused', { cause }); }); return framescaperProjectConnection; }
function invokeFramescaperProject(channel, value) { if (framescaperProjectState !== 'admitted') return Promise.reject(new Error(`Framescaper project-library handshake ${framescaperProjectState === 'refused' ? 'was refused' : 'is required'}`)); return ipcRenderer.invoke(channel, value); }
function framescaperProjectHandshake(value) { const fields = Object.keys(FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE); const candidate = framescaperProjectRecord(value, fields, 'handshake'); const normalized = Object.fromEntries(fields.map((field) => [field, candidate[field]])); if (JSON.stringify(normalized) !== JSON.stringify(FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE)) throw new TypeError('Framescaper project-library generation identity is unsupported'); return FRAMESCAPER_PROJECT_LIBRARY_HANDSHAKE; }
function beginFramescaperProjectPublication(value) { const request = framescaperProjectRecord(value, ['publicationId', 'expectedMetadataRevision', 'expectedProject', 'project', 'bodies'], 'publication begin'); const publicationId = framescaperProjectPublicationId(request.publicationId); if (framescaperProjectPublications.has(publicationId)) throw new Error('Framescaper project-library publication is already active'); const bodies = framescaperProjectArray(request.bodies, 5118, 'publication bodies').map((body) => structuredClone(body)); const normalized = Object.freeze({ publicationId, expectedMetadataRevision: framescaperProjectNonNegative(request.expectedMetadataRevision, 'metadata revision'), expectedProject: structuredClone(request.expectedProject), project: structuredClone(request.project), bodies: Object.freeze(bodies) }); return invokeFramescaperProject(CHANNELS.framescaperProjectBegin, normalized).then((result) => framescaperProjectAdmission(result, publicationId, bodies.length)); }
function framescaperProjectAdmission(value, publicationId, bodyCount) { const result = framescaperProjectRecord(value, ['publicationId', 'maximumChunkBytes', 'bodyCount', 'requiredBodyIndexes'], 'publication admission'); if (result.publicationId !== publicationId || result.maximumChunkBytes !== MAX_CHUNK_BYTES || result.bodyCount !== bodyCount) throw new Error('Framescaper project-library publication admission changed'); const requiredBodyIndexes = Object.freeze(framescaperProjectArray(result.requiredBodyIndexes, bodyCount, 'required publication bodies').map((index) => framescaperProjectNonNegative(index, 'required body index'))); if (requiredBodyIndexes.some((index, position) => index >= bodyCount || position > 0 && index <= requiredBodyIndexes[position - 1])) throw new Error('Framescaper project-library required body indexes changed'); framescaperProjectPublications.add(publicationId); return Object.freeze({ publicationId, maximumChunkBytes: MAX_CHUNK_BYTES, bodyCount, requiredBodyIndexes }); }
function writeFramescaperProjectPublicationChunk(value) { const request = framescaperProjectRecord(value, ['publicationId', 'bodyIndex', 'offset', 'bytes'], 'publication chunk'); const bytes = framescaperProjectChunkResult(request.bytes); if (!bytes.byteLength) throw new RangeError('Framescaper project-library publication chunk is empty'); return invokeFramescaperProject(CHANNELS.framescaperProjectChunk, { publicationId: framescaperProjectActive(request.publicationId), bodyIndex: framescaperProjectNonNegative(request.bodyIndex, 'body index'), offset: framescaperProjectNonNegative(request.offset, 'body offset'), bytes }); }
function finishFramescaperProjectPublication(value, channel, booleanResult) { const request = framescaperProjectRecord(value, ['publicationId'], 'publication completion'); const publicationId = framescaperProjectActive(request.publicationId); return invokeFramescaperProject(channel, { publicationId }).then((result) => { if (booleanResult && typeof result !== 'boolean') throw new TypeError('Framescaper project-library abort acknowledgement is invalid'); framescaperProjectPublications.delete(publicationId); return result; }); }
function framescaperProjectChunkResult(value) { const bytes = binary(value); if (bytes.byteLength > MAX_CHUNK_BYTES) throw new RangeError('Framescaper project-library chunk exceeds its limit'); return bytes; }
function framescaperProjectActive(value) { const publicationId = framescaperProjectPublicationId(value); if (!framescaperProjectPublications.has(publicationId)) throw new Error('Framescaper project-library publication is not active'); return publicationId; } function framescaperProjectPublicationId(value) { if (typeof value !== 'string' || !/^[a-f0-9]{48}$/u.test(value)) throw new TypeError('Framescaper project-library publication id is invalid'); return value; }
function framescaperProjectId(value) { if (typeof value !== 'string' || !/^[\x21-\x7e]{1,256}$/u.test(value)) throw new TypeError('Framescaper project id is invalid'); return value; } function framescaperProjectNonNegative(value, label) { if (typeof value !== 'number' || !Number.isSafeInteger(value) || value < 0) throw new RangeError(`Framescaper project-library ${label} is invalid`); return value; }
function framescaperProjectRecord(value, fields, label) { const keys = value && typeof value === 'object' && !Array.isArray(value) ? Reflect.ownKeys(value) : []; if (keys.length !== fields.length || keys.some((key) => { const descriptor = Object.getOwnPropertyDescriptor(value, key); return typeof key !== 'string' || !fields.includes(key) || !descriptor?.enumerable || !Object.hasOwn(descriptor, 'value'); })) throw new TypeError(`Framescaper project-library ${label} has unsupported fields`); return value; }
function framescaperProjectArray(value, maximum, label) { if (!Array.isArray(value) || value.length > maximum || Reflect.ownKeys(value).length !== value.length + 1) throw new TypeError(`Framescaper project-library ${label} must be a bounded dense array`); return value; }
function strictBoolean(value, message = 'Desktop shared-project delete result must be a boolean') { if (typeof value !== 'boolean') throw new TypeError(message); return value; }
function finalPrefixAcknowledgement(value) { if (typeof value?.byteLength !== 'number') throw new TypeError('Desktop returned an invalid final-prefix acknowledgement');
	const byteLength = saveSize(value.byteLength); if (byteLength < FINAL_PREFIX_BYTES) throw new TypeError('Desktop returned an invalid final-prefix acknowledgement'); return Object.freeze({ byteLength }); }
function utf8Bytes(value, maximumBytes) { let bytes = 0; for (let index = 0; index < value.length; index += 1) { const code = value.charCodeAt(index); if (code <= 0x7f) bytes += 1; else if (code <= 0x7ff) bytes += 2; else if (code >= 0xd800 && code <= 0xdbff && value.charCodeAt(index + 1) >= 0xdc00 && value.charCodeAt(index + 1) <= 0xdfff) { bytes += 4; index += 1; } else bytes += 3; if (bytes > maximumBytes) return bytes; } return bytes; }
function binary(value) { if (value instanceof ArrayBuffer) return new Uint8Array(value.slice(0)); if (ArrayBuffer.isView(value)) return new Uint8Array(value.buffer.slice(value.byteOffset, value.byteOffset + value.byteLength)); throw new TypeError('Expected binary data'); }
function persistentDeliveryInvoke(channel, value) { if (arguments.length < 2) return ipcRenderer.invoke(channel).then(persistentDeliveryResult); persistentDeliveryPathless(value, 'request'); return ipcRenderer.invoke(channel, structuredClone(value)).then(persistentDeliveryResult); }
function persistentDeliveryResult(value) { persistentDeliveryPathless(value, 'response'); return value && typeof value === 'object' ? Object.freeze(structuredClone(value)) : value; }
function persistentDeliveryRecord(value, fields, label) { nativeRecord(value, fields, `persistent delivery ${label}`); persistentDeliveryPathless(value, label); return value; }
function persistentDeliveryOptionalRecord(value, fields, label) { if (!value || typeof value !== 'object' || Array.isArray(value) || Reflect.ownKeys(value).some((key) => typeof key !== 'string' || !fields.includes(key))) throw new TypeError(`Malformed persistent delivery ${label}`); persistentDeliveryPathless(value, label); return value; }
function transferPersistentDeliveryWorkerPort(event) { if (event?.source !== globalThis.window || event?.data?.type !== 'soundscaper-persistent-delivery-worker-connect-v1') return; const ports = Array.from(event.ports ?? []); try { nativeRecord(event.data, ['type', 'request'], 'persistent delivery worker connection'); if (ports.length !== 1 || typeof ports[0]?.close !== 'function') throw new TypeError('Persistent delivery worker connection requires one MessagePort'); const request = persistentDeliveryRecord(event.data.request, ['currentAuthority', 'jobId'], 'worker claim'); ipcRenderer.postMessage(CHANNELS.deliveryWorkerPort, structuredClone(request), ports); } catch { for (const port of ports) port.close?.(); } }
function persistentDeliveryPathless(value, label, seen = new Set()) { if (typeof value === 'string' && persistentDeliveryPathValue(value)) throw new TypeError(`Persistent delivery ${label} must be pathless`); if (!value || typeof value !== 'object' || ArrayBuffer.isView(value) || value instanceof ArrayBuffer) return; if (seen.has(value)) throw new TypeError(`Persistent delivery ${label} must be acyclic`); seen.add(value); for (const key of Reflect.ownKeys(value)) { if (typeof key !== 'string' || /paths?$/iu.test(key)) throw new TypeError(`Persistent delivery ${label} must not carry path fields`); const nested = value[key]; if (key === 'planPayload') { if (typeof nested !== 'string') throw new TypeError(`Persistent delivery ${label} plan must be pathless`); let plan; try { plan = JSON.parse(nested); } catch (error) { throw new TypeError(`Persistent delivery ${label} plan is invalid`, { cause: error }); } persistentDeliveryPathless(plan, `${label} plan`, seen); } persistentDeliveryPathless(nested, label, seen); } seen.delete(value); }
function persistentDeliveryPathValue(value) { return /^(?:\/|[A-Za-z]:[\\/]|\\\\|~[\\/]|file:)/iu.test(value) || /(?:^|[\\/])\.\.?($|[\\/])/u.test(value); }

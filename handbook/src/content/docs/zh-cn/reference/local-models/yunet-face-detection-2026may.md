---
title: "YuNet 人脸检测"
description: "在抽样视频帧中查找人脸和物体，帮助提出新的构图方案。"
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"6229e821e5b7ec8a8b1f7eb76955094d23bb642e459f8a1e73eeb0836416938c","model":"gpt-6-astra","modelProvider":"codex-session","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"6229e821e5b7ec8a8b1f7eb76955094d23bb642e459f8a1e73eeb0836416938c","targetLocale":"zh-CN"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

在抽样视频帧中查找人脸和物体，帮助提出新的构图方案。

YuNet 检测人脸及其位置。主体检测操作将这些结果与 D-FINE 的物体检测结果结合。

## 当前可用性 {#current-availability}

桌面构建包含所需的 onnxruntime-node 1.29.0 引擎，适用于 macOS arm64, Linux arm64, Linux x64, Windows arm64, Windows x64. 通过 Model Manager 安装此模型的权重，然后在本地运行其任务。 这些是受支持的构建目标；特定软件包和设备的结果请参阅夜间测试报告。 The desktop build does not package this 引擎，适用于 macOS x64 even though model weights are listed for those platforms.

## 使用此模型 {#use-this-model}

在具备所需原生运行时的平台上打开 **Framescaper: Effect → Video effects → Reframe**。本地辅助功能在桌面编辑器中运行。

1. 在 Framescaper 中选择一个视频片段。
2. 安装 YuNet 和 D-FINE 进行主体检测。引导式 Reframe 任务还使用 U²-Net-P 显著性检测。
3. 在本地运行并检查建议的裁切。Tools → Advanced Local Processing 可单独运行主体检测。

此操作还需要 [D-FINE Nano object detection](/reference/local-models/dfine-nano-coco/).

## 下载与要求 {#download-and-requirements}

模型标识： `yunet-face-detection-2026may`，版本 **2026.5.0**.

预计下载大小： **224.35 KiB**. 最低系统内存： **2 GiB**. 此目录要求指总内存，而非当前可用内存。推理还需要足够的空闲内存，安装也需要工作空间；内存大小并不保证处理速度。

发布平台： macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64, Windows x64. 还必须有匹配的打包运行时。

在 Windows 上，请安装与桌面应用匹配的最新受支持 [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170)：x64 构建使用 **x64**，ARM64 构建使用 **ARM64**。原生 ONNX 和 Sherpa 引擎需要这些库；模型下载内容不包含它们。使用本应用无需安装 Visual Studio。

Tools → Model Manager 会下载已发布的文件并验证记录的 SHA-256 摘要。处理过程在本地使用已安装文件。打开 Model Manager 中的 Storage and verification，可查看声明、修复安装或更改存储位置。

| 文件 | 预计下载大小 |
| --- | --- |
| face_detection_yunet_2026may.onnx | 224.35 KiB |

## 软件包测试检查的内容 {#what-the-packaged-test-checks}

测试用例： `subject-detection`；操作： `subject-detection`.

输入：两张照片——公共领域的 NASA 宇航员艾琳·柯林斯肖像和 CC0 许可的切尔西猫照片。每张照片分别置于一帧并指定明确的源帧时间，让检测器看到人脸和清晰的物体。

人脸和物体结果必须具有有效的源帧时间、数值有限的置信度分数，以及位于画面内的边界框。主体测试素材必须让两种模型都产生检测结果。

nightly-with-tests 软件包会下载真实模型文件，并通过打包的运行时请求推理。目录中缺少所需模型会使相应测试失败；受支持平台缺少原生引擎也会失败。这些耗时检查独立于常规浏览器测试。通过测试只能确认模型基本运行且输出结构可用，不能证明它在您录音上的感知质量或准确率。

## 检查结果 {#review-the-result}

- 人脸和物体可能漏检或误分类。抽样检测无法保证移动主体始终处于每个建议的裁切范围内。
- 此操作会同时运行两种模型；它们都不是用于确定人物身份的人脸识别系统。

[模型测试覆盖范围与限制](/reference/local-models/) · [本地处理指南](/help/local-processing/)

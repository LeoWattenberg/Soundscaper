---
title: "Room dereverberation"
description: "使用本地去混响模型减少录音语音中的房间反射声。"
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"bd554a1cd332fecfa937182d03f1bde12df41766677969a24297dc1de91ba8c2","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"bd554a1cd332fecfa937182d03f1bde12df41766677969a24297dc1de91ba8c2","targetLocale":"zh-CN"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

使用本地去混响模型减少录音语音中的房间反射声。

## 当前可用性 {#current-availability}

桌面版本为以下平台打包了所需的 onnxruntime-node 1.29.0 引擎：macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64。请通过模型管理器安装此模型的权重，然后在本地运行相应任务。这些是受支持的构建目标；特定软件包和设备的结果请查看 夜间测试报告。

## 使用此模型 {#use-this-model}

在具备所需原生运行时的平台上，打开**工具 → 高级本地处理 → 去混响**。本地辅助处理在桌面编辑器中运行。

1. 选择受房间反射声影响的语音。
2. 通过模型管理器安装此模型并运行去混响。
3. 将处理结果与原始音频比较；只有语音听感改善时才接受。

## 下载与要求 {#download-and-requirements}

模型标识： `dereverb-room`, 版本 **1.0.0**.

下载大小约为： **114.57 MiB**. 最低系统内存： **2 GiB**. 目录中的内存要求指总内存，而非当前可用内存。推理还需要足够的可用内存，安装也需要存储空间；内存大小不代表运行速度。

支持的平台： macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. 还需要匹配的打包运行时。

在 Windows 上，请安装最新受支持的 [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170)，并与桌面应用的架构匹配：**x64**（x64 版本）或**ARM64**（ARM64 版本）。 原生 ONNX 和 Sherpa 引擎需要这些库；模型下载不包含它们。使用应用本身不需要安装 Visual Studio。

通过“工具 → 模型管理器”下载已发布的文件并验证记录的 SHA-256 摘要。处理过程使用本地安装的文件。在模型管理器中打开“存储与验证”，可以查看通知、修复安装或更改存储位置。

| 文件 | 大致下载大小 |
| --- | --- |
| network.onnx | 114.57 MiB |

## 打包测试的检查内容 {#what-the-packaged-test-checks}

用例： `dereverb-room-speech`；操作： `dereverberation`.

输入：肯尼迪演说重采样为 44.1 kHz，并叠加四个可复现的延迟反射声。

解码后的音频必须包含有限采样值、保持预期的音频结构、与输入采样值不同，并具有非零可听信号。仅文件头不同不算作变化。

带测试的夜间软件包会下载真实模型文件，并通过打包运行时执行推理。目录中缺少必需模型时，相应用例会失败；目录支持的平台若缺少原生引擎，也会失败。这些耗时检查与常规浏览器测试套件分开运行。测试通过只能确认模型能够基本运行并产生可用的输出结构，不能证明感知质量，也不能证明模型在你的录音上准确。

## 检查结果 {#review-the-result}

- 强烈的反射声、音乐和陌生的房间环境可能产生伪影或丢失有用细节。
- 音频发生变化且并非静音，只能确认处理已运行，不能证明混响完全消除或听感有所改善。
- 此模型采用 GPL-3.0；重新分发时请保留许可证及对应源代码说明。其干声训练语料和基础检查点来源未知。

[模型测试覆盖范围与限制](/reference/local-models/) · [本地处理指南](/help/local-processing/)

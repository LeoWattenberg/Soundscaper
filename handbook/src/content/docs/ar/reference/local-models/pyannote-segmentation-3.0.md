---
title: "تقسيم Pyannote 3.0"
description: "تجميع الكلام في أدوار للمتحدثين كي تتمكن من مراجعة من يتحدث ومتى."
editUrl: false
---

<!-- docs-ai-provenance: {"model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"139a50078b93c2e56781b181bb48f07513fbe361c5e4f4b9106b1e5063606954","targetLocale":"ar"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

تجميع الكلام في أدوار للمتحدثين كي تتمكن من مراجعة من يتحدث ومتى.

يوفر Pyannote تقسيم الكلام الذي يستخدمه وقت تشغيل تمييز المتحدثين. ويوفر ERes2Net تضمينات المتحدثين المستخدمة لتجميع تلك المقاطع.

## التوفر الحالي {#current-availability}

تتضمن إصدارات سطح المكتب محرك sherpa-onnx-node المطلوب 1.13.5 للمنصات macOS arm64 وLinux arm64 وLinux x64 وWindows arm64 وWindows x64. ثبّت أوزان هذا النموذج عبر مدير النماذج، ثم شغّل مهمته محليًا. هذه أهداف البناء المدعومة؛ راجع تقرير الاختبار الليلي لمعرفة نتائج حزمة وآلة محددتين. لا يحزم إصدار سطح المكتب هذا المحرك لمنصة macOS x64 رغم إدراج أوزان النموذج لتلك المنصات.

## استخدام هذا النموذج {#use-this-model}

افتح **تحليل → الكلام → تمييز المتحدثين** على منصة تتوفر فيها بيئة التشغيل الأصلية المطلوبة. تعمل المساعدة المحلية في محرر سطح المكتب.

1. حدد المحادثة المراد تحليلها.
2. ثبّت كلًا من تقسيم Pyannote وتضمين المتحدث ERes2Net عبر **إدارة النماذج**؛ تستخدم العملية النموذجين.
3. شغّل المعالجة محليًا، وراجع أدوار المتحدثين المقترحة، ثم طبّق النتائج المطلوبة.

تتطلب هذه العملية أيضًا [تضمين المتحدث ERes2Net](/reference/local-models/speech-3d-speaker-eres2net/).

## التنزيل والمتطلبات {#download-and-requirements}

هوية النموذج: `pyannote-segmentation-3.0`، الإصدار **3.0.0**.

حجم التنزيل التقريبي: **5.72 MiB**. ذاكرة النظام الدنيا: **2 GiB**. يشير هذا الشرط الخاص بالفهرس إلى إجمالي الذاكرة، لا إلى الذاكرة المتاحة حاليًا. تحتاج عملية الاستدلال أيضًا إلى ذاكرة متاحة كافية، ويحتاج التثبيت إلى مساحة عمل؛ الذاكرة ليست ضمانًا للسرعة.

المنصات المنشورة: macOS arm64 وmacOS x64 وLinux arm64 وLinux x64 وWindows arm64 وWindows x64. ويلزم أيضًا وجود بيئة تشغيل مضمّنة مطابقة.

في Windows، ثبّت أحدث إصدار مدعوم من [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) المطابق لتطبيق سطح المكتب: **x64** لبناء x64 أو **ARM64** لبناء ARM64. تحتاج محركات ONNX وSherpa الأصلية إلى هذه المكتبات؛ ولا تتضمنها تنزيلات النموذج. لا يلزم Visual Studio نفسه لاستخدام التطبيق.

تنزّل **أدوات → مدير النماذج** العناصر المنشورة وتتحقق من تجزئات SHA-256 المسجلة لها. وتجري المعالجة محليًا باستخدام الملفات المثبّتة. افتح **التخزين والتحقق** في مدير النماذج لفحص الإشعارات أو إصلاح التثبيت أو تغيير موقع التخزين.

| العنصر | حجم التنزيل التقريبي |
| --- | --- |
| model.onnx | 5.72 MiB |
| LICENSE | 1.04 KiB |

## ما الذي يفحصه الاختبار المضمّن في الحزمة {#what-the-packaged-test-checks}

الحالة: `speaker-diarization`؛ العملية: `speaker-diarization`.

المدخل: مقتطف من خطاب التنصيب العام لجون ف. كينيدي، أضيفت ثانية من الصمت إلى كل طرف منه، وجُهّز كصوت أحادي القناة بمعدل 16 كيلوهرتز.

يجب أن تحتوي النتيجة على أدوار للمتحدثين مع معرفات متحدثين وتوقيت صالحين داخل الصوت المدخل. ولا يُشترط عدد محدد من المتحدثين.

تنزّل حزمة الاختبار الليلي مع الاختبارات عناصر النماذج الحقيقية وتطلب الاستدلال عبر بيئة التشغيل المضمّنة. ويؤدي غياب نموذج مطلوب من الكتالوج إلى فشل حالته. كما تفشل محرّكات التنفيذ الأصلية الغائبة على المنصات المدعومة في الكتالوج. تُجرى هذه الفحوص المكلفة بمعزل عن مجموعة اختبارات المتصفح العادية. ويؤكد نجاح التشغيل تنفيذ النموذج الأساسي وبنية مخرجات قابلة للاستخدام؛ لكنه لا يثبت الجودة الإدراكية أو الدقة في تسجيلك.

## مراجعة النتيجة {#review-the-result}

- معرفات المتحدثين مجموعات مستنتجة من التسجيل، وليست هويات مؤكدة لأشخاص حقيقيين.
- قد تُجمّع العبارات القصيرة والأصوات المتداخلة والمتشابهة بطريقة خاطئة. يختبر الاختبار أدوارًا صالحة وغير فارغة، لا عددًا دقيقًا للمتحدثين.

[تغطية اختبار النموذج والقيود](/reference/local-models/) · [دليل المعالجة المحلية](/help/local-processing/)

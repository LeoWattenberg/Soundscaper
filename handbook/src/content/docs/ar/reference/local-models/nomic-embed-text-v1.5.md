---
title: "Nomic Embed Text v1.5"
description: "إنشاء تمثيلات عددية محلية لمقاطع النصوص المفرغة واستعلامات البحث من أجل البحث الدلالي."
editUrl: false
---

<!-- docs-ai-provenance: {"model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"d9b94de80ac885f796c470c742484ecae04ee57417805279597e97868465d84b","targetLocale":"ar"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

إنشاء تمثيلات عددية محلية لمقاطع النصوص المفرغة واستعلامات البحث من أجل البحث الدلالي.

## التوفر الحالي {#current-availability}

تتضمن إصدارات سطح المكتب محرك onnxruntime-node المطلوب 1.29.0 للمنصات macOS arm64 وLinux arm64 وLinux x64 وWindows arm64 وWindows x64. ثبّت أوزان هذا النموذج عبر مدير النماذج، ثم شغّل مهمته محليًا. هذه أهداف البناء المدعومة؛ راجع تقرير الاختبار الليلي لمعرفة نتائج حزمة وآلة محددتين. لا يحزم إصدار سطح المكتب هذا المحرك لمنصة macOS x64 رغم إدراج أوزان النموذج لتلك المنصات.

## استخدام هذا النموذج {#use-this-model}

افتح **الأدوات → البحث → فهرسة النص، ثم الأدوات → البحث → البحث المفهرس** على منصة تتوفر فيها بيئة التشغيل الأصلية المطلوبة. تعمل المساعدة المحلية في محرر سطح المكتب.

1. أنشئ نصًا مفرغًا أو استورده، ثم ثبّت Nomic Embed Text عبر **إدارة النماذج**.
2. شغّل **فهرسة النص** لإعداد النص للبحث الدلالي.
3. استخدم **البحث المفهرس** وراجع المقاطع المطابقة في سياقها. تعرض **المعالجة المحلية المتقدمة** عملية التضمين أيضًا.

## التنزيل والمتطلبات {#download-and-requirements}

هوية النموذج: `nomic-embed-text-v1.5`، الإصدار **1.5.0**.

حجم التنزيل التقريبي: **131.62 MiB**. ذاكرة النظام الدنيا: **4 GiB**. يشير هذا الشرط الخاص بالفهرس إلى إجمالي الذاكرة، لا إلى الذاكرة المتاحة حاليًا. تحتاج عملية الاستدلال أيضًا إلى ذاكرة متاحة كافية، ويحتاج التثبيت إلى مساحة عمل؛ الذاكرة ليست ضمانًا للسرعة.

المنصات المنشورة: macOS arm64 وmacOS x64 وLinux arm64 وLinux x64 وWindows arm64 وWindows x64. ويلزم أيضًا وجود بيئة تشغيل مضمّنة مطابقة.

في Windows، ثبّت أحدث إصدار مدعوم من [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) المطابق لتطبيق سطح المكتب: **x64** لبناء x64 أو **ARM64** لبناء ARM64. تحتاج محركات ONNX وSherpa الأصلية إلى هذه المكتبات؛ ولا تتضمنها تنزيلات النموذج. لا يلزم Visual Studio نفسه لاستخدام التطبيق.

تنزّل **أدوات → مدير النماذج** العناصر المنشورة وتتحق من تجزئات SHA-256 المسجلة لها. وتجري المعالجة محليًا باستخدام الملفات المثبّتة. افتح **التخزين والتحقق** في مدير النماذج لفحص الإشعارات أو إصلاح التثبيت أو تغيير موقع التخزين.

| العنصر | حجم التنزيل التقريبي |
| --- | --- |
| model_quantized.onnx | 130.94 MiB |
| tokenizer.json | 694.72 KiB |
| tokenizer_config.json | 1.16 KiB |
| special_tokens_map.json | 695 bytes |
| config.json | 2.48 KiB |

## ما الذي يفحصه الاختبار المضمّن في الحزمة {#what-the-packaged-test-checks}

الحالة: `nomic-text-embedding`؛ العملية: `text-embedding`.

المدخل: مقطع نصي قصير غير فارغ يُمرَّر إلى محلل الرموز الفعلي وشبكة التضمين.

يجب أن تكون لمتجهات التضمين الأبعاد المتوقعة وأن تحتوي على قيم منتهية وغير صفرية وغير ثابتة.

تنزّل حزمة الاختبار الليلي مع الاختبارات عناصر النماذج الحقيقية وتطلب الاستدلال عبر بيئة التشغيل المضمّنة. ويؤدي غياب نموذج مطلوب من الكتالوج إلى فشل حالته. كما تفشل محرّكات التنفيذ الأصلية الغائبة على المنصات المدعومة في الكتالوج. تُجرى هذه الفحوص المكلفة بمعزل عن مجموعة اختبارات المتصفح العادية. ويؤكد نجاح التشغيل تنفيذ النموذج الأساسي وبنية مخرجات قابلة للاستخدام؛ لكنه لا يثبت الجودة الإدراكية أو الدقة في تسجيلك.

## مراجعة النتيجة {#review-the-result}

- التضمينات بيانات عددية للبحث، وليست نصًا مفرغًا معاد الصياغة أو إجابة مولدة.
- قد يختلف ترتيب المقاطع ذات الصلة بين عمليات التشغيل أو التسجيلات. تتحقق المتجهات المنتهية وغير الثابتة من التنفيذ، لا من صلة كل نتيجة.

[تغطية اختبار النموذج والقيود](/reference/local-models/) · [دليل المعالجة المحلية](/help/local-processing/)

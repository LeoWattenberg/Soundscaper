---
title: "Beat This final"
description: "使用更大的可選 Beat This 模型查找音樂節拍。"
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","targetLocale":"zh-TW"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

使用更大的可選 Beat This 模型查找音樂節拍。

## 當前可用性 {#current-availability}

桌面版本為以下平台打包了所需的 onnxruntime-node 1.29.0 引擎：macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64。請通過模型管理器安裝此模型的權重，然後在本地運行相應任務。這些是受支持的構建目標；特定軟件包和設備的結果請查看 nightly 測試報告。

## 使用此模型 {#use-this-model}

打開 **Tools → Advanced Local Processing → Beat tracking**，並確保平台具備所需的原生運行時。 本地輔助功能在桌面編輯器中運行。

1. Select music with a clear rhythmic passage.
2. Install Beat This final through Model Manager and choose its exact model identity.
3. Review the proposed beats and tempo against the music before accepting them.

## 下載與要求 {#download-and-requirements}

模型標識： `beat-this-final0`, 版本 **1.1.0**.

下載大小約為： **79.32 MiB**. 最低系統內存： **4 GiB**. 目錄中的內存要求指總內存，而非當前可用內存。推理還需要足夠的可用內存，安裝也需要存儲空間；內存大小不代表運行速度。

支持的平台： macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. 還需要匹配的打包運行時。

在 Windows 上，請安裝最新受支持的 [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170)，並與桌面應用的架構匹配：**x64**（x64 版本）或**ARM64**（ARM64 版本）。 原生 ONNX 和 Sherpa 引擎需要這些庫；模型下載不包含它們。使用應用本身不需要安裝 Visual Studio。

通過“工具 → 模型管理器”下載已發佈的文件並驗證記錄的 SHA-256 摘要。處理過程使用本地安裝的文件。在模型管理器中打開“存儲與驗證”，可以查看通知、修復安裝或更改存儲位置。

| 文件 | 大致下載大小 |
| --- | --- |
| beat-this-final0.onnx | 79.32 MiB |

## 打包測試的檢查內容 {#what-the-packaged-test-checks}

用例： `beat-this-final-beats`；操作： `beat-tracking`.

輸入： Sixteen seconds of reproducible synthesized percussion and changing tones at 22.05 kHz, with a regular beat.

節拍位置必須非空、按順序排列且位於音頻範圍內；速度數據必須是有限正數。

帶測試的 nightly 軟件包會下載真實模型文件，並通過打包運行時執行推理。目錄中缺少必需模型時，該測試用例會失敗；目錄支持的平台缺少原生引擎時也會失敗。這些耗時檢查與常規瀏覽器測試套件分開運行。測試通過只能確認模型能夠基本運行並產生可用的輸出結構，不能證明感知質量，也不能證明在你的錄音上準確。

## 檢查結果 {#review-the-result}

- 更大的模型需要更多內存，也不能保證改善每一段錄音。
- This model has its own real inference case. Its test cannot be satisfied by running the smaller model instead.

[模型測試覆蓋範圍與限制](/reference/local-models/) · [本地處理指南](/help/local-processing/)

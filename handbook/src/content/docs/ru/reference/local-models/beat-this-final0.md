---
title: "Beat This final"
description: "Находите музыкальные доли с помощью более крупной необязательной модели Beat This."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","model":"gpt-6-astra","modelProvider":"codex-session","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","targetLocale":"ru"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Находите музыкальные доли с помощью более крупной необязательной модели Beat This.

## Текущая доступность {#current-availability}

Настольные сборки включают необходимый движок onnxruntime-node 1.29.0 для macOS arm64, Linux x64, Linux arm64, Windows x64 и Windows arm64. Установите веса модели через Менеджер моделей, затем запустите её задачу локально. Это поддерживаемые целевые платформы сборки; результаты для конкретного пакета и компьютера смотрите в отчёте ночных тестов.

## Использование модели {#use-this-model}

На платформе с необходимой встроенной средой откройте **Инструменты → Расширенная локальная обработка → Поиск долей**. Локальные вспомогательные функции работают в настольном редакторе.

1. Выделите музыкальный фрагмент с чётким ритмом.
2. Установите Beat This final через Менеджер моделей и выберите её точный идентификатор.
3. Сверьте предложенные доли и темп с музыкой, прежде чем принять их.

## Загрузка и требования {#download-and-requirements}

Идентификатор модели: `beat-this-final0`, версия **1.1.0**.

Приблизительный размер загрузки: **79.32 MiB**. Минимальный объём системной памяти: **4 GiB**. Это требование каталога относится к общему объёму памяти, а не к свободной памяти в данный момент. Для вывода модели также требуется достаточно свободной памяти, а для установки — рабочее место на диске; объём памяти не гарантирует скорость.

Опубликованные платформы: macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. Также необходима соответствующая встроенная среда выполнения.

В Windows установите последнюю поддерживаемую версию [распространяемого пакета Microsoft Visual C++ v14](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170), соответствующую настольному приложению: **x64** для сборки x64 или **ARM64** для сборки ARM64. Встроенным движкам ONNX и Sherpa нужны эти библиотеки; они не входят в загрузку модели. Для использования приложения сама Visual Studio не нужна.

Инструменты → Менеджер моделей загружает опубликованные артефакты и проверяет записанные для них хеши SHA-256. Обработка использует установленные файлы локально. Откройте раздел «Хранение и проверка» в Менеджере моделей, чтобы просмотреть уведомления, восстановить установку или изменить место хранения.

| Артефакт | Приблизительный размер загрузки |
| --- | --- |
| beat-this-final0.onnx | 79.32 MiB |

## Что проверяет тест пакета {#what-the-packaged-test-checks}

Случай: `beat-this-final-beats`; операция: `beat-tracking`.

Входные данные: шестнадцать секунд воспроизводимого синтезированного ударного ритма и меняющихся тонов с равномерными долями при 22.05 kHz.

Позиции долей должны быть заданы, идти по порядку и находиться в пределах аудио; данные о темпе должны быть конечными положительными числами.

Ночной пакет с тестами загружает настоящие артефакты модели и запрашивает вывод через встроенную среду выполнения. Если обязательная модель отсутствует в каталоге, её тест завершается неудачей. Отсутствие встроенного движка приводит к неудаче на поддерживаемых каталогом платформах. Эти ресурсоёмкие проверки выполняются отдельно от обычного набора браузерных тестов. Успешный запуск подтверждает базовую работу модели и пригодную структуру вывода; он не доказывает качество восприятия или точность на вашей записи.

## Проверка результата {#review-the-result}

- Более крупная модель требует больше памяти и не гарантирует улучшения для каждой записи.
- Для этой модели предусмотрен собственный тест реального вывода. Его нельзя пройти, запустив вместо неё меньшую модель.

[Покрытие и ограничения тестов моделей](/reference/local-models/) · [Руководство по локальной обработке](/help/local-processing/)

---
title: "ERes2Net speaker embedding"
description: "Разделяйте речь на фрагменты по говорящим, чтобы проверить, кто и когда говорит."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"67c2a19399ca004c7911e433baf3db62a74337b962fa6ee586844d0e7f360667","model":"gpt-6-astra","modelProvider":"codex-session","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"67c2a19399ca004c7911e433baf3db62a74337b962fa6ee586844d0e7f360667","targetLocale":"ru"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Разделяйте речь на фрагменты по говорящим, чтобы проверить, кто и когда говорит.

ERes2Net преобразует фрагменты речи в числовые представления говорящих. Среда определения говорящих объединяет их с сегментацией Pyannote, чтобы разбить запись на реплики.

## Текущая доступность {#current-availability}

Настольные сборки включают необходимый движок sherpa-onnx-node 1.13.5 для macOS arm64, Linux arm64, Linux x64, Windows arm64 и Windows x64. Установите веса модели через Менеджер моделей, затем запустите её задачу локально. Это поддерживаемые целевые платформы сборки; результаты для конкретного пакета и компьютера смотрите в отчёте ночных тестов. Настольная сборка не включает этот движок для macOS x64, хотя веса модели указаны для этой платформы.

## Использование модели {#use-this-model}

На платформе с необходимой встроенной средой откройте **Анализ → Речь → Определить говорящих**. Локальные вспомогательные функции работают в настольном редакторе.

1. Выделите разговор для анализа.
2. Установите обе модели — Pyannote segmentation и ERes2Net speaker embedding — через Менеджер моделей; операция использует их вместе.
3. Запустите локальную обработку, проверьте предложенные фрагменты речи по говорящим и примените нужные результаты.

Для этой операции также требуется [сегментация Pyannote 3.0](/reference/local-models/pyannote-segmentation-3.0/).

## Загрузка и требования {#download-and-requirements}

Идентификатор модели: `speech-3d-speaker-eres2net`, версия **1.0.0**.

Приблизительный размер загрузки: **25.26 MiB**. Минимальный объём системной памяти: **2 GiB**. Это требование каталога относится к общему объёму памяти, а не к свободной памяти в данный момент. Для вывода модели также требуется достаточно свободной памяти, а для установки — рабочее место на диске; объём памяти не гарантирует скорость.

Опубликованные платформы: macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64, Windows x64. Также необходима соответствующая встроенная среда выполнения.

В Windows установите последнюю поддерживаемую версию [распространяемого пакета Microsoft Visual C++ v14](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170), соответствующую настольному приложению: **x64** для сборки x64 или **ARM64** для сборки ARM64. Встроенным движкам ONNX и Sherpa нужны эти библиотеки; они не входят в загрузку модели. Для использования приложения сама Visual Studio не нужна.

Инструменты → Менеджер моделей загружает опубликованные артефакты и проверяет записанные для них хеши SHA-256. Обработка использует установленные файлы локально. Откройте раздел «Хранение и проверка» в Менеджере моделей, чтобы просмотреть уведомления, восстановить установку или изменить место хранения.

| Артефакт | Приблизительный размер загрузки |
| --- | --- |
| 3dspeaker_speech_eres2net_sv_en_voxceleb_16k.onnx | 25.26 MiB |

## Что проверяет тест пакета {#what-the-packaged-test-checks}

Случай: `speaker-diarization`; операция: `speaker-diarization`.

Входные данные: фрагмент инаугурационной речи Джона Ф. Кеннеди из общественного достояния с секундой тишины в начале и в конце, подготовленный как монофоническое аудио с частотой 16 kHz.

Результат должен содержать фрагменты речи с допустимыми идентификаторами говорящих и временными границами в пределах входного аудио. Точное число говорящих не задано.

Ночной пакет с тестами загружает настоящие артефакты модели и запрашивает вывод через встроенную среду выполнения. Если обязательная модель отсутствует в каталоге, её тест завершается неудачей. Отсутствие встроенного движка приводит к неудаче на поддерживаемых каталогом платформах. Эти ресурсоёмкие проверки выполняются отдельно от обычного набора браузерных тестов. Успешный запуск подтверждает базовую работу модели и пригодную структуру вывода; он не доказывает качество восприятия или точность на вашей записи.

## Проверка результата {#review-the-result}

- Идентификаторы говорящих обозначают группы, выведенные из записи, а не подтверждённые личности.
- Короткие реплики, наложение голосов и похожие голоса могут быть сгруппированы неверно. Тест проверяет наличие допустимых непустых фрагментов, а не точное число говорящих.

[Покрытие и ограничения тестов моделей](/reference/local-models/) · [Руководство по локальной обработке](/help/local-processing/)

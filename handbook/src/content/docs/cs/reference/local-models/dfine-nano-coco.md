---
title: "D-FINE Nano detekce objektů"
description: "Najděte obličeje a objekty v vzorkovaných snímcích videa, aby se pomohlo navrhnout nové zarámování."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"dcf23dbf58561e9e5be37c7cefa07e7a93134ce8ed7777b02cac0a0204c1b73f","model":"aya-expanse:32b","modelDigest":"1603440383bd5504dc7afd01c0407b425b988dde650a8dc1a737433baa3cd432","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"dcf23dbf58561e9e5be37c7cefa07e7a93134ce8ed7777b02cac0a0204c1b73f","targetLocale":"cs"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Identifikujte tváře a objekty ve vzorkovaných snímcích videa, aby bylo možné navrhnout nové zarámování.

D-FINE detekuje kategorie objektů a jejich umístění. Operace detekce subjektu kombinuje tyto výsledky s detekcemi tváří YuNet.

## Aktuální dostupnost {#current-availability}

Verze pro stolní počítače obsahují požadovaný motor onnxruntime-node 1.29.0 pro macOS arm64, Linux arm64, Linux x64, Windows arm64 a Windows x64. Nainstalujte váhy tohoto modelu pomocí Správce modelů a poté spusťte jeho úlohu lokálně. Tyto cíle sestavení jsou podporovány; konzultujte zprávu o nočních testech pro výsledky konkrétního balíčku a počítače. Verze pro stolní počítače neobsahuje tento motor pro macOS x64, i když jsou váhy modelu uvedeny pro tyto platformy.

## Použití tohoto modelu {#use-this-model}

Otevřete **Framescaper: Efekt → Video efekty → Přezarámování** na platformě s požadovaným nativním runtime. Lokální asistence běží v editoru pro stolní počítače.

1. Vyberte video klip v Framescaperu.
2. Nainstalujte YuNet a D-FINE pro detekci subjektu. Úloha řízeného přezerámování také používá detekci salience U²-Net-P.
3. Spusťte lokálně a zkontrolujte navrhovaný oříznutí. Nástroje → Pokročilé lokální zpracování mohou spustit detekci subjektu samostatně.

Tato operace také vyžaduje [detekci tváře YuNet](/reference/local-models/yunet-face-detection-2026may/).

## Stažení a požadavky {#download-and-requirements}

Identita modelu: `dfine-nano-coco`, verze **1.0.0**.

Přibližná velikost stažení: **14,56 MiB**. Minimální systémová paměť: **2 GiB**. Tato požadavek katalogu se vztahuje na celkovou paměť, nikoli na aktuálně volnou paměť. Pro inferenci je také zapotřebí dostatečná volná paměť a pro instalaci je potřeba pracovní prostor; paměť nezaručuje rychlost.

Publikované platformy: macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64, Windows x64. Je také vyžadován odpovídající zabalený runtime.

Na Windows nainstalujte nejnovější podporovanou [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) odpovídající aplikaci pro stolní počítače: **x64** pro sestavení x64 nebo **ARM64** pro sestavení ARM64. Rodné motory ONNX a Sherpa vyžadují tyto knihovny; stažení modelu je neobsahuje. Samotné Visual Studio není zapotřebí k použití aplikace.

Nástroje → Správce modelů stahují publikované artefakty a ověřují jejich zaznamenané kontrolní součty SHA-256. Zpracování používá lokálně nainstalované soubory. Otevřete Úložiště a ověření ve Správci modelů, abyste zkontrolovali oznámení, opravili instalaci nebo změnili umístění úložiště.

| Artefakt | Přibližná velikost stažení |
| --- | --- |
| model.onnx | 14,55 MiB |
| config.json | 6,44 KiB |
| preprocessor_config.json | 444 bajtů |

## Co ověřuje zabalený test {#what-the-packaged-test-checks}

Případ: `subject-detection`; operace: `subject-detection`.

Vstup: Dvě fotografie: veřejně dostupný portrét astronautky Eileen Collins od NASA a fotografie kočky Chelsea pod licencí CC0. Každá je vložena do samostatného rámu s explicitním časováním zdrojového rámu, což detektorům poskytuje lidskou tvář a jasný objekt.

Výsledky tváří a objektů musí mít platné časování zdrojového rámu, konečné skóre důvěryhodnosti a ohraničující rámečky. Fixace subjektu musí vyprodukovat detekce z obou modelů.

Balíček nightly-with-tests stahuje skutečné artefakty modelu a vyžaduje inferenci prostřednictvím zabaleného runtime. Chybějící požadovaný model v katalogu způsobí selhání případu. Chybějící rodné motory selžou na platformách podporovaných katalogem. Tyto nákladné kontroly běží odděleně od běžného prohlížečového testovacího souboru. Úspěšné spuštění potvrzuje základní provádění modelu a použitelnou strukturu výstupu; nezaručuje však kvalitu vnímání nebo přesnost na vašem záznamu.

## Zkontrolujte výsledek {#review-the-result}

- Tváře a objekty mohou být přehlédnuty nebo nesprávně klasifikovány. Detekce je vzorkovaná a nezaručuje, že se pohybující subjekt bude nacházet ve všech navrhovaných oříznutích.
- Operace spouští oba modely najednou; žádný z modelů není systém rozpoznávání tváří pro identifikaci osob.

[Pokrytí testu modelu a omezení](/reference/local-models/) · [Průvodce lokálním zpracováním](/help/local-processing/)

---
title: "Độ nổi bật U²-Net-P"
description: "Ước tính vùng nổi bật về thị giác trong các khung hình lấy mẫu để hỗ trợ chỉnh khung."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"dd068e5f1e35b3056d9e1c84b80ed9493b64bf42d634ece49a03b4759cde1243","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"dd068e5f1e35b3056d9e1c84b80ed9493b64bf42d634ece49a03b4759cde1243","targetLocale":"vi"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Ước tính vùng nổi bật về thị giác trong các khung hình lấy mẫu để hỗ trợ chỉnh khung.

## Tình trạng hiện tại {#current-availability}

Bản desktop đóng gói engine onnxruntime-node 1.29.0 cần thiết cho macOS arm64, Linux arm64, Linux x64, Windows arm64 và Windows x64. Cài trọng số mô hình qua Model Manager, rồi chạy tác vụ cục bộ. Đây là các mục tiêu bản dựng được hỗ trợ; hãy xem báo cáo kiểm thử hằng đêm để biết kết quả trên một gói và máy cụ thể. Bản desktop không đóng gói engine này cho macOS x64 dù danh sách có trọng số mô hình cho nền tảng đó.

## Dùng mô hình này {#use-this-model}

Mở **Framescaper: Effect → Video effects → Reframe** trên nền tảng có runtime gốc cần thiết. Hỗ trợ cục bộ chạy trong trình biên tập desktop.

1. Chọn clip video và cài U²-Net-P qua Manage Models.
2. Tác vụ Reframe có hướng dẫn cũng cần YuNet và D-FINE; Tools → Advanced Local Processing có thể chạy riêng tác vụ phát hiện độ nổi bật.
3. Chạy cục bộ rồi xem lại khung hình được đề xuất trước khi áp dụng.

## Tải xuống và yêu cầu {#download-and-requirements}

Danh tính mô hình: `u2netp-saliency`, phiên bản **1.0.0**.

Kích thước tải xuống ước tính: **4.36 MiB**. Bộ nhớ hệ thống tối thiểu: **2 GiB**. Yêu cầu trong danh mục này nói về tổng bộ nhớ, không phải lượng bộ nhớ hiện còn trống. Suy luận cũng cần đủ bộ nhớ trống, còn cài đặt cần dung lượng lưu trữ; bộ nhớ không đảm bảo tốc độ.

Nền tảng được công bố: macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64, Windows x64. Cũng cần có runtime được đóng gói tương ứng.

Trên Windows, cài [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) mới nhất được hỗ trợ, phù hợp với ứng dụng desktop: **x64** cho bản dựng x64 hoặc **ARM64** cho bản dựng ARM64. Engine ONNX và Sherpa gốc cần các thư viện này; tệp tải mô hình không bao gồm chúng. Không cần Visual Studio để dùng ứng dụng.

Tools → Model Manager tải các hiện vật được công bố và xác minh digest SHA-256 đã ghi nhận. Quá trình xử lý dùng các tệp đã cài đặt cục bộ. Mở Storage and verification trong Model Manager để xem thông báo, sửa cài đặt hoặc đổi vị trí lưu trữ.

| Hiện vật | Kích thước tải xuống ước tính |
| --- | --- |
| u2netp.onnx | 4.36 MiB |

## Bài kiểm tra đóng gói kiểm tra điều gì {#what-the-packaged-test-checks}

Trường hợp: `saliency-detection`; thao tác: `saliency-detection`.

Đầu vào: Hai ảnh: ảnh chân dung phi hành gia Eileen Collins của NASA thuộc phạm vi công cộng và ảnh mèo Chelsea theo giấy phép CC0. Mỗi ảnh được đặt vừa trong khung hình riêng với thời gian khung nguồn rõ ràng, cung cấp cho bộ phát hiện một khuôn mặt người và một vật thể dễ nhận biết.

Kết quả khung hình phải giữ nguyên thời gian nguồn và chứa điểm nổi bật hữu hạn, có giới hạn, tại chủ thể mẫu.

Gói nightly-with-tests tải hiện vật mô hình thực và yêu cầu suy luận qua runtime được đóng gói. Thiếu mô hình bắt buộc trong danh mục sẽ khiến trường hợp kiểm tra thất bại. Thiếu engine gốc cũng gây lỗi trên nền tảng được danh mục hỗ trợ. Các kiểm tra tốn kém này chạy riêng với bộ kiểm thử trình duyệt thông thường. Kết quả đạt xác nhận mô hình chạy cơ bản và đầu ra có cấu trúc dùng được; kết quả không xác nhận chất lượng cảm nhận hoặc độ chính xác trên bản ghi của bạn.

## Đánh giá kết quả {#review-the-result}

- Vùng nổi bật về thị giác không nhất thiết là chủ thể bạn muốn giữ lại. Hãy xem lại toàn bộ vùng chọn.
- Kiểm thử xác minh kết quả độ nổi bật hữu hạn, có giới hạn và thời gian khung nguồn, chứ không đánh giá chất lượng nghệ thuật của vùng cắt.

[Phạm vi và giới hạn kiểm thử mô hình](/reference/local-models/) · [Hướng dẫn xử lý cục bộ](/help/local-processing/)

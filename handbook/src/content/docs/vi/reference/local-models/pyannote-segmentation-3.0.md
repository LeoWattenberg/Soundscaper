---
title: "Phân đoạn Pyannote 3.0"
description: "Nhóm lời nói thành lượt của từng người để xem ai nói vào lúc nào."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"139a50078b93c2e56781b181bb48f07513fbe361c5e4f4b9106b1e5063606954","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"139a50078b93c2e56781b181bb48f07513fbe361c5e4f4b9106b1e5063606954","targetLocale":"vi"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Nhóm lời nói thành lượt của từng người để xem ai nói vào lúc nào.

Pyannote cung cấp phân đoạn lời nói mà runtime phân biệt người nói sử dụng.
ERes2Net cung cấp các biểu diễn số của người nói để nhóm những đoạn đó.

## Tình trạng sẵn có {#current-availability}

Bản dựng desktop đóng gói engine sherpa-onnx-node 1.13.5 cần thiết cho macOS
arm64, Linux arm64, Linux x64, Windows arm64 và Windows x64. Cài trọng số của
mô hình này qua Model Manager, sau đó chạy tác vụ cục bộ. Đây là các nền tảng
bản dựng được hỗ trợ; xem báo cáo kiểm thử nightly để biết kết quả trên gói và
máy cụ thể. Bản dựng desktop không đóng gói engine này cho macOS x64 dù trọng
số mô hình được liệt kê cho các nền tảng đó.

## Dùng mô hình này {#use-this-model}

Mở **Phân tích → Lời nói → Nhận diện người nói** trên nền tảng có runtime gốc
cần thiết. Hỗ trợ cục bộ chạy trong trình chỉnh sửa desktop.

1. Chọn cuộc hội thoại cần phân tích.
2. Cài cả phân đoạn Pyannote lẫn biểu diễn người nói ERes2Net qua Quản lý mô hình; thao tác này dùng cả hai.
3. Chạy cục bộ, xem lại các lượt người nói được đề xuất và áp dụng kết quả mong muốn.

Thao tác này cũng cần [biểu diễn người nói ERes2Net](/reference/local-models/speech-3d-speaker-eres2net/).

## Tải xuống và yêu cầu {#download-and-requirements}

Định danh mô hình: `pyannote-segmentation-3.0`, phiên bản **3.0.0**.

Dung lượng tải xuống ước tính: **5.72 MiB**. Bộ nhớ hệ thống tối thiểu:
**2 GiB**. Yêu cầu trong danh mục này nói về tổng bộ nhớ, không phải bộ nhớ còn
trống hiện tại. Suy luận cũng cần đủ bộ nhớ trống, còn cài đặt cần dung lượng
lưu trữ; nhiều bộ nhớ không bảo đảm tốc độ.

Nền tảng được phát hành: macOS arm64, macOS x64, Linux arm64, Linux x64,
Windows arm64 và Windows x64. Cũng cần runtime được đóng gói phù hợp.

Trên Windows, cài [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) mới nhất được hỗ trợ và phù hợp với ứng dụng desktop: **x64** cho bản dựng x64 hoặc **ARM64** cho bản dựng ARM64. Engine ONNX và Sherpa gốc cần các thư viện này; gói tải mô hình không kèm chúng. Không cần Visual Studio để dùng ứng dụng.

**Công cụ → Model Manager** tải các tạo tác đã phát hành và xác minh mã băm
SHA-256 đã ghi nhận. Xử lý dùng các tệp đã cài đặt cục bộ. Mở **Lưu trữ và
xác minh** trong Model Manager để xem thông báo, sửa bản cài đặt hoặc đổi vị
trí lưu trữ.

| Tạo tác | Dung lượng tải xuống ước tính |
| --- | --- |
| model.onnx | 5.72 MiB |
| LICENSE | 1.04 KiB |

## Bài kiểm tra gói xác minh điều gì {#what-the-packaged-test-checks}

Ca kiểm tra: `speaker-diarization`; thao tác: `speaker-diarization`.

Đầu vào: một đoạn diễn văn nhậm chức thuộc phạm vi công cộng của John F.
Kennedy, được thêm một giây im lặng ở mỗi đầu và chuẩn bị thành âm thanh mono
16 kHz.

Kết quả phải có các lượt người nói cùng ID người nói hợp lệ và thời gian nằm
trong âm thanh đầu vào. Bài kiểm tra không yêu cầu số người nói chính xác.

Gói nightly-with-tests tải các tạo tác mô hình thật và yêu cầu suy luận qua
runtime đóng gói. Nếu thiếu mô hình bắt buộc trong danh mục, ca kiểm tra thất
bại. Thiếu engine gốc cũng làm kiểm tra thất bại trên nền tảng được danh mục
hỗ trợ. Các kiểm tra tốn kém này chạy riêng với bộ kiểm thử trình duyệt thông
thường. Kết quả đạt xác nhận mô hình chạy cơ bản và đầu ra có cấu trúc dùng
được; kết quả này không chứng minh chất lượng cảm nhận hay độ chính xác trên
bản ghi của bạn.

## Xem lại kết quả {#review-the-result}

- ID người nói là các nhóm được suy luận từ bản ghi, không phải danh tính thực đã xác minh.
- Câu nói ngắn, giọng nói chồng lấn và giọng giống nhau có thể bị nhóm sai. Bài kiểm tra xác minh có các lượt hợp lệ, không yêu cầu số người nói chính xác.

[Phạm vi và giới hạn kiểm thử mô hình](/reference/local-models/) · [Hướng dẫn xử lý cục bộ](/help/local-processing/)

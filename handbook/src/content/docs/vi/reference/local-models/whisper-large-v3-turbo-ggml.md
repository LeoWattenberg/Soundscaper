---
title: "Whisper large-v3 turbo"
description: "Chép lời nói đã chọn cục bộ qua runtime whisper.cpp được đóng gói."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"2e60c8b2f6345621f07507ef096c62707b9679929c18606753a9e1ec8ec028b5","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"2e60c8b2f6345621f07507ef096c62707b9679929c18606753a9e1ec8ec028b5","targetLocale":"vi"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Chép lời nói đã chọn cục bộ qua runtime whisper.cpp được đóng gói.

## Tình trạng hiện tại {#current-availability}

Bản desktop đóng gói engine whisper-cpp v1.9.3 cần thiết cho macOS arm64, Linux arm64, Linux x64, Windows arm64 và Windows x64. Cài trọng số mô hình qua Model Manager, rồi chạy tác vụ cục bộ. Đây là các mục tiêu bản dựng được hỗ trợ; hãy xem báo cáo kiểm thử hằng đêm để biết kết quả trên một gói và máy cụ thể. Bản desktop không đóng gói engine này cho macOS x64 dù danh sách có trọng số mô hình cho nền tảng đó.

## Dùng mô hình này {#use-this-model}

Mở **Analyze → Speech → Transcribe & Captions** trên nền tảng có runtime gốc cần thiết. Hỗ trợ cục bộ chạy trong trình biên tập desktop.

1. Chọn lời nói và cài Whisper large-v3 turbo qua Manage Models.
2. Khi muốn chọn đích danh Whisper, hãy chọn mô hình nhận dạng lời nói này trong Tools → Advanced Local Processing.
3. Chạy cục bộ rồi xem lại cả văn bản lẫn ranh giới các đoạn trước khi chấp nhận.

## Tải xuống và yêu cầu {#download-and-requirements}

Danh tính mô hình: `whisper-large-v3-turbo-ggml`, phiên bản **1.0.0**.

Kích thước tải xuống ước tính: **547.45 MiB**. Bộ nhớ hệ thống tối thiểu: **4 GiB**. Yêu cầu trong danh mục này nói về tổng bộ nhớ, không phải lượng bộ nhớ hiện còn trống. Suy luận cũng cần đủ bộ nhớ trống, còn cài đặt cần dung lượng lưu trữ; bộ nhớ không đảm bảo tốc độ.

Nền tảng được công bố: macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64, Windows x64. Cũng cần có runtime được đóng gói tương ứng.

Tools → Model Manager tải các hiện vật được công bố và xác minh digest SHA-256 đã ghi nhận. Quá trình xử lý dùng các tệp đã cài đặt cục bộ. Mở Storage and verification trong Model Manager để xem thông báo, sửa cài đặt hoặc đổi vị trí lưu trữ.

| Hiện vật | Kích thước tải xuống ước tính |
| --- | --- |
| ggml-large-v3-turbo-q5_0.bin | 547.45 MiB |

## Bài kiểm tra đóng gói kiểm tra điều gì {#what-the-packaged-test-checks}

Trường hợp: `whisper-turbo-transcript`; thao tác: `speech-recognition`.

Đầu vào: Một đoạn trích bài diễn văn nhậm chức của John F. Kennedy thuộc phạm vi công cộng, thêm một giây im lặng ở mỗi đầu và chuẩn bị dưới dạng âm thanh mono 16 kHz.

Bản chép lời phải có văn bản không rỗng với thời gian hợp lệ nằm trong âm thanh đầu vào. Kiểm thử không so sánh chính xác câu chữ.

Gói nightly-with-tests tải hiện vật mô hình thực và yêu cầu suy luận qua runtime được đóng gói. Thiếu mô hình bắt buộc trong danh mục sẽ khiến trường hợp kiểm tra thất bại. Thiếu engine gốc cũng gây lỗi trên nền tảng được danh mục hỗ trợ. Các kiểm tra tốn kém này chạy riêng với bộ kiểm thử trình duyệt thông thường. Kết quả đạt xác nhận mô hình chạy cơ bản và đầu ra có cấu trúc dùng được; kết quả không xác nhận chất lượng cảm nhận hoặc độ chính xác trên bản ghi của bạn.

## Đánh giá kết quả {#review-the-result}

- Whisper có thể tạo văn bản sai, nhất là khi có tiếng ồn hoặc nội dung không có lời nói rõ ràng. Hãy nghe trong khi xem lại.
- Kiểm thử xác minh runtime thực trả về bản chép lời không rỗng, có thời gian; không đảm bảo cách viết cụ thể hoặc điểm nhận dạng nào.

[Phạm vi và giới hạn kiểm thử mô hình](/reference/local-models/) · [Hướng dẫn xử lý cục bộ](/help/local-processing/)

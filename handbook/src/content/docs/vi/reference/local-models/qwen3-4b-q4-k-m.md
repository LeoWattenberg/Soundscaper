---
title: "Đề xuất nội dung với Qwen3 4B"
description: "Đề xuất tiêu đề, câu mở đầu, chương và giải thích cho các mục nổi bật hiện có."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"70517a074a6159de10f47c80ff1cccf9d7170e25a57f49dd2bd3eb1fab84a824","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"70517a074a6159de10f47c80ff1cccf9d7170e25a57f49dd2bd3eb1fab84a824","targetLocale":"vi"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Đề xuất tiêu đề, câu mở đầu, chương và giải thích cho các mục nổi bật hiện có.

## Tình trạng hiện tại {#current-availability}

Bản desktop đóng gói engine llama-cpp b10509 cần thiết cho macOS arm64, Linux x64, Linux arm64, Windows x64 và Windows arm64. Cài trọng số mô hình qua Model Manager, rồi chạy tác vụ cục bộ. Đây là các mục tiêu bản dựng được hỗ trợ; hãy xem báo cáo kiểm thử hằng đêm để biết kết quả trên một gói và máy cụ thể.

## Dùng mô hình này {#use-this-model}

Mở **Framescaper: Tools → Advanced Local Processing → Editorial generation** trên nền tảng có runtime gốc cần thiết. Hỗ trợ cục bộ chạy trong trình biên tập desktop.

1. Chuẩn bị và xem lại các mục nổi bật từ phương tiện đã chọn.
2. Cài Qwen qua Model Manager và bật tùy chọn tạo nội dung biên tập.
3. Xem lại mọi đề xuất văn bản trước khi chấp nhận. Chỉ danh tính ứng viên được cho phép và các trường văn bản được yêu cầu mới hợp lệ.

## Tải xuống và yêu cầu {#download-and-requirements}

Danh tính mô hình: `qwen3-4b-q4-k-m`, phiên bản **1.0.0**.

Kích thước tải xuống ước tính: **2.33 GiB**. Bộ nhớ hệ thống tối thiểu: **16 GiB**. Yêu cầu trong danh mục này nói về tổng bộ nhớ, không phải lượng bộ nhớ hiện còn trống. Suy luận cũng cần đủ bộ nhớ trống, còn cài đặt cần dung lượng lưu trữ; bộ nhớ không đảm bảo tốc độ.

Nền tảng được công bố: macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. Cũng cần có runtime được đóng gói tương ứng.

Tools → Model Manager tải các hiện vật được công bố và xác minh digest SHA-256 đã ghi nhận. Quá trình xử lý dùng các tệp đã cài đặt cục bộ. Mở Storage and verification trong Model Manager để xem thông báo, sửa cài đặt hoặc đổi vị trí lưu trữ.

| Hiện vật | Kích thước tải xuống ước tính |
| --- | --- |
| model.gguf | 2.33 GiB |

## Bài kiểm tra đóng gói kiểm tra điều gì {#what-the-packaged-test-checks}

Trường hợp: `qwen-editorial-proposals`; thao tác: `editorial-generation`.

Đầu vào: Hai mục nổi bật cố định về khôi phục và so sánh bản ghi, được chuyển qua bộ tạo kế hoạch biên tập trong bản phát hành với yêu cầu cung cấp mọi trường văn bản.

JSON nghiêm ngặt phải chứa chính xác một lần mọi ứng viên được cho phép, cùng tiêu đề, câu mở đầu, phần giải thích dễ đọc và nội dung chương hợp lệ. Bộ đánh giá trong bản phát hành từ chối nội dung không an toàn hoặc không thuộc yêu cầu.

Gói nightly-with-tests tải hiện vật mô hình thực và yêu cầu suy luận qua runtime được đóng gói. Thiếu mô hình bắt buộc trong danh mục sẽ khiến trường hợp kiểm tra thất bại. Thiếu engine gốc cũng gây lỗi trên nền tảng được danh mục hỗ trợ. Các kiểm tra tốn kém này chạy riêng với bộ kiểm thử trình duyệt thông thường. Kết quả đạt xác nhận mô hình chạy cơ bản và đầu ra có cấu trúc dùng được; kết quả không xác nhận chất lượng cảm nhận hoặc độ chính xác trên bản ghi của bạn.

## Đánh giá kết quả {#review-the-result}

- Câu chữ được tạo có thể gây hiểu nhầm hoặc lặp lại. Đây là đề xuất dựa trên bằng chứng được cung cấp, không phải phân tích thực tế đã xác minh.
- Mô hình chạy cục bộ trên CPU và cần tổng cộng ít nhất 16 GiB bộ nhớ hệ thống cùng bộ nhớ trống phù hợp. Yêu cầu ngắn vẫn có thể mất vài phút.
- Kiểm thử yêu cầu JSON nghiêm ngặt, mỗi ứng viên hợp lệ xuất hiện đúng một lần và các trường được yêu cầu dễ đọc. Lệnh, đường dẫn, đánh dấu, thời gian mới, ứng viên ngoài danh sách và đầu ra sai định dạng đều bị từ chối thay vì áp dụng.

[Phạm vi và giới hạn kiểm thử mô hình](/reference/local-models/) · [Hướng dẫn xử lý cục bộ](/help/local-processing/)

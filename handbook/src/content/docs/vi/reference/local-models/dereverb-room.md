---
title: "Khử vang phòng"
description: "Giảm tiếng phản xạ phòng trong giọng nói đã ghi bằng mô hình khử vang cục bộ."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"bd554a1cd332fecfa937182d03f1bde12df41766677969a24297dc1de91ba8c2","model":"gpt-6-luna","modelProvider":"codex-subagent","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"bd554a1cd332fecfa937182d03f1bde12df41766677969a24297dc1de91ba8c2","targetLocale":"vi"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Giảm tiếng phản xạ phòng trong giọng nói đã ghi bằng mô hình khử vang cục bộ.

## Tình trạng sẵn có {#current-availability}

Bản dựng desktop đóng gói engine onnxruntime-node 1.29.0 cần thiết cho macOS
arm64, Linux x64, Linux arm64, Windows x64 và Windows arm64. Cài trọng số của
mô hình này qua Model Manager, sau đó chạy tác vụ cục bộ. Đây là các nền tảng
bản dựng được hỗ trợ; xem báo cáo kiểm thử nightly để biết kết quả trên gói và
máy cụ thể.

## Dùng mô hình này {#use-this-model}

Mở **Công cụ → Xử lý cục bộ nâng cao → Khử vang** trên nền tảng có runtime gốc
cần thiết. Hỗ trợ cục bộ chạy trong trình chỉnh sửa desktop.

1. Chọn lời nói bị ảnh hưởng bởi tiếng phản xạ phòng.
2. Cài mô hình qua Model Manager và chạy khử vang.
3. So sánh kết quả với bản gốc và chỉ chấp nhận nếu lời nói nghe tốt hơn.

## Tải xuống và yêu cầu {#download-and-requirements}

Định danh mô hình: `dereverb-room`, phiên bản **1.0.0**.

Dung lượng tải xuống ước tính: **114.57 MiB**. Bộ nhớ hệ thống tối thiểu:
**2 GiB**. Yêu cầu trong danh mục này nói về tổng bộ nhớ, không phải bộ nhớ còn
trống hiện tại. Suy luận cũng cần đủ bộ nhớ trống, còn cài đặt cần dung lượng
lưu trữ; nhiều bộ nhớ không bảo đảm tốc độ.

Nền tảng được phát hành: macOS arm64, Linux x64, Linux arm64, Windows x64 và
Windows arm64. Cũng cần runtime được đóng gói phù hợp.

Trên Windows, cài [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) mới nhất được hỗ trợ và phù hợp với ứng dụng desktop: **x64** cho bản dựng x64 hoặc **ARM64** cho bản dựng ARM64. Engine ONNX và Sherpa gốc cần các thư viện này; gói tải mô hình không kèm chúng. Không cần Visual Studio để dùng ứng dụng.

**Công cụ → Model Manager** tải các tạo tác đã phát hành và xác minh mã băm
SHA-256 đã ghi nhận. Xử lý dùng các tệp đã cài đặt cục bộ. Mở **Lưu trữ và
xác minh** trong Model Manager để xem thông báo, sửa bản cài đặt hoặc đổi vị
trí lưu trữ.

| Tạo tác | Dung lượng tải xuống ước tính |
| --- | --- |
| network.onnx | 114.57 MiB |

## Bài kiểm tra gói xác minh điều gì {#what-the-packaged-test-checks}

Ca kiểm tra: `dereverb-room-speech`; thao tác: `dereverberation`.

Đầu vào: bài phát biểu của Kennedy được chuyển đổi mẫu về 44.1 kHz cùng bốn
tiếng phản xạ trễ có thể tái tạo.

Âm thanh đã giải mã phải chứa các mẫu hữu hạn, giữ nguyên cấu trúc âm thanh
mong đợi, khác các mẫu đầu vào và có tín hiệu nghe được khác 0. Chỉ khác tiêu
đề tệp không được tính là có thay đổi.

Gói nightly-with-tests tải các tạo tác mô hình thật và yêu cầu suy luận qua
runtime đóng gói. Nếu thiếu mô hình bắt buộc trong danh mục, ca kiểm tra thất
bại. Thiếu engine gốc cũng làm kiểm tra thất bại trên nền tảng được danh mục
hỗ trợ. Các kiểm tra tốn kém này chạy riêng với bộ kiểm thử trình duyệt thông
thường. Kết quả đạt xác nhận mô hình chạy cơ bản và đầu ra có cấu trúc dùng
được; kết quả này không chứng minh chất lượng cảm nhận hay độ chính xác trên
bản ghi của bạn.

## Xem lại kết quả {#review-the-result}

- Tiếng phản xạ mạnh, âm nhạc và căn phòng lạ có thể tạo tạp âm hoặc làm mất chi tiết cần giữ.
- Âm thanh đã thay đổi và không im lặng xác nhận quá trình xử lý, không chứng minh đã loại bỏ hoàn toàn tiếng vang hay cải thiện cảm nhận.
- Mô hình công bố giấy phép GPL-3.0; hãy giữ nguyên giấy phép và hướng dẫn mã nguồn tương ứng khi phân phối lại. Chưa rõ nguồn gốc tập dữ liệu huấn luyện âm thanh khô và điểm kiểm tra cơ sở.

[Phạm vi và giới hạn kiểm thử mô hình](/reference/local-models/) · [Hướng dẫn xử lý cục bộ](/help/local-processing/)

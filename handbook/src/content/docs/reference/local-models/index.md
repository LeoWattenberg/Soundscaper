---
title: "Local model guides and real execution tests"
description: "Check the native availability of every published local model and understand its intended workflow and real execution test."
editUrl: false
---

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

These guides describe every model in the published catalog and its real execution case. Model weights and native inference engines are separate requirements: an available download does not mean a task can run.

**Native runtime packaging is incomplete: 8 published models in 7 cases have no admitted native engine target.** Their weights can be downloaded, but their tests fail on catalog-supported platforms until the verified engine packages are provided. The table reflects the committed native runtime inventories, not a successful all-model run.

| Model guide | Purpose | Native runtime inventory | Packaged execution case |
| --- | --- | --- | --- |
| [Silero VAD v6](/reference/local-models/silero-vad-v6/) | voice activity detection | Native payloads recorded; see platform list | `silero-voice-activity` |
| [Parakeet TDT 0.6B v2](/reference/local-models/parakeet-tdt-0.6b-v2/) | speech recognition | Native payloads recorded; see platform list | `parakeet-v2-transcript` |
| [Parakeet TDT 0.6B v3](/reference/local-models/parakeet-tdt-0.6b-v3/) | speech recognition | Native payloads recorded; see platform list | `parakeet-v3-transcript` |
| [Whisper large-v3 turbo](/reference/local-models/whisper-large-v3-turbo-ggml/) | speech recognition | Native engine pending | `whisper-turbo-transcript` |
| [Pyannote segmentation 3.0](/reference/local-models/pyannote-segmentation-3.0/) | speaker segmentation | Native payloads recorded; see platform list | `speaker-diarization` |
| [ERes2Net speaker embedding](/reference/local-models/speech-3d-speaker-eres2net/) | speaker embedding | Native payloads recorded; see platform list | `speaker-diarization` |
| [DeepFilterNet3](/reference/local-models/deepfilternet3/) | speech enhancement | Native engine pending | `deepfilter-enhancement` |
| [YuNet face detection](/reference/local-models/yunet-face-detection-2026may/) | face detection | Native engine pending | `subject-detection` |
| [D-FINE Nano object detection](/reference/local-models/dfine-nano-coco/) | object detection | Native engine pending | `subject-detection` |
| [U²-Net-P saliency](/reference/local-models/u2netp-saliency/) | saliency detection | Native engine pending | `saliency-detection` |
| [PP-OCRv4 mobile](/reference/local-models/ppocr-v4-mobile/) | optical character recognition | Native engine pending | `ppocr-recognized-text` |
| [Nomic Embed Text v1.5](/reference/local-models/nomic-embed-text-v1.5/) | text embedding | Native engine pending | `nomic-text-embedding` |
| [SigLIP2 base patch16 224](/reference/local-models/siglip2-base-patch16-224/) | image text embedding | Native engine pending | `siglip-image-text-embedding` |

## Test scope {#test-scope}

The catalog contains 13 models covered by 11 execution cases. Speaker diarization uses a segmentation model and an embedding model together. Subject detection uses face and object models together. SigLIP2 testing exercises its image and text networks.

The ordinary browser tests check the interface with a simulated desktop backend. The nightly-with-tests Electron package separately downloads verified model files and requests real inference. Missing native engines are reported as failures on catalog-supported platforms. Expensive model runs are excluded from the normal browser suite. Read the report from a particular package run for its actual results; these pages describe the required checks, not a claim that every build or machine has passed.

The published model artifacts total approximately **2.36 GiB** before runtime files, fixtures, temporary space, and test output. For the complete run, use a machine with at least **8 GiB free memory**; **16 GiB system memory or more** is a practical starting point. The operation host reserves more memory than some individual catalog minimums. Models run sequentially.

Audio checks reject silence, non-finite samples, and output identical to the input. Text checks require meaningful nonempty result fields. Embedding, detection, and timing checks validate their appropriate numerical structure. Exact sample values, spelling, and rankings are not used as golden outputs.

Passing these checks proves basic operation on a small fixture. It does not prove accurate transcription, correct object labels, lossless denoising, or subjective quality. Review results on your own media before applying them.

## Operations without published models {#operations-without-published-models}

The current published catalog has no models for source separation, dereverberation, beat tracking, audio tagging, word alignment, or editorial generation. Their presence in a menu or operation reference does not make a compatible model available to download. These model tests do not claim to cover those unavailable model installations. Shot detection can also run without a model.

[Local processing guide](/help/local-processing/) · [Workflows and operations](/reference/generated/local-assistance/)

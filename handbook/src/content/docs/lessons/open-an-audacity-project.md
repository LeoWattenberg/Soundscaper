---
title: "Open an Audacity project"
description: "Bring an existing .aup3 or .aup4 project into Soundscaper with its tracks intact."
editUrl: false
sidebar:
  order: 48
head:
  - tag: script
    attrs:
      type: "application/ld+json"
    content: "{\"@context\":\"https://schema.org\",\"@type\":\"HowTo\",\"name\":\"Open an Audacity project\",\"description\":\"Bring an existing .aup3 or .aup4 project into Soundscaper with its tracks intact.\",\"tool\":[{\"@type\":\"HowToTool\",\"name\":\"Soundscaper\"}],\"step\":[{\"@type\":\"HowToStep\",\"position\":1,\"name\":\"Open Soundscaper. A new, empty project is ready as soon as the editor loads.\",\"text\":\"Open Soundscaper. A new, empty project is ready as soon as the editor loads.\"},{\"@type\":\"HowToStep\",\"position\":2,\"name\":\"Choose File → Audacity projects → Open Audacity project (.aup3, .aup4) and pick the project. This lesson uses a small example project. Its tracks and clips appear as they were in Audacity.\",\"text\":\"Choose File → Audacity projects → Open Audacity project (.aup3, .aup4) and pick the project. This lesson uses a small example project. Its tracks and clips appear as they were in Audacity.\"},{\"@type\":\"HowToStep\",\"position\":3,\"name\":\"The project now shows a clip named Audio 1 and a track named Fixture track.\",\"text\":\"The project now shows a clip named Audio 1 and a track named Fixture track. The track and clip from the Audacity project, with their names.\"}]}"
---

<!-- Generated by `node scripts/docs-reference.mjs`. Do not edit. -->

Projects made in Audacity 3 (.aup3) and Audacity 4 (.aup4) open directly. The tracks, clips, names and positions come across; effects that Soundscaper does not have are listed so you know what to check. The original file is not changed.

:::note[Coming from Audacity?]
This is Audacity's **File → Open**. The names below are Soundscaper's own, which sometimes differ.
:::

## Steps

1. Open Soundscaper. A new, empty project is ready as soon as the editor loads.
2. Choose **File → Audacity projects → Open Audacity project (.aup3, .aup4)** and pick the project. This lesson uses a small example project. Its tracks and clips appear as they were in Audacity.
3. The project now shows a clip named **Audio 1** and a track named **Fixture track**.
   *You should see:* The track and clip from the Audacity project, with their names.

## Tips

- An old-style `.aup` project needs its `_data` folder next to it; pick both when asked.
- **File → Audacity projects → Export AUP4** goes the other way, for handing a project back to Audacity.

## Related lessons

- [Save your project](/lessons/save-your-project/) — Keep the project, with every track and edit, in the local project library.
- [Move a project between computers](/lessons/move-a-project-between-computers/) — Export the whole project as one file, then open that file elsewhere.
- [Export each track as its own file](/lessons/export-each-track-as-its-own-file/) — Render the tracks separately, as stems, in one download.
- [Change a clip’s sample rate](/lessons/change-a-clips-sample-rate/) — Resample a clip so it matches the rate the rest of the project uses.

## Reference

- [Project file suffixes and label file formats are in the project and label files reference.](/reference/generated/project-files/)

## About this lesson

Every step on this page is replayed against each build of Soundscaper by the browser suite (`tests/browser/soundscaper-lessons.spec.js`). If a menu entry, field or button stops matching, the build fails until the lesson is corrected, so the steps you read are the steps the editor accepts.

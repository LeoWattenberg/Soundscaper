---
title: "Beat This final"
description: "Find music beats with the larger optional Beat This model."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"fe14e2e0d43131e74827298ac37d996880633a12ed9399ca796a8a570593905d","targetLocale":"en-GB"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Find music beats with the larger optional Beat This model.

## Current availability {#current-availability}

Desktop builds package the required onnxruntime-node 1.29.0 engine for macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. Install this model’s weights through Model Manager, then run its task locally. These are supported build targets; consult the nightly test report for results on a particular package and machine.

## Use this model {#use-this-model}

Open **Tools → Advanced Local Processing → Beat tracking** on a platform with the required native runtime. Local assistance runs in the desktop editor.

1. Select music with a clear rhythmic passage.
2. Install Beat This final through Model Manager and choose its exact model identity.
3. Review the proposed beats and tempo against the music before accepting them.

## Download and requirements {#download-and-requirements}

Model identity: `beat-this-final0`, version **1.1.0**.

Approximate download size: **79.32 MiB**. Minimum system memory: **4 GiB**. This catalogue requirement refers to total memory, not currently free memory. Inference also needs sufficient free memory, and installation needs working space; memory is not a speed guarantee.

Published platforms: macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. A matching packaged runtime is also required.

On Windows, install the latest supported [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) matching the desktop app: **x64** for an x64 build or **ARM64** for an ARM64 build. The native ONNX and Sherpa engines require these libraries; model downloads do not include them. Visual Studio itself is not required to use the app.

Tools → Model Manager downloads the published artefacts and verifies their recorded SHA-256 digests. Processing uses the installed files locally. Open Storage and verification in Model Manager to inspect notices, repair an installation, or change the storage location.

| Artefact | Approximate download size |
| --- | --- |
| beat-this-final0.onnx | 79.32 MiB |

## What the packaged test checks {#what-the-packaged-test-checks}

Case: `beat-this-final-beats`; operation: `beat-tracking`.

Input: Sixteen seconds of reproducible synthesized percussion and changing tones at 22.05 kHz, with a regular beat.

Beat positions must be nonempty, ordered, and inside the audio; tempo data must be finite and positive.

The nightly-with-tests package downloads real model artefacts and requests inference through the packaged runtime. A required model missing from the catalogue fails its case. Missing native engines fail on catalogue-supported platforms. These costly checks run separately from the normal browser suite. A passing run confirms basic model execution and usable output structure; it does not establish perceptual quality or accuracy on your recording.

## Review the result {#review-the-result}

- The larger model needs more memory and is not guaranteed to improve every recording.
- This model has its own real inference case. Its test cannot be satisfied by running the smaller model instead.

[Model test coverage and limitations](/reference/local-models/) · [Local processing guide](/help/local-processing/)

---
title: "هم‌ترازی واژه با wav2vec2 base 960h"
description: "رونویسی موجود به زبان انگلیسی را با واژه‌های گفته‌شده در ضبط هم‌تراز کنید."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"55565379d1a86871ec57d126fccb26ccdc947149b0bf17fc54e0b7cabab9c8dd","model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"55565379d1a86871ec57d126fccb26ccdc947149b0bf17fc54e0b7cabab9c8dd","targetLocale":"fa"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

رونویسی موجود به زبان انگلیسی را با واژه‌های گفته‌شده در ضبط هم‌تراز کنید.

## دسترس‌پذیری کنونی {#current-availability}

نسخه‌های دسکتاپ موتور لازم onnxruntime-node 1.29.0 را برای macOS arm64، Linux x64، Linux arm64، Windows x64 و Windows arm64 همراه دارند. وزن‌های این مدل را با مدیر مدل‌ها نصب کنید و سپس وظیفهٔ آن را به‌صورت محلی اجرا کنید. این‌ها هدف‌های ساخت پشتیبانی‌شده‌اند؛ برای نتیجهٔ هر بسته و دستگاه مشخص، گزارش آزمون شبانه را ببینید.

## استفاده از این مدل {#use-this-model}

در پلتفرمی با موتور بومی لازم، **تحلیل → گفتار → رونویسی و زیرنویس، یا ابزارها → پردازش محلی پیشرفته → هم‌ترازی واژه** را باز کنید. کمک محلی در ویرایشگر دسکتاپ اجرا می‌شود.

1. گفتار را انتخاب کنید و رونویسی انگلیسیِ آن را آماده یا بازبینی کنید.
2. مدل هم‌ترازی را از مدیر مدل‌ها نصب کنید.
3. هم‌ترازی را اجرا کنید و هنگام بازبینی، پیش از پذیرفتن مرزهای پیشنهادی واژه‌ها به صدا گوش دهید.

## بارگیری و نیازمندی‌ها {#download-and-requirements}

شناسهٔ مدل: `wav2vec2-base-960h`، نسخهٔ **1.0.0**.

اندازهٔ تقریبی بارگیری: **360.38 MiB**. حداقل حافظهٔ سیستم: **4 GiB**. این نیاز کاتالوگ به کل حافظهٔ سیستم اشاره دارد، نه حافظهٔ آزاد فعلی. استنتاج به حافظهٔ آزاد کافی نیاز دارد و نصب هم فضای کاری می‌خواهد؛ مقدار حافظه تضمین‌کنندهٔ سرعت نیست.

پلتفرم‌های منتشرشده: macOS arm64، Linux x64، Linux arm64، Windows x64، Windows arm64. موتور بسته‌بندی‌شدهٔ سازگار نیز لازم است.

در Windows، آخرین نسخهٔ پشتیبانی‌شدهٔ [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) را متناسب با برنامهٔ دسکتاپ نصب کنید: برای ساخت x64 نسخهٔ **x64** و برای ساخت ARM64 نسخهٔ **ARM64**. موتورهای بومی ONNX و Sherpa به این کتابخانه‌ها نیاز دارند؛ بارگیری مدل آن‌ها را دربر ندارد. برای استفاده از برنامه نیازی به خود Visual Studio نیست.

ابزارها → مدیر مدل‌ها مصنوعات منتشرشده را بارگیری می‌کند و درستی آن‌ها را با هش‌های SHA-256 ثبت‌شده می‌سنجد. پردازش با فایل‌های نصب‌شده و به‌صورت محلی انجام می‌شود. برای دیدن اعلان‌ها، تعمیر نصب یا تغییر محل ذخیره‌سازی، بخش ذخیره‌سازی و تأیید را در مدیر مدل‌ها باز کنید.

| مصنوع | اندازهٔ تقریبی بارگیری |
| --- | --- |
| model.onnx | 360.38 MiB |

## آزمون بسته‌بندی‌شده چه چیزی را می‌سنجد {#what-the-packaged-test-checks}

مورد: `wav2vec2-word-alignment`؛ عملیات: `word-alignment`.

ورودی: سخنرانی کندیِ در مالکیت عمومی با نرخ 16 kHz، به‌همراه رونویسی انگلیسیِ شناخته‌شدهٔ آن به‌عنوان ورودی جداگانه و تأییدشده.

هر واژهٔ ارائه‌شده باید با زمان‌بندی معتبر و مرتب‌شده در بازهٔ صدا بازنمایی شود؛ زمان دقیق هم‌ترازی تعیین نشده است.

بستهٔ شبانهٔ دارای آزمون، مصنوعات واقعی مدل را بارگیری می‌کند و از موتور بسته‌بندی‌شده استنتاج می‌خواهد. اگر مدل لازم در کاتالوگ نباشد، مورد آزمون آن شکست می‌خورد. نبود موتور بومی در پلتفرم‌های پشتیبانی‌شدهٔ کاتالوگ نیز شکست محسوب می‌شود. این بررسی‌های پرهزینه جدا از مجموعه‌آزمون عادی مرورگر اجرا می‌شوند. موفقیت یعنی مدل به‌درستی اجرا شده و ساختار خروجی قابل استفاده است؛ کیفیت ادراکی یا دقت روی صدای شما را ثابت نمی‌کند.

## بازبینی نتیجه {#review-the-result}

- این مدل واژه‌های انگلیسیِ ارائه‌شده را هم‌تراز می‌کند؛ رونویسی نادرست را ترجمه یا اصلاح نمی‌کند.
- واژه‌های تکراری، گفتار پس‌زمینه و واژه‌های حذف‌شده از رونویسی می‌توانند مرزهای نادرست ایجاد کنند. آزمون زمان‌بندی مرتب و درون‌بازه‌ای را با فهرست واژه‌های داده‌شده می‌سنجد، نه تطابق زمانیِ بی‌نقص را.

[پوشش آزمون مدل‌ها و محدودیت‌ها](/reference/local-models/) · [راهنمای پردازش محلی](/help/local-processing/)

---
title: "DeepFilterNet3"
description: "با مدل محلی بهبود گفتار، نویز پس‌زمینه را در گفت‌وگو کاهش دهید."
editUrl: false
---

<!-- docs-ai-provenance: {"factPacketSha256":"f85d297ec5888b31b3d5b975fbec8ea110b8dc74c6546835cf728038aa422358","model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"f85d297ec5888b31b3d5b975fbec8ea110b8dc74c6546835cf728038aa422358","targetLocale":"fa"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

با مدل محلی بهبود گفتار، نویز پس‌زمینه را در گفت‌وگو کاهش دهید.

## دسترس‌پذیری کنونی {#current-availability}

نسخه‌های دسکتاپ موتور موردنیاز onnxruntime-node 1.29.0 را برای macOS arm64، Linux arm64، Linux x64، Windows arm64 و Windows x64 بسته‌بندی می‌کنند. وزن‌های این مدل را از طریق مدیر مدل نصب کنید و سپس وظیفه‌اش را به‌صورت محلی اجرا کنید. این‌ها هدف‌های ساخت پشتیبانی‌شده‌اند؛ برای دیدن نتیجه روی یک بسته و دستگاه مشخص، گزارش آزمون شبانه را ببینید. ساخت دسکتاپ این موتور را برای macOS x64 بسته‌بندی نمی‌کند، هرچند وزن‌های مدل برای این پلتفرم فهرست شده‌اند.

## استفاده از این مدل {#use-this-model}

در پلتفرمی که زمان‌اجرای بومی لازم را دارد، **افکت → حذف نویز و ترمیم → بهبود گفت‌وگو** را باز کنید. پردازش محلی در ویرایشگر دسکتاپ انجام می‌شود.

1. گفت‌وگویی را که می‌خواهید بهبود دهید انتخاب کنید.
2. DeepFilterNet3 را از مدیریت مدل‌ها نصب کنید و اجرای محلی را انتخاب کنید.
3. پیش از اعمال نتیجه پیشنهادی، آن را در برابر نسخه اصلی گوش کنید. واگرد، ویرایش پذیرفته‌شده را در پروژه برمی‌گرداند.

## دانلود و نیازمندی‌ها {#download-and-requirements}

هویت مدل: `deepfilternet3`، نسخه **3.0.0**.

اندازه تقریبی دانلود: **8.33 MiB**. حداقل حافظه سیستم: **2 GiB**. این نیازمندی کاتالوگ به کل حافظه اشاره دارد، نه حافظه‌ای که اکنون آزاد است. استنتاج نیز به حافظه آزاد کافی نیاز دارد و نصب به فضای کاری نیازمند است؛ حافظه تضمین‌کننده سرعت نیست.

پلتفرم‌های منتشرشده: macOS arm64، macOS x64، Linux arm64، Linux x64، Windows arm64 و Windows x64. زمان‌اجرای بسته‌بندی‌شده‌ای که با آن مطابقت داشته باشد نیز لازم است.

در Windows، آخرین [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) پشتیبانی‌شده و سازگار با برنامه دسکتاپ را نصب کنید: برای ساخت x64، نسخه **x64** و برای ساخت ARM64، نسخه **ARM64**. موتورهای بومی ONNX و Sherpa به این کتابخانه‌ها نیاز دارند؛ دانلود مدل آن‌ها را در بر نمی‌گیرد. برای استفاده از برنامه نیازی به Visual Studio نیست.

ابزارها → مدیر مدل‌ها فایل‌های منتشرشده را دانلود و هش‌های SHA-256 ثبت‌شده آن‌ها را تأیید می‌کند. پردازش از فایل‌های نصب‌شده و محلی استفاده می‌کند. برای دیدن اعلان‌ها، تعمیر نصب یا تغییر محل ذخیره‌سازی، ذخیره‌سازی و تأیید را در مدیر مدل‌ها باز کنید.

| فایل | اندازه تقریبی دانلود |
| --- | --- |
| deepfilter.onnx | 8.21 MiB |
| deepfilter-auxiliary.bin | 124.00 KiB |
| config.json | 370 bytes |

## آزمون بسته‌بندی‌شده چه چیزی را بررسی می‌کند {#what-the-packaged-test-checks}

مورد: `deepfilter-enhancement`؛ عملیات: `speech-enhancement`.

ورودی: بخشی از سخنرانی آغاز به کار جان اف. کندی که در مالکیت عمومی است و به صدای مونو با فرکانس 48 kHz بازنمونه‌برداری شده و به آن خش‌خش بازتولیدپذیر افزوده شده است.

صدای رمزگشایی‌شده باید نمونه‌های متناهی داشته باشد، هندسه صوتی موردانتظار را حفظ کند، با نمونه‌های ورودی تفاوت داشته باشد و سیگنالی شنیدنی و غیرصفر داشته باشد. تفاوت در سرآیند فایل به‌تنهایی تغییر محسوب نمی‌شود.

بسته شبانه همراه آزمون، فایل‌های واقعی مدل را دانلود می‌کند و از طریق زمان‌اجرای بسته‌بندی‌شده استنتاج را درخواست می‌کند. اگر مدل ضروری در کاتالوگ نباشد، مورد مربوط به آن شکست می‌خورد. نبود موتورهای بومی در پلتفرم‌های پشتیبانی‌شده کاتالوگ نیز باعث شکست می‌شود. این بررسی‌های پرهزینه جدا از مجموعه آزمون معمول مرورگر اجرا می‌شوند. اجرای موفق، اجرای پایه مدل و ساختار قابل‌استفاده خروجی را تأیید می‌کند؛ کیفیت شنیداری یا دقت مدل را روی ضبط شما ثابت نمی‌کند.

## بررسی نتیجه {#review-the-result}

- این مدل برای بهبود گفتار است؛ ابزار حذف نویز موسیقی یا بازسازی مبتنی بر پروفایل نویز نیست.
- خروجی تغییریافته و شنیدنی نشان می‌دهد پردازش اجرا شده است، اما ثابت نمی‌کند همه نویز حذف شده یا هیچ جزئیات مطلوبی از دست نرفته است.

[پوشش آزمون مدل‌ها و محدودیت‌ها](/reference/local-models/) · [راهنمای پردازش محلی](/help/local-processing/)

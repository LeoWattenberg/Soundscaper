---
title: "SigLIP2 base patch16 224"
description: "Representa imágenes de video muestreadas y texto en un espacio numérico compartido para la búsqueda visual local."
editUrl: false
---
<!-- docs-ai-provenance: {"model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"75fbb3732c3884e3166ccfd56fe37dbb2c0d91a1b889abaf1c779e66a1c25871","targetLocale":"es"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

Representa imágenes de video muestreadas y texto en un espacio numérico compartido para la búsqueda visual local.

## Disponibilidad actual {#current-availability}

Las compilaciones de escritorio incluyen el motor onnxruntime-node 1.29.0 requerido para macOS arm64, Linux arm64, Linux x64, Windows arm64 y Windows x64. Instala los pesos de este modelo con Model Manager y luego ejecuta su tarea localmente. Estas son plataformas de destino compatibles; consulta el informe de pruebas nocturnas para conocer los resultados de un paquete y equipo concretos. La compilación de escritorio no incluye este motor para macOS x64, aunque los pesos del modelo figuren para esa plataforma.

## Usar este modelo {#use-this-model}

Abre **Framescaper: Herramientas → Buscar → Indexar video y luego Herramientas → Buscar → Búsqueda indexada** en una plataforma con el entorno de ejecución nativo requerido. La asistencia local se ejecuta en el editor de escritorio.

1. Selecciona un video e instala SigLIP2 con Administrar modelos.
2. Ejecuta Indexar video para analizar los fotogramas muestreados. Instala también PP-OCRv4 si quieres incluir su paso opcional de lectura de texto.
3. Usa Búsqueda indexada y revisa los momentos coincidentes del video antes de decidir cómo utilizarlos.

## Descarga y requisitos {#download-and-requirements}

Identidad del modelo: `siglip2-base-patch16-224`, versión **2.0.0**.

Tamaño de descarga aproximado: **393.25 MiB**. Memoria mínima del sistema: **8 GiB**. Este requisito del catálogo se refiere a la memoria total, no a la memoria libre en ese momento. La inferencia también necesita suficiente memoria libre, y la instalación requiere espacio de trabajo; la memoria no garantiza la velocidad.

Plataformas publicadas: macOS arm64, macOS x64, Linux arm64, Linux x64, Windows arm64 y Windows x64. También se requiere un entorno de ejecución empaquetado compatible.

En Windows, instala la versión compatible más reciente de [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) que corresponda a la aplicación de escritorio: **x64** para una compilación x64 o **ARM64** para una compilación ARM64. Los motores nativos ONNX y Sherpa necesitan estas bibliotecas; las descargas del modelo no las incluyen. No hace falta Visual Studio para usar la aplicación.

Herramientas → Model Manager descarga los artefactos publicados y verifica sus resúmenes SHA-256 registrados. El procesamiento usa localmente los archivos instalados. Abre Almacenamiento y verificación en Model Manager para consultar avisos, reparar una instalación o cambiar la ubicación de almacenamiento.

| Artefacto | Tamaño de descarga aproximado |
| --- | --- |
| vision_model_int8.onnx | 90.17 MiB |
| text_model_int8.onnx | 270.31 MiB |
| tokenizer.json | 32.77 MiB |
| config.json | 435 bytes |
| preprocessor_config.json | 394 bytes |

## Qué comprueba la prueba empaquetada {#what-the-packaged-test-checks}

Caso: `siglip-image-text-embedding`; operación: `image-text-embedding`.

Entrada: dos fotografías: el retrato de dominio público de la astronauta Eileen Collins, de la NASA, y la fotografía CC0 de un gato de Chelsea. Cada una se ajusta a un fotograma separado con tiempos de fotograma de origen explícitos, de modo que los detectores reciben un rostro humano y un objeto claramente reconocible.

Los vectores de incrustación deben tener las dimensiones esperadas y contener valores finitos, distintos de cero y no constantes.

El paquete nocturno con pruebas descarga artefactos reales del modelo y solicita inferencia a través del entorno de ejecución empaquetado. Si falta del catálogo un modelo obligatorio, su caso falla. Si faltan motores nativos en plataformas admitidas por el catálogo, la prueba falla. Estas comprobaciones costosas se ejecutan aparte de la suite normal del navegador. Una ejecución correcta confirma la ejecución básica del modelo y una estructura de salida utilizable; no demuestra calidad perceptiva ni precisión con tu grabación.

## Revisar el resultado {#review-the-result}

- Este modelo genera incrustaciones, no subtítulos ni una descripción autoritativa de cada fotograma.
- La prueba empaquetada usa la entrada de fotogramas y prototipos de etiquetas de texto, y ejercita tanto las redes de imagen como las de texto. Comprueba que los vectores sean válidos, no que la clasificación sea exacta.

[Cobertura y limitaciones de las pruebas del modelo](/reference/local-models/) · [Guía de procesamiento local](/help/local-processing/)

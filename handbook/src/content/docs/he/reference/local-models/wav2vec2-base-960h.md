---
title: "יישור מילים של wav2vec2 base 960h"
description: "יישור תמליל קיים באנגלית למילים המדוברות בהקלטה."
editUrl: false
---
<!-- docs-ai-provenance: {"factPacketSha256":"55565379d1a86871ec57d126fccb26ccdc947149b0bf17fc54e0b7cabab9c8dd","model":"gpt-5.6-luna","modelDigest":"manual","operation":"translate","promptVersion":"docs-translate-v1","schemaVersion":1,"sourceLocale":"en","sourceSha256":"55565379d1a86871ec57d126fccb26ccdc947149b0bf17fc54e0b7cabab9c8dd","targetLocale":"he"} -->

<!-- Generated by `node scripts/docs-local-models.mjs`. Do not edit. -->

יישור תמליל קיים באנגלית למילים המדוברות בהקלטה.

## זמינות נוכחית {#current-availability}

גרסאות שולחן העבודה כוללות את מנוע onnxruntime-node 1.29.0 הדרוש עבור macOS arm64, Linux x64, Linux arm64, Windows x64 ו-Windows arm64. התקינו את משקולות המודל דרך מנהל המודלים, ואז הפעילו את המשימה שלו מקומית. אלה יעדי הבנייה הנתמכים; עיינו בדוח בדיקות הלילה לקבלת תוצאות עבור חבילה ומחשב מסוימים.

## שימוש במודל {#use-this-model}

פתחו את **ניתוח → דיבור → תמלול וכתוביות, או כלים → עיבוד מקומי מתקדם → יישור מילים** בפלטפורמה עם סביבת הריצה המקורית הדרושה. הסיוע המקומי פועל בעורך שולחן העבודה.

1. בחרו את הדיבור והכינו או בדקו את התמליל באנגלית.
2. התקינו את מודל היישור דרך מנהל המודלים.
3. הפעילו יישור, ואז האזינו להקלטה תוך כדי בדיקת גבולות המילים המוצעים לפני אישורם.

## הורדה ודרישות {#download-and-requirements}

זהות המודל: `wav2vec2-base-960h`, גרסה **1.0.0**.

גודל הורדה משוער: **360.38 MiB**. זיכרון מערכת מינימלי: **4 GiB**. דרישת הקטלוג מתייחסת לזיכרון הכולל, לא לזיכרון הפנוי כרגע. ההסקה דורשת גם די זיכרון פנוי, וההתקנה דורשת שטח עבודה; הזיכרון אינו מבטיח מהירות.

פלטפורמות שפורסמו: macOS arm64, Linux x64, Linux arm64, Windows x64, Windows arm64. נדרשת גם סביבת ריצה ארוזה תואמת.

ב-Windows התקינו את [Microsoft Visual C++ v14 Redistributable](https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist?view=msvc-170) העדכני הנתמך, התואם לאפליקציית שולחן העבודה: **x64** לגרסת x64 או **ARM64** לגרסת ARM64. מנועי ONNX ו-Sherpa המקוריים זקוקים לספריות האלה; הורדות המודלים אינן כוללות אותן. אין צורך ב-Visual Studio כדי להשתמש באפליקציה.

כלים → מנהל המודלים מוריד את הפריטים שפורסמו ומוודא את ערכי ה-SHA-256 המתועדים שלהם. העיבוד משתמש בקבצים המותקנים באופן מקומי. פתחו את אחסון ואימות במנהל המודלים כדי לעיין בהודעות, לתקן התקנה או לשנות את מיקום האחסון.

| פריט | גודל הורדה משוער |
| --- | --- |
| model.onnx | 360.38 MiB |

## מה בודקת הבדיקה של החבילה {#what-the-packaged-test-checks}

מקרה: `wav2vec2-word-alignment`; פעולה: `word-alignment`.

קלט: נאום קנדי שנמצא ברשות הציבור, בדגימה של 16 kHz, יחד עם התמליל הידוע שלו באנגלית כקלט מאומת נפרד.

יש לייצג כל מילה שסופקה בתזמון תקין ומסודר בתוך השמע; זמני היישור המדויקים אינם מוגדרים מראש.

חבילת הלילה הכוללת בדיקות מורידה קובצי מודל אמיתיים ומבקשת הסקה דרך סביבת הריצה הארוזה. אם מודל נדרש חסר בקטלוג, הבדיקה שלו נכשלת. מנועים מקוריים חסרים גורמים לכישלון בפלטפורמות הנתמכות בקטלוג. הבדיקות הכבדות האלה רצות בנפרד מחבילת בדיקות הדפדפן הרגילה. מעבר בבדיקה מאשר שהמודל פועל באופן בסיסי ומחזיר מבנה תוצאות שימושי; הוא אינו מעיד על איכות תפיסתית או על דיוק בהקלטה שלכם.

## סקירת התוצאה {#review-the-result}

- המודל מיישר מילים שסופקו באנגלית; הוא אינו מתרגם ואינו מתקן תמליל שגוי.
- מילים חוזרות, דיבור ברקע ומילים חסרות בתמליל עלולים ליצור גבולות שגויים. הבדיקה בוחנת תזמוני מילים מסודרים ובטווח בהתאם לרשימת המילים שסופקה, ולא דורשת התאמת תזמון מושלמת.

[כיסוי בדיקות המודלים והמגבלות שלהן](/reference/local-models/) · [מדריך לעיבוד מקומי](/help/local-processing/)
